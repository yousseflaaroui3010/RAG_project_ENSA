# DECISIONS (ADR-lite: one row per real choice)

| Date | Task | Decision | Options considered | Why | Owner |
|------|------|----------|--------------------|-----|-------|
| 2026-07-20 | SETUP-000 | TYPECHECK_CMD = `uv run ruff check .` | mypy/pyright as new dep / ruff / leave empty | pinned stack has no type checker; ruff is already pinned, no new dependency | setup |
| 2026-07-20 | SETUP-000 | Branch naming = `feat/S<sprint>-ST-<nn>-<slug>` | kit `task/T-xxx` / Phase 2 `ST-nn` | docs/phase2 is signed and ST-01..ST-52 already exist in the plan | setup |
| 2026-07-20 | SETUP-000 | CI package manager = uv (setup-uv, `uv sync --frozen`) | keep npm block / uv | Sanad is Python-only, no JS toolchain (ADR-02, ADR-10) | setup |
| 2026-07-20 | Phase3-S0 | Claude maintains BUILD-STATE/CHANGELOG/DECISIONS directly (masters editable) | immutable masters + human fold-delta / direct edit | the fold indirection created per-turn friction the user rejected | human + orchestrator |
| 2026-07-20 | Phase3-S2 | Contract of record stays frozen at docs/phase2/openapi.yaml; served copy docs/api/openapi.yaml created by ST-51 as the drift-test target | edit the phase2 copy / keep a single copy | docs/phase2 is the write-locked signed pack; ST-51 owns the served contract (ADR-13) | orchestrator |
| 2026-07-20 | ST-02 | Cloud provider = Google Gemini (langchain-google-genai, free tier); free local models (Mistral, Llama) via Ollama | OpenAI / Gemini / hosted Mistral / Grok (xAI) | user chose Gemini free tier + free local models over OpenAI; provider stays config-swappable (ADR-06); Gemini terms to review before non-public docs (OR-2) | user + orchestrator |
| 2026-07-20 | ST-02 | pytest pythonpath = ["."] for the flat-layout root config module | src/ layout / installed package / pythonpath | package=false flat app (§11); pythonpath is the minimal fix so `from config import` resolves under pytest | orchestrator |
| 2026-07-25 | ST-10 | db/repo.py data access = stdlib `sqlite3` with a single `_connect_raw()` call site + thin per-table insert functions | stdlib sqlite3 (minimal, no new dep) / SQLAlchemy Core-ORM (heavier, unvetted new dep for a 6-table registry) | ST-10 scope says minimal, no new-dep needed for six tables + cascade rules; stdlib keeps PRAGMA foreign_keys enforcement in one auditable place per docs/phase2/CLAUDE.md hard rule | b2 |
| 2026-07-25 | ST-10 | PR tooling = human installs `gh` CLI + `gh auth login`; agents use `gh` for PR open/status, humans still press merge | install gh (one-time setup, agents can read CI) / manual prefilled compare URL per story (a click x ~40 stories, agents blind to CI) / long-lived GH_TOKEN in env (standing credential, larger blast radius) | b1 correctly refused to extract the Git Credential Manager token for REST calls; gh keeps auth in the tool that owns it, and merge stays a human action either way | user + orchestrator |
| 2026-07-25 | ST-10 | `.claude/agent-memory/` is gitignored, not committed | commit it (agent memory becomes shared reviewable state) / gitignore it (per-machine scratch) | it duplicated BUILD-STATE Blockers and went stale within the hour (its gh-gap note was wrong minutes after gh was installed); an unreviewed second memory that can contradict the flight recorder is the exact failure fixed earlier the same day | orchestrator (delegated by user) |
| 2026-07-28 | ST-10 | `init_db` executes schema.sql statement-by-statement via `conn.execute()`; it never calls `executescript()` and never commits | keep `executescript()` and drop only the trailing `commit()` (documenting the residual quirk) / execute each DDL statement individually | `executescript()` issues its own implicit COMMIT of any pending transaction, verified empirically: with the trailing `commit()` removed it still force-committed a write made earlier in the same `session()`. Option A left the required rollback regression test permanently red, so it was not a fix. Option B makes `session()` the sole transaction owner and touches no signed artifact - db/schema.sql is unchanged | b2 + orchestrator |
| 2026-07-28 | integration | Bring stale branches up to date by merging `main` INTO the branch, never by rebasing | rebase onto main (linear branch history, but rewrites commits and needs a force-push) / merge main into the branch | the `protect-main` ruleset requires branches be up to date before merge (strict status checks), but rebasing MB's pushed commits would rewrite their SHAs and force-push a shared branch, breaking MB's clone. Squash-merge collapses the branch to one commit on main anyway, so linear history is preserved regardless of the branch's internal shape | orchestrator |
| 2026-07-28 | workflow | No AI attribution in commits, PRs, or issues: no `Co-Authored-By` naming an assistant, no `[AI]` subject marker, no "generated with" footer. Human co-authors ARE still credited | keep both markers / drop the trailer only / drop both / drop both and rename CHANGELOG-AI.md | graded academic work must show the team's names only, and the `[AI]` marker was as much of a tell as the trailer; CI enforces `INTENT:` and never `[AI]`, so the gate is unaffected; CHANGELOG-AI.md keeps its filename because renaming it touches every agent definition for no reader-visible gain | user + orchestrator |
| 2026-07-28 | chore/journal-refresh | `merge=union` on CHANGELOG-AI.md and DECISIONS.md via .gitattributes; BUILD-STATE.md deliberately excluded | leave the default line merge / union all three journals / union the append-only two only / go back to a per-branch delta folder | three of three branches merged on 2026-07-28 (PRs #7, #8, #9) conflicted in the journals and in no code file at all; union is exactly right for append-only logs, but BUILD-STATE is a mutable snapshot so unioning it would interleave two contradictory pictures of the same moment; the delta-folder scheme was already tried and rejected for per-turn friction (Phase3-S0) | orchestrator |
| 2026-07-28 | workflow | Leave the commits already on `main` that carry the assistant trailer unrewritten | rewrite history + force-push / leave as-is / defer | scrubbing them needs a force-push to `main`, which git-discipline forbids and the `protect-main` ruleset blocks via `non_fast_forward`; it would also break MB's clone and invalidate every SHA cited in BUILD-STATE | user |
| 2026-07-28 | ST-11 | Single `workspaces.py` module, not a `workspaces/` package | package with submodules (rules/, errors.py) / one flat module | only five verbs plus a legal-flag toggle over one table; a package would split ~150 lines across files for no navigational win. Revisit if ST-12+ adds enough workspace-adjacent logic | b2 |
| 2026-07-28 | ST-11 | Domain errors = plain `Exception` subclasses (`WorkspaceNotFoundError`, `DuplicateWorkspaceNameError`) under a `WorkspaceError` base, raised by catching `sqlite3.IntegrityError` at the UNIQUE-constraint call sites rather than pre-checking name existence | pre-check name via SELECT then raise / catch IntegrityError and translate | pre-checking is a TOCTOU race (two callers could pass the check before either inserts); catching the constraint violation reuses the schema's own UNIQUE guarantee and needs no extra query | b2 |
| 2026-07-28 | ST-11 review fix | `IntegrityError` -> `DuplicateWorkspaceNameError` translation now requires the literal sqlite3 text `UNIQUE constraint failed: workspace.name` to be present, else re-raise | keep the bare `except sqlite3.IntegrityError` / inspect `exc.args`/`sqlite_errorname` via a driver API / string-match the known constraint text | reviewer proved the bare catch mislabels any IntegrityError (e.g. NOT NULL on folder_path) as a name collision; sqlite3 does not expose a structured constraint-name API in the stdlib driver, so matching SQLite's own (stable, documented) error text is the narrowest fix that needs no new dependency | b2 |
| 2026-07-28 | ST-11 review fix | Workspace name length limits (1-100) live in `config.py` (`workspace_name_min_length`/`workspace_name_max_length`), enforced by `workspaces._validate_name` on create/rename, raising `InvalidWorkspaceNameError(WorkspaceError)` | hardcode 1/100 in workspaces.py / put limits in config.py | CLAUDE.md hard rule: no magic literals in module code, all tunables in config.py; the limits also mirror docs/phase2/openapi.yaml WorkspaceCreate/WorkspaceUpdate exactly, so one source of truth for both | b2 |
| 2026-07-28 | ST-11 review fix | Default-path bootstrap: `db/repo._connect_raw` calls `init_db(conn)` on every connect, rather than adding a separate app-startup hook or a lazy once-per-process cache | bootstrap in `_connect_raw` (every connect) / bootstrap once in a new app-startup function outside db/repo.py / lazy cache keyed by resolved path | ST-10 follow-up 4: the default `sqlite_db_path` had no caller that ever created the tables. `_connect_raw` is already the single sqlite3.connect() call site (db/repo.py's own hard rule), so bootstrapping there covers both explicit and default paths with no new call site; schema.sql is entirely `CREATE TABLE IF NOT EXISTS` and DDL is outside Python sqlite3's implicit DML transaction, so repeating it on every connect is idempotent and never fights `session()`'s rollback. A once-per-process cache would save a few no-op statements per connect at the cost of a second piece of state to reason about, not worth it for a 6-table local-first registry | b2 |
| 2026-07-28 | ST-11 2nd review fix | Bootstrap moved off the connect site entirely: `_connect_raw` is back to pure connect+PRAGMA; a new `repo.ensure_schema(db_path)` owns mkdir+init_db and is called only from `session()` (write path). `get_connection()` (read path) raises `RegistryNotFoundError` instead of connecting to a path that does not exist | keep bootstrap in `_connect_raw` (every connect, prior decision) / bootstrap in `ensure_schema`, called by write paths only, reads raise instead of connecting / lazy once-per-process cache in `_connect_raw` | the prior decision made every connect - including reads - silently create the directory tree and an empty db, so `ws.list_workspaces(db_path="/typo/deep/x.db")` returned `[]` instead of erroring: a mistyped or unmounted `SQLITE_DB_PATH` became indistinguishable from a fresh install, corrupting F-01 criterion 2's own exit gate. Splitting the seam so only `session()` (writes) calls `ensure_schema` fixes this with no new call site for `sqlite3.connect()` and needs no cache; a real migration runner is ST-12's problem, not a hidden side effect of connecting | b2 |
| 2026-07-28 | ST-12 ownership | ST-12 Content hashing + change-detection is owned by MB, not YL | follow BUILD-PLAN line 60 as written (YL) / reassign to MB / leave unassigned until someone starts it | the plan assigns ST-10, ST-11 AND ST-12 all to YL, but MB wrote every line of ST-10 while it was assigned to YL, so the recorded split already did not match reality. Sanad is graded academic work and contribution has to be defensible per story, so the deviation is made explicitly here rather than left to whoever types first. YL owns ST-11 (merged, PR #11); MB owns ST-12 | user + orchestrator |
| 2026-07-28 | ST-11 | Workspace names are normalized once in `_normalize_name`, which returns the value to persist, rather than validated and stored separately | validate raw and store raw (breaks the contract on padded input) / strip at each call site (a convention every future caller must remember) / normalize once and return the value to persist | validating a stripped copy while persisting the raw string is what produced the defect: a 100-char name plus a trailing space stored at 101 against a signed maxLength of 100, and "HR" and " HR " coexisted despite workspace.name being UNIQUE. Returning the value makes the normalization impossible to forget instead of a rule; the reviewer confirmed create and rename are the only two writers of workspace.name | orchestrator |
| 2026-07-28 | CR-02 | UI is server-rendered templates off the existing FastAPI host, with hypermedia attributes for partial updates and hand-written CSS. Gradio removed | keep Gradio with a heavy custom theme / server-rendered templates plus hypermedia / a JS framework with a build toolchain / custom UI for S1-S2 only and Gradio for S3 | ADR-02 accepted "less visual polish and layout control" in writing; the human withdrew that acceptance because the interface is judged by an examination jury. A JS framework was rejected: it breaks ADR-10, adds a build step and a second language for a two-person team, and spends the same weeks the unbuilt answer pipeline needs. Templates keep ADR-10 intact because the hypermedia library is one vendored static file with no npm and no bundler. The change also DELETES the gr.mount_gradio_app seam and both failure modes ADR-13 logged against it | user + orchestrator |
| 2026-07-28 | CR-02 | UX-01 names no technology, so a stack change does not bump it | write the stack into the UX spec / keep the UX spec technology-free | discovery artifacts stay technology-free by chain law, and the practical payoff showed up immediately: swapping the entire UI stack left the screen specification untouched and unversioned | orchestrator |
| 2026-08-01 | ST-12 | Fingerprint = `sha256:<hex digest>:<byte size>` serialized into the existing `document.content_hash` column | add a `file_size` column to `document` / pack both fields into `content_hash` / hash only, no size guard | the exit gate demands a size+hash collision guard, but the signed reference DDL (architecture §7.3) has no size column and docs/phase2/ is write-locked, so widening the table would be an unescalated deviation from a signed artifact; the packed form is documented, parseable and round-trips, so nothing is lost and no migration is needed for a schema created by `CREATE TABLE IF NOT EXISTS`. Reversible: if a size column is ever escalated and approved, `Fingerprint.parse` is the single read site to change | orchestrator |
| 2026-08-01 | ST-12 | A registry row with status `removed` classifies a returning file as NEW, not UNCHANGED; and if it is still absent it produces no report row at all | compare hashes regardless of status / special-case `removed` | ST-17 deletes a removed file's chunks, so identical bytes still need re-ingesting or the restored file stays permanently unanswerable while the report calls it Unchanged; and re-reporting an already-Removed absent file every sync would put a permanent phantom row in the user's file report. `failed`/`skipped` deliberately do NOT get this treatment: they were never reported as gone, so their disappearance is still a Removed row | orchestrator |
| 2026-08-01 | ST-12 | An unparseable stored `content_hash` resolves toward CHANGED, never UNCHANGED | treat as unchanged / treat as changed / raise | re-ingesting costs one conversion, whereas guessing UNCHANGED on an identity the module cannot read strands stale passages in answers permanently; this is also what makes a future digest-algorithm change safe (old rows simply reprocess) | orchestrator |
| 2026-08-01 | ST-12 | `scan_folder` is non-recursive and raises `FolderNotFoundError` rather than reporting an empty folder | recurse into subfolders / one folder only; missing folder == empty folder / missing folder raises | a workspace is one folder of documents (F-01), and silently walking subfolders ingests content the user never pointed at; separately, an unplugged drive must never be indistinguishable from a folder the user emptied, because the second means "delete every chunk in this workspace" | orchestrator |
| 2026-08-01 | ST-12 | Keep the packed `sha256:<digest>:<size>` fingerprint; do NOT add a `file_size` column now | keep packed / add the column while data/ is still empty / drop the size guard | revisited deliberately, because "while nobody has a real registry yet" is the cheapest this schema change will ever be (there is no migration runner, and `CREATE TABLE IF NOT EXISTS` will not add a column to an existing file). Still declined: the packed form already satisfies the exit gate, the size is not read by anything except the comparison, and a fast-path that skips hashing on a size mismatch would save nothing because ST-13 re-reads the file to convert it anyway. On graded work, conformance to the signed §7.3 DDL outweighs data-model elegance for a field with one consumer. Revisit if ST-17 or ST-18 needs size as a queryable column; `Fingerprint.parse` is the single read site | orchestrator |
| 2026-08-01 | ST-12 | Leave data-layer follow-ups 1, 2 and 6 out of PR #14; they get their own chore branch | fold into ST-12 as originally planned / separate branch | BUILD-STATE originally said "fold into ST-12", but 5 and 7 were genuinely in scope (ST-12 touches the same code paths) whereas 1, 2 and 6 are unrelated to change detection; adding them would put drive-by edits in a diff a reviewer is trying to grade against one story's exit gate, which .claude/rules/00-global.md's scoped boy-scout rule forbids by name | orchestrator |
| 2026-08-01 | ST-12 | Unreadable files are COLLECTED by `scan_folder` (ScanResult.unreadable), never raised, and are excluded from the REMOVED sweep | let the error propagate / collect per file / skip them silently | PRD F-02 #3 requires the other files to complete, so propagating loses a whole sync to one bad file; skipping silently is worse than either, because an unreadable file would then be absent from `fingerprints` and get swept up as REMOVED, making ST-17 delete the chunks of a document still on disk. Removed must mean "gone from disk", never "we had trouble with it". Kept in a separate list from `unsupported` because they map to different sync_item.result values (Failed vs Skipped) | orchestrator |
| 2026-08-01 | ST-12 | One mocked seam in the change-detection test suite (`_fail_reads_of`), everywhere else acts for real | chmod the file / replace it with a directory / patch compute_fingerprint | neither real option works portably: chmod does not remove an owner's read access on Windows, and swapping in a directory changes `is_file()` so scan_folder skips the entry before reading it -- the first version of these tests did exactly that and passed for the wrong reason. The unmocked half of the chain is still covered by test_unreadable_file_raises_a_domain_error against a real filesystem | orchestrator |
| 2026-08-01 | ST-13 | Flat `conversion.py` at the repo root, not `ingestion/convert.py` as architecture §11 draws it | follow §11's tree exactly / stay flat like the modules already on main / create the package now and move change_detection.py into it | §11's tree is signed, but main already deviates from it deliberately and consistently: `workspaces.py` and `change_detection.py` sit at the root, and pytest's `pythonpath = ["."]` is configured for exactly that flat layout (ST-02 decision). Introducing the package for one new module would leave the repo half-migrated, which is worse than either shape. The move is a mechanical rename plus one import line per file and belongs in its own chore branch once there are enough ingestion modules to justify it | orchestrator |
| 2026-08-01 | ST-13 | `convert_file` returns FAILED/SKIPPED for a bad document and never raises; a last-resort `except Exception` at the single converter boundary translates undeclared third-party crashes into one Failed row, logged with a full traceback | let document errors propagate and make every caller wrap it / catch the documented exception types only / catch the documented types plus a logged last resort | PRD F-02 criterion 3 ("every other file completes") is a Highest-priority signed criterion, and both parsers have ALREADY been caught throwing something undeclared on hostile input (pymupdf4llm's TypeError on an encrypted PDF). A tidy exception list is worth less than the criterion. `.claude/rules/backend.md` says fail loud, so the catch is narrow in scope (it wraps only third-party parser calls, never our own logic) and logs `logger.exception`, and a test asserts the traceback is present -- containing a surprise must never mean hiding it | orchestrator |
| 2026-08-01 | ST-13 | DOCX files are validated as real OOXML packages (zip opens, `word/document.xml` present) BEFORE markitdown sees them | trust markitdown to reject what it cannot parse / sniff the file's magic bytes / open the zip and require the Word main part | markitdown 0.1.5 does not reject: given an unparseable .docx it falls through to another converter and returns that output as a SUCCESS (verified: a corrupted file came back as its own raw bytes, a valid non-Word zip as a directory listing). Reported Added, that content would be chunked, embedded and cited to a user -- the worst failure mode available to this module, because nothing raises. Magic-byte sniffing would catch the non-zip half only; the main-part check catches both and needs no new dependency | orchestrator |
| 2026-08-01 | ST-13 | TXT/MD decoding is strict, with `text_file_encoding` defaulting to `utf-8-sig`; a file in another encoding is reported Failed | strict UTF-8 / try UTF-8 then fall back to latin-1 / strict utf-8-sig | a latin-1 fallback never raises, so a mis-encoded French document would be indexed as 'CongÃ©s payÃ©s' and only surface when an answer quoted the mojibake back at the user -- silent corruption of the product's core promise, traded for the convenience of not showing an error. `utf-8-sig` over plain `utf-8` because Windows editors prepend a byte-order mark that otherwise survives as an invisible first character of the first heading and breaks the §7.5 chunker's heading match on a file that looks perfect in every editor | orchestrator |
| 2026-08-01 | ST-13 | A converted document with no text is SKIPPED (not FAILED), via one shared emptiness gate for every format, with PDF-specific wording | detect scanned PDFs by inspecting the page tree for images / one emptiness gate after conversion / per-format emptiness rules | PRD F-16 binds V1 to "Skipped with the reason stated" for a scanned PDF, and Skipped vs Failed is a promise to the user: "nothing to index here" versus "this file is broken, go fix it". Inspecting the page tree would answer a narrower question (is there an image?) than the one that matters (did we get text?), and an empty .txt and a DOCX of blank paragraphs need the same verdict anyway. Only the REASON forks by format, because telling someone who scanned a contract that "the file has no text in it" reads as a bug in Sanad rather than a description of their file | orchestrator |
| 2026-08-09 | ST-14 | Flat `chunking.py` at the repo root, not `indexing/chunking.py` as architecture §11 draws it | follow §11's tree / stay flat like every module already on main / start the package now | identical reasoning to the ST-13 row: main is consistently flat (`workspaces.py`, `change_detection.py`, `conversion.py`) and pytest's `pythonpath = ["."]` is configured for it. Introducing `indexing/` for one module leaves the repo half-migrated, which is worse than either shape. The move is one chore branch once enough modules exist to justify it | orchestrator |
| 2026-08-09 | ST-14 | Children are FIXED CHARACTER windows, not sentence- or token-aware splits | character windows / split on sentence boundaries then pack / a recursive separator splitter | the story's exit criterion is a VERIFIABLE 100-character overlap, and it is the character count that guarantees every embedded input stays under E5's 512-token ceiling (§7.5). A sentence-aware boundary produces variable-length chunks, which turns both guarantees into approximations and the exit criterion into something that cannot be asserted. The cost is real and accepted: a window can start mid-word. Parents, which are what the model actually READS, do get paragraph-aware splitting, so the mid-word cut only ever affects the search key, never the answer context | orchestrator |
| 2026-08-09 | ST-14 | A merged parent carries the RANGE of its headings; separator is " ... " | first heading only / range with " - " / range with " ... " | the label is shown to the user as the source (PRD F-03), so first-heading-only mislabels every passage after the first section in a merged parent. " - " was tried and rejected on a realistic document: real headings contain dashes, so "Article 2 - Duree - Article 4 - Rupture" is unreadable as a range. One imprecision is knowingly kept: an unlabelled preamble too short to stand alone merges forward and is cited under the following heading, because the alternative is a parent of a few dozen characters that gives the model nothing. It is bounded to at most one parent per document | orchestrator |
| 2026-08-09 | ST-14 | SUPERSEDED 2026-08-09 by the derived-parent-id row below. Parent ids come from `db.repo.new_id()` rather than a local uuid helper | local `uuid.uuid4()` call in chunking.py / import repo.new_id | zero-duplication rule, and these ARE the same kind of app-generated identifier §7.4 defines (SQLite has no gen_random_uuid()). The parent id becomes a filename in the §7.5 parent store and a payload field in Qdrant, so it belongs to the same id space as every other row. Noted as a mild coupling of a pure-text module to the data layer; revisit only if `new_id` ever grows database-specific behaviour | orchestrator |
| 2026-08-09 | ST-14 | SUPERSEDED 2026-08-09 by the derived-parent-id row below; the constraint it describes was fixed rather than carried. Parent ids are RANDOM per run (`repo.new_id()`), so `chunk_document` is NOT idempotent: the same document chunked twice yields a different id set. Recorded as a constraint ST-16/ST-17 must handle, not fixed here | leave random and make the caller clean up / derive the id from (workspace, file, piece index) so re-chunking overwrites in place / hash the piece text | proven, not assumed: chunking the same input twice returns disjoint id sets. The hazard is downstream and real -- §7.5 stores parents at `data/parents/<workspace_id>/<parent_id>.json` and Qdrant payloads carry `parent_id`, so re-ingesting a CHANGED file mints all-new ids and orphans the old JSON files; worse, any vector that survives the rewrite points at a parent file that no longer exists, which is a search hit that resolves to nothing. A deterministic id would make re-ingestion self-healing (same file overwritten in place, no orphans), and is the better design, but it changes `chunk_document`'s signature (it would need the workspace id, which a pure text splitter does not currently take) and the cleanup semantics belong to the story that owns the stores. Deliberately left as a WRITTEN constraint rather than a silent one | orchestrator |
| 2026-08-09 | ST-13/ST-14 | Per-file logging lives in the SYNC LOOP (ST-17), not in the pure ingestion modules; `conversion.py`'s logger is the one exception and only fires for undeclared third-party crashes | log in every module / log only in the caller / log in the caller plus a narrow exception for surprises | rubric review found `conversion.py` carrying a logger while `change_detection.py` and `chunking.py` carry none, with no written reason -- an inconsistency that reads as an oversight. It is a real choice: these three modules are pure transforms called once per file, so logging inside them would emit N lines per sync with no request id and no per-file outcome to attach them to, which is exactly the 'entering function' noise .claude/rules/backend.md warns against. ST-17 owns the per-file report row and is where a log line can carry the file, the outcome and the run id. `conversion.py` keeps its logger because a parser crashing in an undeclared way is information no caller can reconstruct | orchestrator |
| 2026-08-09 | ST-14 | REPLACES the two rows above. Parent ids are DERIVED, not random: `uuid5(namespace, "<source_file>\x00<index>")`, so chunking the same document twice yields the same ids | keep random ids and make ST-17 clean up after them / derive from file name + position / derive from the piece's content hash | the rubric row 'same thing twice: assumes twice, makes the second harmless' failed outright, proven by chunking one input twice and getting disjoint id sets. §7.5 keys the parent store on this id and Qdrant carries it in the payload, so random ids meant a re-synced CHANGED file orphaned every previous JSON and could leave a vector pointing at a parent file that no longer exists -- a search hit resolving to nothing, which a sourced answer cannot survive. Content-hashing was rejected for the same reason random was: changed text would change the id and orphan the file again, which is the exact failure being fixed. Deriving from position also removed chunking.py's only import of `db.repo`, so a pure text splitter no longer depends on the data layer. Residue that remains, deliberately: a document shrinking from ten parents to six leaves ids 6..9 behind -- but they are now DETERMINISTIC, so ST-17 can compute exactly which to delete instead of guessing | orchestrator |
| 2026-08-09 | chore/dev-setup-script | Developer setup is a script that verifies by running, not a checklist people follow by hand | keep the manual checklist in START-HERE / add a script that installs / add a script that installs AND proves it worked | the two machines had already drifted: gh went unauthenticated for days and stranded three branches, and the CR-02 dependency swap left stale environments. A checklist cannot detect drift; a script that ends by running lint and the full suite can. It judges the run by exit code rather than the last line of output, because the first version read the pytest progress dots and reported a green 173-test run as a failure | orchestrator |
| 2026-08-09 | chore/dev-setup-script | The setup script does NOT install codebase-memory-mcp | script the install / leave it manual with a written reason | the install source found earlier carried what looked like prompt-injection text and installation was refused at the time. Automating an untrusted download inside a setup script would push that risk onto both machines silently. The tool stays optional; .mcp.json already points at it so it works the moment a trusted copy exists | orchestrator |
| 2026-08-09 | fix/setup-script-path-detection | A tool check must distinguish "absent" from "present but not on PATH", and never advise installing until it has ruled out PATH | keep the simple `command -v` check / add a fallback search of known install locations | `command -v` searches PATH only, so a correctly installed tool reads as missing. On MB's machine that turned "your PATH needs one folder added" into "install gh again", which creates a second copy and leaves the original problem intact. Wrong diagnosis is worse than no diagnosis, because the reader acts on it | orchestrator |
| 2026-08-16 | ST-15 | The `passage: `/`query: ` prefix seam is enforced structurally, not by convention: one PRIVATE encoder plus three public functions (`embed_passages`, `embed_query`, and the model loader), with expected prefixes pinned as literals in tests rather than read from the same config the code reads | document the rule and trust callers to prepend it / one private seam with public prefixing functions, config-driven prefixes / same seam but prefix literals hardcoded in both code and tests | independent review found the first version's tests self-referential: they built the expected prefixed string from `config.embedding_passage_prefix`, so emptying that setting left every prefix assertion passing while multilingual-e5-base silently degraded (ADR-05's exact failure mode, with a green suite). Pinning literal `"passage: "`/`"query: "` in the tests, plus making `load_model` private so `.encode()` cannot bypass the seam, closes both the config-blind-spot and the bypass in one change; mutation-proven in both directions (emptying the config prefix now fails 6 tests, removing the code-side prefix fails 5/1) | orchestrator |
| 2026-08-17 | chore/harness-migration | The Claude Code control plane (agents, hooks, rules, skills, settings.json) lives at the user level (`~/.claude`), never inside this repo | keep `.claude/` in-repo, shared via git / move to `~/.claude`, one control plane across all projects | the harness installer zip unpacking into the repo root (not a staging folder) was a symptom, not the root cause: an in-repo `.claude/` mixes a personal, cross-project tool configuration into graded academic work's history, and it had already caused two incidents this project -- the ST-12 near-miss where a dead `python3` alias silently disabled all 15 guard.sh checks, and this migration's own payload burying Sanad's 770-node graph under 5,637 nodes of vendored harness content. User-level placement means the harness is configured once and versioned separately from any project it happens to be used in; the tradeoff accepted is that nothing in `.claude/` is enforced by CI or visible in a diff, so the project rules in CLAUDE.md and `.github/workflows/gate.yml` must stand on their own without assuming a hook is watching | user + orchestrator |
| 2026-08-22 | chore/harness-fit | Gate checks may declare a literal `cmd`; npm artifacts required only when a check is npm-shaped | leave the gate off and run it by hand / add a package.json shim wrapping uv scripts / teach checks() a runner-agnostic form | the shim was rejected outright: it puts a package.json into a repo whose ADR-10 says no JS toolchain, to satisfy a hook. Running by hand was the honest interim but leaves the human as the one who discovers red. Teaching checks() an explicit cmd is ~15 lines and makes the gate real for every non-Node stack, not just this one | orchestrator |
| 2026-08-22 | chore/harness-fit | `config-guard.mjs` now also inspects Write/Edit `file_path`, and `guardedPaths()` from harness.json is finally read | new dedicated guarded-paths hook / extend the existing config-guard / leave file_path unguarded and rely on the permissions deny-list | a second hook would have duplicated config-guard's whole Bash target-extraction block, which is the third copy the duplication rule says to abstract or justify. The deny-list alone was not enough: it is settings-level and was silently absent for docs/phase2 entirely. Extending the one guard keeps a single place where 'what is write-locked' is answered | orchestrator |
| 2026-08-22 | chore/harness-fit | The `.claude` rule is deliberately NOT applied to Write/Edit, only to Bash | apply it to both / apply to neither / Bash-only | applying it to both means a hook living inside .claude locks every tool out of repairing .claude -- the exact dead end that cost this session, and the reason the human had to delete settings.json by hand to grant access. Bash writes there stay blocked, a deliberate reviewable Edit does not. Recorded here rather than weakened silently; the permissions deny-list still asks on those paths | orchestrator |
| 2026-08-22 | ST-16 | The sparse BM25 encoder lives in `embeddings.py` behind `embed_sparse_passages` / `embed_sparse_query`, not in `vector_store.py` and not in a new module | put it in vector_store.py where Qdrant needs it / a new sparse.py beside embeddings.py / extend embeddings.py's existing seam | proven by running fastembed 0.8.0, not read from a doc page: `model.embed(text)` and `model.query_embed(text)` return DIFFERENT vectors for the same string (IDF-weighted values versus flat 1.0 term indicators), and calling the wrong one raises nothing, logs nothing and only shows up as slightly worse answers. That is the identical defect shape ADR-05's `passage: `/`query: ` rule exists to prevent, and the same shape belongs behind the same seam rather than a second one somebody has to remember. `vector_store.py` therefore never loads an encoder and never sees a string it has to decide how to encode: `upsert_children` takes children and computes their sparse side itself, and `search` takes the raw QUESTION rather than vectors, so no caller can hand either one a wrongly-encoded input. Cost accepted: `embeddings.py` now imports fastembed as well as sentence-transformers, and one dataclass (`SparseVector`) exists purely so this module never imports qdrant | orchestrator |
| 2026-08-22 | ST-16 | Child point ids are DERIVED -- `uuid5(namespace, "<parent_id> <index within that parent>")` -- and the index is the child's position within ITS PARENT, not within the document | random uuid4 per point / derive from the child text's hash / derive from parent id + position in the document / derive from parent id + position within the parent | qdrant-client 1.18.0 refuses a non-UUID point id outright (`ValueError: Point id X is not a valid UUID`, verified by running it), and children arrive from chunking.py with no id at all, so one has to be made. Random was rejected for the reason ST-14 rejected it for parents: every re-sync would insert a second copy of every unchanged chunk, so the collection grows without bound and retrieval returns the same passage twice. Content-hashing was rejected because changed text changes the id, which is the orphaning failure being fixed, and because two identical windows inside one parent would collide and silently lose a chunk. Position-within-the-DOCUMENT was rejected on batching: a resumed or partial sync that upserts one parent's children alone would compute different ids and duplicate everything it had already written. Position-within-the-parent is the only one of the four that is both stable across re-syncs and independent of how the caller batches | orchestrator |
| 2026-08-22 | ST-16 | A document's vectors and its parent JSON files are deleted as ONE unit by `vector_store.delete_document`, ordered VECTORS FIRST then parents, with the parent store (not the derived id sequence, not Qdrant) as the authority for which parents belong to a source file | let ST-17 call the two stores separately / delete parents first then vectors / delete vectors first then parents, parent ids derived by walking index 0 upward / delete vectors first then parents, parent ids read from the files on disk | BUILD-STATE has carried this hazard since ST-14: a document shrinking from ten parents to six leaves ids 6..9 behind, and if the two stores are cleaned up in two places, one of them eventually is not -- a surviving vector then resolves a search hit to a parent file that is gone, which is the one failure a sourced-answer product cannot absorb (PRD F-03). Two separate calls is exactly the shape that lets them drift. ORDER is a safety property and not a detail: both orders can fail halfway, but vectors-first leaves orphan parent FILES (wasted disk, invisible to search, and cleaned up by simply re-running the same call), while parents-first leaves live vectors citing deleted files, which is a broken citation served to a user. Deriving the id list by walking index 0 upward would be a guess dressed as arithmetic -- it has to stop somewhere, and one file missing from the middle of the run truncates the sweep and strands every parent after it. Reading `source_file` back out of the JSON costs one read per parent in the workspace and is the authority on what is actually on disk, which also makes the call self-healing after a crash | orchestrator |
| 2026-08-22 | ST-16 | `open_store` is a context manager that holds a process-level lock on the storage path and raises `StoreAlreadyOpenError` naming ADR-04 | let qdrant's own RuntimeError surface / a module-level singleton client / a context manager with a named error | a second QdrantClient on the same path raises `RuntimeError: Storage folder ... is already accessed by another instance of Qdrant client` (verified by running it). That message reads like a STALE LOCK and invites deleting the lock folder, which is the wrong fix and a destructive one; ADR-04 chose embedded mode knowing it is single-process, and the fix is always to pass the open client down. A singleton was rejected because it makes the client ambient, which is how a second one gets opened the first time two code paths run at once, and because tests would need a global reset between them. Every other function here therefore takes the client explicitly. Honest limit, recorded rather than implied: the lock is per-PROCESS and per-path, so it does nothing about a second Sanad process on the same data directory -- that is still ADR-04's assumption and nothing in this module enforces it | orchestrator |
| 2026-08-22 | ST-16 | ST-16's PR may be merged by its own author, deviating from CLAUDE.md rule 5 (every branch gets a review pass before merge) | wait for the other teammate's review / merge directly under a recorded deviation | authorised by YL in writing on 2026-08-22, for this PR. It deviates from the rule agreed 2026-08-10 after PRs #18 and #20 merged unreviewed, and the reason that rule exists is on the record: a green suite has never once been sufficient on this project -- ST-11 went through three review rounds and each found a real defect while the tests were green, and ST-15's reviewer found the self-referential prefix tests. Written down because a deviation nobody records becomes the new default. What was done INSTEAD of a second pair of eyes, and it is weaker: seven deliberate mutations run one at a time, a self-review of the diff after CI was green (which found the collection-created-before-encoding defect), and a run against realistic French text with the real models. Rule 5 stands for ST-17 unless YL says otherwise again | YL (authorised), orchestrator (recorded) |
| 2026-08-23 | ST-17 | `change_detection`'s one document-status set becomes TWO: `_STATUS_WITHOUT_DERIVED_DATA` (`removed`+`failed`+`skipped`, forces re-ingest) and `_STATUS_ALREADY_REPORTED_REMOVED` (`removed` only, suppresses a repeat Removed row) | leave it alone and let ST-17 re-query the row status itself / widen the single set / split into two sets | one set was answering two different questions and one answer was wrong: a `failed` file read as UNCHANGED forever, vanishing from the report after run one (breaks F-02 criterion 3) -- but widening the single set would have stopped a deleted `failed` file from ever being cleared from the registry. Re-querying in ST-17 would have put the classification rule in a second place, which is what change_detection exists to prevent. db/schema.sql is signed and has no column for the failure reason, so re-attempting is the only way the report stays truthful | YL |
| 2026-08-23 | ST-17 | Sync writes the derived stores PARENTS FIRST, THEN VECTORS -- the exact mirror of `vector_store.delete_document`'s vectors-then-parents order | vectors first (matches the read path's mental order) / parents first / write both then verify | both orders can be interrupted and only one fails safely. Parents first leaves parent files nothing points at: invisible to search, overwritten by the next sync. Vectors first leaves points citing a parent file that was never written, which is a broken citation served to a user -- the one failure a sourced-answer product cannot absorb. Pinned by a test that makes the SECOND write fail and asserts every point still resolves, so the two orders are distinguishable rather than merely commented | YL |
| 2026-08-23 | ST-17 | The double-sync guard is the unfinished `sync_run` row (`finished_at IS NULL`), claimed BEFORE the folder scan, always stamped finished in a `finally`, and protected by a process-level lock held only across the check-and-insert | in-memory flag only / a lock file / the DB row / DB row + process lock | the row is what openapi.yaml's `state: running` already renders, so the guard and the API read one fact. Claiming it before the scan covers the slowest part of the run, which is the window a double-click actually lands in. The process lock closes a real TOCTOU race that ST-51 creates next story by running Sync as a FastAPI background task. KNOWN LIMIT, recorded not papered over: a process KILLED mid-sync (not merely raising) leaves a row that blocks that workspace until someone clears it; a staleness timeout was deliberately NOT invented because "how long is too long" is ST-28's product decision and a wrong guess starts a second sync over live data | YL |
| 2026-08-23 | ST-17 | Workspace deletion across both derived stores lives in `sync.delete_workspace`, not in `workspaces.py`; derived stores go first, registry second | add a client parameter to workspaces.delete_workspace / open a client inside workspaces.py / put it in sync.py | ST-16 left this wiring undone for exactly this reason: opening a second QdrantClient inside workspaces.py breaks ADR-04's single-process rule, and sync is the module that owns the client. Derived-first ordering means an interrupted delete leaves a registry row whose derived data is gone -- indistinguishable from a never-synced workspace, repaired by one Sync. Registry-first would leave collections and parent directories that no registry row names, so nothing could ever find them again | YL |
| 2026-08-23 | ST-17 | `sync_workspace` calls `ensure_collection` once per run even when the run indexes nothing | rely on upsert_children's own ensure_collection / create it eagerly at workspace creation / create it once per sync run after the scan | `upsert_children` returns early for an empty child list and never reaches its own `ensure_collection`, so a run where every file was Skipped or Failed left NO collection and `search` reported "never been synced" immediately after a sync -- the precise confusion `CollectionNotFoundError`'s own docstring says it exists to prevent. Placed after `detect_changes` so a missing folder still ingests nothing (PRD section 11). Found by self-review after green; no mutation could have found it, because it is a missing line rather than a wrong one | YL |
| 2026-08-23 | ST-17 | The registry is written per FILE (one `repo.session` per file, document row and its sync_item row in one transaction), never once per run | one transaction for the whole run / per file / no transaction | F-02 criterion 3 ("every other file completes") has to hold at the registry level too: a failure on file 47 must not roll back the 46 that already succeeded. Document row and report row must still be atomic with each other, or a document marked `active` whose report row never landed becomes a state no later sync can detect. Cost is one connection plus an idempotent `ensure_schema` per file, roughly 2ms, against a G5 budget of 10 minutes -- measured properly by ST-18 | YL |
| 2026-08-23 | ST-17 | ST-17 was built and committed by MB (git identity `meriem-mb`) while BUILD-PLAN line 65 assigns it to YL | reassign in BUILD-PLAN / record the deviation and leave the plan as signed | same shape as the ST-12 deviation already recorded for 2026-07-28. CLAUDE.md's team note requires saying so rather than letting recorded owner and actual author drift silently. Flagged to the human; the plan row is left as it is until a human decides | MB (recorded), YL (planned) |
| 2026-08-23 | ST-17 | ST-17's PR (#35) was squash-merged to main WITHOUT the CLAUDE.md rule 5 review pass | wait for a review pass / merge on the human's instruction and carry the debt | the human instructed the merge explicitly on 2026-08-23 after being told the branch was unreviewed. Recorded rather than absorbed because rule 5 is not ceremony on this project: ST-10 shipped a rollback-defeating defect that only an independent pre-merge re-review caught, ST-11 needed three rounds and each found a real defect WHILE THE SUITE WAS GREEN, and ST-12 merged unreviewed and is still owed one. ST-17 is the largest diff in the project (+2,189) and touches a module it does not own. The review is now a debt on two stories, tracked in BUILD-STATE Next item 3, and the PR body names where to start | human (instructed), MB (merged) |
| 2026-08-23 | fix/S1-citation-label-markup | Heading markup (`**`, `_x_`, `` ` ``, HTML tags) is stripped before a heading becomes a `section_label`, and a markup-only heading becomes None rather than "" | leave it to the UI to sanitise / strip in chunking at the single place labels are born / render markdown on the source card | the label is citation text shown to a user (PRD F-03, UX spec 6.2), not markdown. A real Moroccan labour-law PDF produced `**G-** **<u>Securite sociale et charges sociales</u>**` as a heading, which is what would have appeared on the source card. Stripping at the UI would mean every future renderer repeats it; `_split_on_headings` is the one place a label is created. The underscore form is matched only at a WORD BOUNDARY so `_Formalites_` loses its markers and `max_length` survives -- the first version stripped every underscore and damaged real headings | MB |
| 2026-08-23 | ESCALATION (no owner yet) | Parents produced by SPLITTING one oversized section all inherit that section's label, so a citation can name the wrong part of a long document. NOT fixed -- escalated, because the fix deviates from signed architecture 7.5 | add "(part 2 of 9)" to split parents (in spec, honest, still not precise) / also split on a configurable citation pattern such as `^Article \d+` (precise, DEVIATES from 7.5 "parents split on markdown headings H1-H3") / accept as-is | measured on the real Moroccan labour code: 313,255 chars, 588 distinct "Article N" references, and only 22 markdown headings. Chunking produced 84 parents carrying just 10 distinct labels, so a passage from Article 235 is cited as "Titre II : Definitions" -- it does not merely under-inform, it points the reader at the wrong part of the document. chunking's own `_merged_label` docstring states the rule being broken ("Labelling a parent that spans Articles 1 to 3 as 'Article 1' would cite a sentence from Article 3 under the wrong heading -- a quietly wrong citation"); ST-14 applied it to the MERGE path and the SPLIT path violates it. F-03 is in the never-cut set (BUILD-PLAN ladder step 6), and CLAUDE.md rule 1 forbids self-deciding a spec deviation | ESCALATED to a human |
| 2026-08-23 | fix/S1-citation-marker-labels | SUPERSEDES the ESCALATION row above. A parent and a child are labelled by the CITABLE MARKERS inside their own text (config `parent_citation_marker_pattern`, default `Article\s+\d+`, case-sensitive), falling back to the enclosing heading when there is none. No change request, and no spec deviation | (a) "(part 2 of 9)" suffix / (b) also split on `^Article \d+`, needing a change request against signed 7.5 / (a2) leave splitting alone and derive the LABEL from markers | the earlier row claimed the good fix required deviating from architecture 7.5. RE-READ OF THE SPEC SHOWS THAT WAS WRONG, and it is the reason this supersedes rather than amends: 7.5 fixes where parents are SPLIT and names `section_label` as a field, but never defines how the label is COMPUTED. So the precise fix was available all along with zero deviation, and the escalation should not have been raised. Case-sensitivity is the load-bearing detail, measured not assumed: in the real labour code capital "Article 42" starts an article (588, every one distinct) while lowercase "l'article 26" is a cross-reference (234, only 163 distinct); matching case-insensitively would label parents by the articles they merely mention. Result on the real corpus: distinct parent labels 10 -> 82, child labels 584 distinct across 800 children, 0 mislabelled, 0 children still cited by a heading. Unchanged for prose (the CNSS guide and the technical manuals keep heading labels) | MB |
| 2026-08-24 | docs/share-testing-skills | Three tracked skills overlap on "break the check on purpose" (prove-it, test-strategy, research-discipline) and are kept separate rather than merged | merge into one testing skill / keep all three and accept the overlap / strip the overlapping sections out of the two imported ones | a skill is loaded by its own trigger, so the entry point IS the design: test-strategy fires when a test is being written, research-discipline when a check passed but may not have checked, prove-it when a root cause is being hunted. One merged skill either fires constantly or fails to fire for the case in hand, and stripping the overlap would leave each one incomplete at the moment it is actually read. This is the THIRD copy of the rule, which is exactly where the duplication law requires a written reason instead of an abstraction | YL |
| 2026-08-24 | docs/share-rules-and-agents | The core law moves into CLAUDE.md rather than `.claude/rules/`, and only 2 of 9 user-level rule files are adopted at all | copy all 9 rule files across / adopt a subset into .claude/rules/ / fold the load-bearing subset into CLAUDE.md | `.claude/rules/**` is a deny rule for Edit, CONFIRMED by attempting the write and being refused, so an agent cannot place a file there and only a human can. CLAUDE.md is writable AND auto-loads, so folding the law in reaches MB on her next pull with nothing to install. Four of the nine were rejected on content, not convenience: `git-discipline.md` mandates `task/T-xxx` branches and an `[AI]` commit marker, both of which CLAUDE.md rules 2 and 4 FORBID here, so importing it would have shipped a contradiction (it also carries a UTF-8 BOM, the exact trap tooling-discipline warns about); `stack-defaults.md` is pnpm/Drizzle/Prisma guidance for a Python/uv/SQLite repo; `00-global.md` asserts a jscpd duplication gate in CI that gate.yml does NOT have, verified by reading the workflow; `engineering-seniority.md` would be a fourth copy of what prove-it and research-discipline already carry. context7.md and prompts-registry.md were skipped as MCP-dependent with unknown availability on the other machine | YL |
| 2026-08-25 | ST-21 | The agent is a PACKAGE, `agent/`, not flat root modules like the rest of the app | flat `agent_graph.py` / `agent_trace.py` at the root, matching how ingestion and indexing were flattened / an `agent/` package matching signed architecture 11 | architecture 11 names `agent/graph.py`, `nodes.py`, `prompts.py`, `trace.py` and `db/` already follows that shape; docs/phase2/CLAUDE.md and config.py line 51 both already refer to `agent/nodes.py` BY NAME. And a flat `trace.py` at the repo root would SHADOW the Python standard library's `trace` module for every import in the process -- confirmed by `importlib.util.find_spec('trace')` resolving to the stdlib file before this branch existed | YL |
| 2026-08-25 | ST-21 | The agent ports have NO default implementations; the stub the exit gate names lives in the test file only | ship runnable stub ports as defaults so the graph is usable immediately / require every caller to pass real ports | a stub that answers plausibly is the most dangerous object in a sourced-answer product: as a default it would ship the day someone forgot to pass real ports, and Sanad would invent an answer instead of failing. Seven required fields make that impossible to reach by accident -- only by writing the fake out loud | YL |
| 2026-08-25 | ST-21 | No hand-computed LangGraph recursion limit; the framework default is the backstop and the retry ceiling is the real bound | derive a limit from `retry_ceiling` (what the first draft did) / pass nothing and let the framework default apply | MEASURED rather than trusted: langgraph 1.2.9 defaults to `DEFAULT_RECURSION_LIMIT = 10007` (`langgraph/_internal/_config.py:32`) and a ceiling of 50 -- 155 nodes -- ran untouched. The documented 25 is stale. A derived limit would therefore have been a second, LOWER bound whose only possible effect is to cut a legitimate run short the day a node is added to 5.2, which is the exact failure it was invented to prevent. `test_a_high_retry_ceiling_runs_to_completion` needs 65 nodes, so a future langgraph lowering its default turns that test red instead of surprising a user | YL |
| 2026-08-25 | ST-21 | The trace IS the retry counter: `retries` and `searched` are derived by counting recorded steps, never stored as their own fields | an integer in the graph state incremented by the reword node / derive both from the trace steps | two numbers describing the same thing drift, and both of these have a consumer that shows the drift to a user: F-04's ceiling is enforced against the count, and UX spec 6.2 puts a retry marker on the answer bubble. Deriving makes 'the loop ran three times but the marker says two' unrepresentable. `rewords_in` / `searches_in` are single free functions so the in-flight nodes and the finished `Trace` cannot answer differently | YL |
| 2026-08-25 | ST-21 | No parent-fetch node, despite architecture 5.2 drawing one (box P) | add a pass-through node now so the graph matches the diagram box for box / leave the parent fetch inside whichever port needs the text (ST-23's retrieve or ST-24's write_answer) | ADR-03's own node list -- summary, rewrite-and-split, clarification pause, retrieval, grading, reword retry, answer, refusal -- does not include it, and a parent fetch changes no ROUTE: it is the answer step reading the full section behind a hit it already holds. An empty node added now would be a box in a picture, not a seam. Recorded so ST-23/ST-24 do not read its absence as an oversight | YL |
| 2026-08-25 | ESCALATION (no owner yet) | openapi's answer invariant is AMBIGUOUS about the clarification kind, and ST-21 took the narrower reading rather than editing the signed contract | read it as 'refusal false implies sources non-empty' (openapi line 223) and force clarifications to carry sources or set refusal=true / read it as 'sources non-empty whenever kind is answer' (openapi line 512) and let a clarification carry none | the two sentences agree for the answer/refusal pair the first was written about and disagree about the third kind: a clarification is not a refusal, so `refusal` is false, yet it has retrieved nothing and can cite nothing. `Answer.__post_init__` enforces the line-512 reading because it is the one G3 measures. NOT a code fix either way -- if line 223 is meant literally the contract needs a change request (CLAUDE.md rule 1), which is a human's call | unassigned |
| 2026-08-25 | ESCALATION (no owner yet) | ADR-09 says V1 PERSISTS the trace beside the answer, and there is nowhere to persist it: db/schema.sql has no answer or trace table | add an `answer_trace` table now / carry the trace in process only and escalate | openapi calls `trace_id` a 'stored trace reference', so the contract assumes a store that does not exist. db/schema.sql is signed (architecture 7.3/7.4), so adding a table is a change request and not a code edit. ST-21's own exit gate is only that every answer object CARRIES its trace, which it does, in memory. Nothing is lost today because nothing reads a trace back yet -- F-10 is V1.1 -- but the gap has to be closed before any story claims a trace survives a restart | unassigned |
| 2026-08-25 | ST-21 | SUPERSEDES the 'no parent-fetch node' row above, which was WRONG. Architecture 5.2 box P gets its own node, its own port (`fetch_parents`) and its own state field, with a real implementation in `agent/stores.py` | keep it out and let ST-23's retrieve or ST-24's write_answer do it (the superseded row) / give it a node and a port of its own | the superseded row was not a matter of taste, it was wrong on a fact: `parent_store.get_parent` requires a workspace id and `write_answer`'s signature has none, so NO port could have filled the gap without a signature change -- which is a graph reshape, i.e. exactly what a skeleton exists to prevent later stories from having to do. Meanwhile the graph answered from 500-character child chunks while its docstrings claimed it read sections (7.5: search the small thing, read the big thing). Found by the human's review, not by the suite | YL |
| 2026-08-25 | ST-21 | `rewrite` and `reword` return a SEQUENCE of queries, not one string; `retrieve` runs once per query and hits are merged on (parent_id, chunk_text) | one query string in V1 and widen later when ST-22 needs it / a sequence from the start | 5.2's box is 'Rewrite and split query', ADR-03 keeps the reference implementation's sub-queries, and BUILD-PLAN line 76 names ST-22 'Rewrite-and-split node'. The single string made the split UNREPRESENTABLE, so ST-22 would have had to reshape the graph to do the job its own title names -- and the code comment justifying it ('V1 returns one query') cited no signed source, because there is none. One query stays a one-element tuple, so the ordinary case is unchanged. Merging on (parent_id, chunk_text) rather than on the hit object is deliberate: the same chunk found by two sub-queries carries two different fusion scores, so object equality would keep both and the model would read the same passage twice | YL |
| 2026-08-25 | ST-21 | A parent section the store cannot find is OMITTED from the answer's context and counted in the trace; a CORRUPT one raises and stops the answer | fail the whole answer on any unreadable parent / skip every unreadable parent silently / split the two cases | they are different failures. Missing is a store that has drifted from the index by one file -- `parent_store.get_parent`'s own message says 'the workspace may need a re-sync' -- and refusing an otherwise good answer over one absent section serves the user worse than answering from the four that are there, PROVIDED the shortfall is visible, which the trace step makes it ('loaded 1 of 2'). Corrupt is a file that exists and does not say what it should, which risks citing a section that is not the section it claims to be, and F-03 makes the citation the product's contract with the user | YL |
| 2026-08-26 | ST-21 | A run that loads ZERO parent sections refuses instead of answering, with its own wording naming Sync as the next step; a run that loads SOME still answers | answer from whatever context came back / refuse on any missing section / refuse only when none loaded | found by a cold verifier pass asking what 'loaded 0 of 5' does. It answered: the model got an empty context and the answer still carried five source citations built from chunk metadata, which is a citation to documents whose text nothing read -- and F-03 makes the source line the product's contract with the user. Refusing on ANY missing section would be the opposite error, since four of five is a good answer with a visible shortfall. The wording is separate from the ordinary refusal because a different thing happened: 'not covered here' would be false when the passages were found and unreadable, and PRD 11 requires a real next step, which here is a Sync | YL |
| 2026-08-26 | ST-21 | `ask` validates the question against openapi's 1..2000 bounds itself, using `config.question_min_length` / `question_max_length` | leave it to the API layer's request schema (ST-51) / check it in the service function | ADR-13 has the UI calling these service functions IN-PROCESS, so the route's validation is not in that path and the contract would have held for HTTP callers only. It also failed at the wrong seam: a blank question reached the rewrite port and raised 'the rewrite port returned a blank string', sending a reader to ST-22's code for a fault the caller committed. Mirrors the workspaces.py precedent, where openapi's name limits already live in config rather than in a route | YL |
| 2026-08-26 | ST-21 | The query split is capped by `config.max_sub_queries` (default 5) and a wider split is refused at the seam | no cap, trusting the rewrite port / truncate silently to the cap / refuse | the F-04 retry ceiling bounds how many ROUNDS run and nothing bounded how WIDE a round is: a rewrite returning forty phrases costs (ceiling + 1) x 40 real model and store calls for one question, every one of them also read back to the user in the F-05 disclosure. Truncating silently was rejected for the same reason the blank-port guard exists -- a search the user is told about but that never ran, or one that ran and vanished, are both dishonest. ST-22 owns splitting and may tune the number; it is a guard rail, not a target | YL |
| 2026-08-26 | ST-21 | The `dict.fromkeys` / `setdefault` de-duplication idiom is repeated seven times inside `agent/` rather than abstracted | one shared `first_seen()` helper / accept the repetition and record why | the core law says the third copy needs an abstraction or a written reason. Each copy is one line and they de-duplicate DIFFERENT things on different keys -- parent ids, file names, (parent_id, chunk_text) pairs, Source objects, trace files -- so a shared helper would take a key function and be longer at every call site than the line it replaced, while hiding which key each caller actually de-duplicates on. That key is the interesting part in at least two of them: `_merge_hits` is a bug fix expressed as a key choice. Recorded rather than abstracted, and this row is the third-copy rule being met, not ignored | YL |
| 2026-08-26 | ST-23 | ST-23 BUILDS THE ADR-06 CHAT-MODEL SEAM, although no BUILD-PLAN row owns one | wait and give the model interface its own story / build it inside ST-23, the first story that needs a model / put it in ST-24 | ADR-06 requires a provider-agnostic interface with a cloud and a strict-local mode, and NOTHING in the repo had one -- verified three ways before writing a line: graph search, a project-wide grep across every *.py for `langchain|chat_model|ChatGoogle|ChatOllama|init_chat_model` (which found only config.py lines 32 and 34, the model NAMES), and reading every row of BUILD-PLAN. ST-23 is the first story whose exit gate needs a model to exist, so it lands here rather than blocking behind a story nobody has written. The gap is in the plan, not the code, and is recorded so ST-24 does not rediscover it | YL |
| 2026-08-26 | ST-23 | The chat seam is Sanad's own one-method `complete(system, user) -> str`, NOT langchain's `BaseChatModel`, and the grader returns one parsed word rather than structured output | pass BaseChatModel through and use with_structured_output for the verdict / a narrow Sanad-owned seam with a parsed one-word reply | structured output is more reliable against a real cloud model and is UNTESTABLE under this project's own rules: docs/phase2/CLAUDE.md requires tests to use a scripted fake chat model with no API keys anywhere, and langchain's fakes cannot serve structured output -- `bind_tools` raises NotImplementedError for any model that does not override it (`langchain_core/language_models/chat_models.py:2510`), read from the installed package rather than remembered. A seam the mandated test double cannot satisfy is a seam that gets tested by pretending. One word is also the smallest thing ADR-06's 7B local floor can be relied on to produce. Cost accepted in writing: we own the parser | YL |
| 2026-08-26 | ST-23 | An unparseable grader reply RAISES; it is never counted as off-topic | treat anything that is not RELEVANT as off-topic (simpler, never crashes) / raise a named error | the two outcomes are one keystroke apart in code and a world apart for the user. An off-topic verdict is a CLAIM ABOUT THE CORPUS -- F-05 tells the reader their documents do not cover the question, with the product's full confidence -- and a model that answered gibberish has made no such claim. Worse, the retry ceiling multiplies it: three unparseable replies produce a refusal listing three searches, as if the corpus had been examined three times. The parse is therefore generous about FORM (case, punctuation, quotes, markdown, a 'Verdict:' label, hyphen or underscore) and strict about MEANING | YL |
| 2026-08-26 | ST-23 | Each ADR-06 mode names its provider class directly; `init_chat_model` is deliberately not used | init_chat_model with the model name from config (one line, most idiomatic) / a small dict of mode -> builder naming each class | READ FROM THE INSTALLED PACKAGE, not remembered: `init_chat_model("gemini-2.0-flash")` with no provider argument infers `google_vertexai` (`langchain/chat_models/base.py:563`) -- a different product, a different auth story, and a package this project does not depend on. The one-line provider swap ADR-06 admires is real but only with the provider named explicitly, and a dict of builders keeps that explicit while still making a third provider one line plus a config value. A test asserts the concrete class, so a future 'simplification' to init_chat_model turns red instead of quietly pointing at Vertex | YL |
| 2026-08-26 | ST-23 | The retrieve port passes NO search depth; `vector_store.search` resolves `config.retrieval_depth_k` itself | pass retrieval_depth_k from the port (explicit at the call site) / pass nothing and let the store resolve it | ST-16 already made `search` own that default deliberately, and its docstring says why: 'F-04 makes the depth operator-tunable; never hardcode it at a call site.' Reading the setting in the port as well would be a SECOND place reading it -- the shape that drifts, where one caller is updated and another is not and two searches in the same product use different depths. The unit test asserts the ABSENCE of a limit and the integration test proves the operator's setting still governs, by running at depth 1 and depth 3 against a real Qdrant and asserting the hit counts differ | YL |
| 2026-08-26 | ST-23 | The fake dense/sparse encoders move to `tests/fake_encoders.py`; the three existing copies are NOT migrated in this story | paste a fourth copy into the new test / extract a shared module and migrate all four now / extract and migrate the new caller only | the core law says the third copy earns an abstraction or a written reason, and there were already three (test_vector_store, test_embeddings, test_sync). Extracting is the abstraction. Rewriting the three existing files to import it would put unrelated churn into a diff a reviewer is grading against one exit gate -- the scoped boy-scout rule -- so the migration is its own `chore/` branch, named in BUILD-STATE so it is a task rather than a smell nobody wrote down | YL |
| 2026-08-27 | fix/dead-cloud-model-name | `chat_model_cloud` moves from `gemini-2.0-flash` to `gemini-3.6-flash`, pinned to a specific stable version rather than the `gemini-flash-latest` alias | keep 2.0-flash (it was the signed value) / follow Google's named successor 3.6-flash / use the moving `gemini-flash-latest` alias / pick the newest, 3.7-flash | keeping it was not an option: the value is DEAD. The first live call this project ever made returned `404 NOT_FOUND: This model models/gemini-2.0-flash is no longer available`. It was correct when ST-02 pinned it on 2026-07-20 and retired by 2026-08-27. 3.6-flash is the successor Google's own error message names, which beats picking by taste, and it was then verified by a live call rather than trusted; 2.5-flash and 3.5-flash were also confirmed working as fallbacks. The moving alias was rejected because the evaluation gate (G1-G3) compares scores across releases, and a model that changes underneath a threshold makes those numbers meaningless. Which model FINALLY ships is left to ST-32/ST-36 with real numbers | YL |
| 2026-08-27 | fix/dead-cloud-model-name | The strict one-word grader parse STAYS strict; the lenient alternative is rejected on evidence rather than deferred | accept a leading verdict followed by prose (more forgiving) / keep the strict parse | ST-23 recorded this as UNVERIFIED because nothing on the machine could measure it. A key arrived and it was measured: four live calls across gemini-2.5-flash and gemini-3.5-flash, each grading one on-topic and one off-topic case, returned EXACTLY the one word asked for, with all four verdicts correct -- including correctly rejecting a pump-maintenance manual as off-topic for a labour-law question. Zero parse failures. So leniency would buy tolerance nobody needs while making a confused reply harder to notice. Narrowed rather than closed: four calls, two models, one prompt, short passages -- nothing is known about 4,000-character sections, load, or the Ollama path | YL |
| 2026-08-27 | UI design review | `designrag-main/` is a VISUAL REFERENCE ONLY and is gitignored; the screens are reimplemented as Jinja templates, not ported | port the React components / rebuild in Jinja using it as a picture / commit it so MB gets it through git | CR-02 replaced the framework interface with server-rendered Jinja templates and ADR-10 rules out a JavaScript toolchain, so porting was never available -- the only question was whether to track it. Ignored because it carries its own `.env.example` and a `server.ts` reading a GEMINI_API_KEY, and because a second package.json and bun lockfile in a uv-only Python repo would put CI, gitleaks and the code graph to work on a toolchain this project deliberately does not have. STATED COST: MB does not receive it through git | YL |
| 2026-08-28 | ST-24 | `write_answer` signals "these sections do not answer the question" by RAISING `AnswerNotCoveredError`, declared in `agent/ports.py` as part of the port contract | return prose and let the graph read it / widen the return to `str \| None` like `Clarify` / a sentinel string the caller compares / raise a named exception | prose would produce an `Answer` of kind `answer` with `refusal` false and source cards attached, whose text refuses -- a refusal wearing an answer's clothes, which F-08's out-of-scope half scores as a non-refusal. `str \| None` is the trap: a port implementation that forgets a `return` yields None, so a plain coding mistake becomes a fluent honest-LOOKING refusal listing the searches it ran -- the same failure `agent/grading.py` refuses for an unparseable verdict. Raising cannot happen by omission. It lives in ports.py, not answering.py, so `agent/nodes.py` need not import ST-24's module to know what its own seam can do | YL |
| 2026-08-28 | ST-24 | The answer node builds ONE tuple, `cited`, and uses it BOTH as what the writer is shown AND as what the sources are built from; a passage whose section box P could not load is neither shown nor cited | keep citing every passage and show the writer only the loaded ones (the previous behaviour) / show the writer the chunk text as a fallback for a missing section / one tuple for both | the previous behaviour was right whenever box P loaded everything and wrong the moment it did not: "loaded 4 of 5" answered from four sections and printed five source cards, so one card pointed at a document whose text nothing had read -- the same defect the floor at zero already refuses, surviving in the partial case (F-03 calls the source line the product's contract with the user). The chunk fallback would have kept every card but hands the model a 500-character window while the reader is told a section was consulted. Filtering in two places was rejected outright: two filters agreeing is a convention, one tuple is a fact | YL |
| 2026-08-28 | ST-24 | The `NOT_COVERED` decline is read POSITIONALLY -- the token must open the FIRST non-empty line and be followed by a stop -- not by searching the reply for the word | substring search anywhere in the reply / the whole cleaned reply must equal the token / the token must open the first line and be followed by end-of-line or a separator | a substring search misreads an answer that quotes the token, and the ordinary reply here is free prose rather than the grader's two known words. Requiring exact equality would misread "NOT_COVERED - rien ici" as an answer, which is the worse direction. The first-line rule is what lets the prompt's own partial-answer instruction work: a model told to "state plainly which part they do not cover" naturally writes a later line opening "Not covered: ...", and a parser scanning every line would throw that whole answer away. "Not covered by Article 13, but ..." resolves to ANSWER because the token is followed by a word rather than a stop | YL |
| 2026-08-28 | ST-24 | `agent/grading.py::_passage_block` and `agent/answering.py::_section_block` stay TWO formatters; the shared three-line "file -- section label" computation is a second copy, not an abstraction | extract a shared `provenance_label(hit)` helper into a common module / leave two copies | the core law allows two copies and this is the second. The reason it should STAY two is not laziness: the registry versions each prompt separately and a released version is never edited in place, so a shared formatter would mean tuning the answer prompt's layout silently changes what the grader is shown -- one prompt's change leaking into another prompt's measured behaviour. Extracting it would also mean editing ST-23's module inside ST-24's diff. A third copy earns the abstraction | YL |
| 2026-08-28 | ST-24 | The scripted fake chat model moves to `tests/fake_chat.py`; ST-23's two existing copies are NOT migrated in this story | paste the fourth and fifth copies into ST-24's two new test files / extract a shared module and migrate all copies now / extract and use it from the new callers only | exactly the shape ST-23 settled for the fake encoders on 2026-08-26, and settled the same way. ST-24 needed the double in two new files, which would have made five copies of a nine-line class; extracting stops the growth. Migrating ST-23's own test files would be rewriting another story's tests inside this story's diff, which the scoped boy-scout rule keeps out. Named as a `chore/` follow-up in BUILD-STATE rather than left implicit | YL |
| 2026-08-28 | ST-24 | The "sections were read and none of them answers the question" refusal reuses `REFUSAL_TEXT` word for word; only the TRACE detail distinguishes it | write a third user-facing message for this case / reuse the "not covered here" wording and separate them in the trace | the user's next step is identical in both cases -- rephrase, add the document, or switch workspace -- so a third message would ask the reader to act on a distinction they cannot act on. The mechanism still has to be recoverable, and the trace is where a mechanism belongs (ADR-09, F-10). Contrast the UNREADABLE-sections refusal, which DOES get its own words, because there the next step genuinely differs: run a Sync | YL |
| 2026-08-28 | ST-24 | The decline is read under TWO rules, split by spelling: `NOT_COVERED` / `NOT-COVERED` are TOKENS and may carry a trailing note; "not covered" with a SPACE is prose and must be the whole first line | one positional rule for all spellings (the first design) / require exact equality for all spellings / split token from prose | the single rule threw away CORRECT ANSWERS, found by a cold review and reproduced before fixing. This branch's own prompt tells the model to "answer that part and state plainly which part they do not cover", so a model obeying it in English and naming the gap first writes "Not covered: overtime rates. Article 13 sets the trial period at three months." -- read as a refusal and discarded, leaving the user told their documents lack something they have, with the model's answer kept nowhere. Exact equality was rejected in the other direction: it would ship `NOT_COVERED (the sections do not mention it)` as answer text with source cards under it. The underscore and the hyphen are what carry the split -- no sentence produces either by accident | YL |
| 2026-08-28 | ST-24 | A parent section that loads with BLANK text is omitted from `parent_texts` exactly like a missing one, rather than counted as read | count it as loaded (the behaviour before) / raise on it / omit it like a missing section | `parent_store` only checks the `text` field is PRESENT, so a blank one came back as a successful read and every count downstream believed it: the trace said "loaded 2 of 2", the router saw a full mapping, the model got a headed block with nothing under it, and the document was still printed as a source card -- a citation to text nothing read. Raising was rejected because one truncated file would take down a whole question that four good sections could answer. Omitting puts it back on machinery that already exists: the shortfall shows in the trace, the citation drops with it, and zero readable sections still routes to the Sync refusal. Fixed in `agent/stores.py`, which is where "loaded" is defined, with a backstop in the writer | YL |
| 2026-08-28 | ST-24 | An EMPTY model completion raises `EmptyAnswerError`, declared in `agent/answering.py`, and is never turned into a decline | let `_spoken` raise "the write_answer port returned a blank string" / treat it as a decline and refuse / a distinct named error | the same rule `agent/grading.py` applies to an unparseable verdict: a refusal is a claim about the USER'S DOCUMENTS and an empty completion is a fact about the MODEL, so letting one impersonate the other emits an honest-looking refusal listing searches when nothing judged anything. `_spoken`'s message was accurate about the shape and wrong about the culprit, sending whoever read it to ST-24's module for a provider's safety filter. It is NOT in ports.py, unlike `AnswerNotCoveredError`, because no node routes on it -- the graph lets it through and the API layer owns the retry action (PRD section 11) | YL |
| 2026-08-28 | ST-24 | The decline parser splits on SPELLING, not on position: `not_covered` / `not-covered` (underscore or hyphen, any case) are VERDICTS and may carry a trailing note; "not covered" with a SPACE must be the ENTIRE reply. `NOT COVERED - rien ici` is therefore read as an ANSWER, and that boundary is deliberate | one positional rule for every spelling / the whole first LINE for the prose spelling / the whole REPLY for the prose spelling, trailing notes allowed only for the token spellings | this is the THIRD version of this rule and the first two were both wrong in the direction that DISCARDS A CORRECT ANSWER. A cold review found `Not covered: overtime rates. Article 13 sets the trial period at three months.` being thrown away; the rule-5 review then found the fix still failing on the same sentence split over two lines. The underscore and hyphen carry the split because no French or English sentence produces either by accident, so text after one annotates a verdict. The accepted cost, pinned by its own test: a spaced decline WITH a note reads as an answer, because it cannot be told apart from a correct one-line answer that opens by naming its gap -- and losing the answer is the worse error, since the user is then told their documents lack something they have and the text is kept nowhere | YL |
| 2026-08-28 | fix/ST-24-shouted-decline | SUPERSEDES the ST-24 row above: CASE is a third discriminator in the decline parser. A SHOUTED `NOT COVERED` carries a trailing note and is a verdict; a sentence-case `Not covered` is prose. Both spaced forms must still be a SINGLE-LINE reply | leave the boundary as recorded / use case as the discriminator / drop the spaced spelling entirely | THE ROW ABOVE GAVE A REASON THAT WAS FALSE, and that is why this row exists rather than a quiet code change. It said `NOT COVERED - rien ici` "cannot be told apart" from `Not covered: overtime rates. Article 13 sets the trial period at three months.` They differ in CASE, and a model does not write an answer in capitals. A wrong reason in a decision record is worse than a missing one: the next reader inherits a constraint that was never real, and would not think to look again. Caught by the rule-5 re-review, which approved the merge and said the trade was right for the wrong reason. The shouted form is still held to a single line, so a caps gap-heading above a real answer stays an answer -- without that, this fix would re-open the very defect two reviews closed. STILL ACCEPTED, now stated as a choice rather than a limit: `Not-covered by Article 13, but ...`, the HYPHEN spelling in front of prose, reads as a decline; closing it would cost the three real misses that dropping the token stop list fixed, and prose uses a space | YL |

| 2026-08-30 | ST-12 review (PR #65) | A file that falls OUTSIDE `supported_document_extensions` after the operator narrows the list has its DERIVED STORES DELETED (vectors + parent files) and is reported `skipped`, deletion first | (a) report `skipped` and leave the data, the pre-#65 behaviour / (b) report `removed` and delete, rejected by PR #40 because the file is still present on disk / (c) report `skipped` AND delete the derived stores | (a) let the product keep CITING a file the report calls Skipped -- the report lied about the product, which is the defect #65 exists to close. (c) mirrors `_remove` deliberately: deletion BEFORE the row, so a crash mid-way leaves a file that gets re-scanned rather than a report row promising a deletion that never happened. The cost is real and is accepted: re-widening the extension list means re-ingesting those files from scratch | MB |
| 2026-08-30 | ST-27 | The loading stage hint is read AT THE PORT SEAM on a worker thread, and the page reports whatever stage the run is in. No timer anywhere | a plain synchronous form POST with no stage hints / vendor htmx as pyproject's CR-02 comment anticipates / a background run plus a poll, hand-written script, nothing vendored | UX spec 6.3 bans a bare spinner and principle 3 bans faked progress: 'stage hints say what is actually happening'. A synchronous POST ships neither. htmx does NOT solve it -- its own indicator is a bare spinner, so the background run is needed underneath it either way, and vendoring would add a third-party file to review for no part of the hard problem. The chosen shape also pays for two other things: the passage viewer gets the sections the answer was written from, and Cancel becomes real (it is checked at the next seam, which is exactly 6.3's 'stops after the current stage'). PROVEN DISCRIMINATING rather than argued: the same question held at three ports must report three labels, and fixing two of the three stages killed exactly those two parametrised cases | YL + human |
| 2026-08-30 | ST-27 | The screen works with JavaScript off, and script only UPGRADES it. No control is rendered that does nothing without script | require script (simplest) / progressive enhancement / a second no-script render path | ADR-10's ban is on a TOOLCHAIN, not on a script tag, so either was available. Enhancement won because every upgrade here has a working primitive underneath: the composer is a real form POST, the stage hints fall back to a <noscript> meta refresh showing the SAME server-rendered stage, a sample question asks itself instead of populating the input, and a source card is a real <a> to a real passage page. The theme toggle is the one control script alone provides, so it ships hidden and script reveals it -- a button that does nothing is worse than no button. A second render path was rejected outright: two descriptions of one passage is the shape where the overlay quietly shows a stale section | YL |
| 2026-08-30 | ST-27 | The passage viewer is a native <dialog> opened with showModal(), not a hand-built overlay | hand-rolled focus trap / a component library's primitive / the platform's <dialog> | acceptance criterion 4 needs three behaviours -- focus moves in, focus is trapped, focus returns to the invoking card -- and hand-rolled focus management is where accessibility goes to die. There IS no component library here (ADR-10), so the usual advice 'reach for an accessible primitive' points at the platform: <dialog> traps Tab, closes on Escape and restores focus to the opener, in the browser, with no code. Where showModal is unavailable the card link simply navigates to the passage page, which is the same content | YL |
| 2026-08-30 | ST-27 | Forms are decoded with urllib.parse.parse_qsl. `python-multipart` is NOT added | add python-multipart (what FastAPI's error message tells you to do) / decode the urlencoded body with the standard library | the package parses multipart/form-data, the FILE-UPLOAD encoding, and UX spec 13 rules out file upload outright: 'No drag-and-drop file upload, because workspaces point at folders on disk'. It would be a dependency added for a format the product refuses to accept. This is not a hand-rolled parser -- parse_qsl is the standard library's own decoder for application/x-www-form-urlencoded. The one judgement is the charset, and it is UTF-8 because base.html declares it. COST, stated: if a future story ever needs a real file upload, this decision is reversed rather than extended | YL |
| 2026-08-30 | ST-27 | S1's empty-state sample questions are built from the workspace's own DOCUMENT FILE NAMES | hardcode three French labour-law questions (what the React reference ships) / generate them with a model call / read them from ST-19's golden set / build them from the file names | UX spec 6.3 says 'drawn from the active workspace', and a file name is the only thing about a workspace that is both free to read and certainly true. Hardcoded questions are wrong the moment the active workspace is the technical-manuals one. A model call on an empty screen spends a provider call before the user has asked anything. Reading section labels from the parent store was rejected on its own docstring: list_parent_ids costs one read per parent in the WORKSPACE and is scoped there as 'a deletion-path function and not something an answer ever calls'. PARKED WITH AN OWNER: the good questions are ST-19's golden set -- reviewed, French, written against this corpus -- and when it exists this should read three from it and keep the file names as the fallback | YL |
| 2026-08-30 | ST-27 | `app.py` creates the SQLite registry itself at startup (`repo.ensure_schema`) | leave it to whoever runs a sync first / create it in the app's lifespan | found by RUNNING it, not by reading it: on a machine with no data/sanad.db, GET / raised RegistryNotFoundError and served a stack trace instead of acceptance criterion 1's 'No workspace yet' screen -- the first thing a new operator ever sees. Leaving it to sync means the app cannot render its own empty state until something else has run, which inverts the order a new user actually moves in (open the app, then create a workspace). ensure_schema is idempotent so an existing database is untouched. Pinned by a test whose fixture deliberately does NOT create the database first | YL |
| 2026-08-30 | ST-19 | The golden-set row carries THREE fields beyond the six signed in architecture section 14: `workspace`, `corpus_probe` and `notes` | keep exactly the six signed fields / add the three / put the extra data in a second parallel file | the six are a MINIMUM ("each with id, question, reference answer, source file, source article, kind"), not a closed list, and each addition pays for itself. `workspace` is needed by ST-32 to know where to ask -- without it the runner guesses, and batch 2 may add a manuals question. `corpus_probe` is what makes the file checkable at all (see the row below). `notes` carries the trap each row sets, which is the difference between a question and a test. A parallel file was rejected outright: two files describing the same 23 rows are two files free to disagree, which is the drift shape this project has already paid for twice. The schema test lists the signed six and the local three SEPARATELY so that widening the schema is a deliberate edit rather than a silent one | MB |
| 2026-08-30 | ST-19 | The golden set's claims about the corpus are checked by `scripts/golden_grounding.py`, run BY HAND, not by a pytest; and that script refuses to report any absence until a CONTROL PROBE proves its matcher can find something | a pytest that reads the corpus / a pytest that skips when the corpus is missing / a hand-run script quoted in the journal | `data/` is git-ignored, so a corpus-reading pytest would pass on MB's machine, SKIP on CI, and prove nothing anywhere -- and the core law says a check that skips itself is untested, not passing. So the SHAPE of the golden set is a pytest (no corpus needed, runs in the gate) and the TRUTH of it is a script whose output is quoted with a date. The control probe is not decoration and it is the half worth keeping: this story's FIRST pass of absence checks used `grep -E "pr.avis"`, where `.` matches one BYTE while `é` is two, and it reported ZERO occurrences of `préavis` in a document containing FORTY. Four out-of-scope questions were about to be written on that zero. A blind route and a true absence return exactly the same thing, so absence is only believable from a route that has been shown to find something first | MB |
| 2026-08-30 | ST-29 | Batch 2's branch is cut from ST-19's UNMERGED branch, not from latest main, and if PR #67 is sent back this branch is REBUILT rather than patched | wait for #67 to merge / cut from main and duplicate the schema + tests / cut from the ST-19 branch and say so | CLAUDE.md rule 2 says branches come from latest main, and this one does not. Cutting from main was the worse option, not the safer one: `batch2.jsonl` is meaningless without the schema and the ten tests that exist only on #67, so a main-based branch would have had to carry a second copy of both, and the merge would then have had to reconcile two versions of a schema. Waiting was available and was rejected because the human asked for batch 2 to start while #67 sits in review, which is exactly the parallel MB stream the project plan describes (section 5: "MB also runs one continuous stream across sprints 1-4 ... it never blocks the build line"). The cost is stated rather than hidden: this branch inherits #67's review risk in full | MB |
| 2026-08-30 | ST-29 | The per-batch counts live in ONE TABLE in `tests/unit/test_golden_set.py`, with a companion test that fails on any golden file the table does not list, and a hard cap at 40 in-scope + 20 out-of-scope | a test per batch (the batch-1 shape, copy-pasted) / one table / count only the running total | the plan builds this set in THREE batches, so the copy-paste route ends at three near-identical tests whose only difference is two numbers -- the third copy is precisely where the core law says abstract it or write down why not. Counting only the RUNNING TOTAL was rejected because it cannot tell which batch is short. The companion test is the load-bearing half: without it ST-35 could add `batch3.jsonl`, forget the table row, and every count test would go on passing while grading a set nobody counted -- a table that silently ignores what it does not know about is not a check. The cap exists because G2 grades "20 refusals out of 20": a 21st out-of-scope question does not merely add work, it makes the release gate's own denominator false | MB |
| 2026-08-30 | ST-29 | The golden set's French-language check is "an accented character OR at least two distinct French function words", and the marker list EXCLUDES words English also uses | keep the NFKD accent test / reword the offending question to contain an accent / accent OR French function words | THIS CHECK HAS NOW FAILED TWICE ON TWO DESIGNS AND WAS RIGHT BOTH TIMES. Version 1 was a hand-written list of accented letters and missed `g-in-008` ("À partir de quel âge"). Version 2, NFKD decomposition, rejected `g-out-009` -- "Comment saisir le conseil de prud'hommes contre mon employeur ?", flawless French carrying no accented character at all. Rewording the question was available and was refused outright: bending the artifact to satisfy a weak check is the shape the core law bans, and the question is a good one. The real fault is that both earlier versions were PROXIES for "is this French" that held on the rows written so far. Excluding English look-alikes ("a", "on", "par", "sans") is deliberate -- a marker an English sentence can contain makes the check weaker, not more generous; re-proven by putting an English question in batch 2 and watching it go red | MB |
| 2026-09-01 | ST-35 | The golden set is FROZEN at v1 by two mechanisms: the running total asserts `==` 40+20 rather than `<=`, and `FROZEN_IDS` pins the sixty ids as a contiguous run gated on `GOLDEN_SET_VERSION`. Neither pins the WORDING of a row | leave the cap at `<=` and rely on the README's freeze notice / pin a sha256 of the three .jsonl files / pin the id set and the exact counts / pin ids plus a normalised content hash | a `<=` cap was the right assertion while the set was BEING BUILT and is the wrong one now: it passes just as happily on 39 in-scope rows as on 40, so a row lost in a merge would leave F-08 measured against a denominator nobody chose. A BYTE HASH was the obvious way to also lock the wording and was REFUSED ON EVIDENCE, not on taste: this repo is checked out on two machines and git converts line endings on checkout -- the commit that added `batch3.jsonl` printed "LF will be replaced by CRLF the next time Git touches it" -- so a hash would go red on MB's clone or YL's for a reason with nothing to do with the golden set, and a check that cries wolf gets deleted. A NORMALISED hash (strip CR, then hash) would survive that and was still refused, because it locks whatever bytes it was first shown and its green then says nothing about whether it is aimed at the right side -- the exact failure this project already recorded when a drift test named "byte-identical to the S1-A3 delivery" compared against a constant instead of the delivery. So the mechanical check covers what it can honestly cover (a row added, dropped or renumbered) and the test's docstring SAYS OUT LOUD that wording is held by review and git history instead. Proven by mutation: renumbering g-in-040 to g-in-041 goes red | MB |
| 2026-09-01 | ST-35 | An out-of-scope row needs a term absent from every document AND a human ruling that the corpus cannot answer the question under any other name. Term-absence alone is not enough | keep the ST-19 rule (the probe term must be absent everywhere) / add the human ruling as a second, un-mechanised condition / try to mechanise the second condition too | the ST-19 rule is what makes the out-of-scope half falsifiable and it stays. It is just not sufficient, and batch 3 is where that showed: "Un pere a-t-il droit a un conge de paternite ?" passes the probe -- `conge de paternite` is in none of the three documents -- while the corpus answers it under another name, as g-in-015's three-day `conge de naissance`. Scored as a refusal it would have cost G2 a point for being right. Mechanising the second condition was considered and dropped: it amounts to asking whether a corpus answers a question, which is the product itself, so the check would be graded by the thing it is meant to grade. The condition is therefore recorded as a REVIEW step in the README beside the rows, not as code, and the two dropped candidates are written up so the next person sees the shape rather than the rule. Note which way each failure ran: `perte d'emploi` was caught BY the machine and `conge de paternite` was caught DESPITE it | MB |
| 2026-09-03 | ST-35 review | The freeze is `FROZEN_TOTALS = {"v1": (40, 20)}`, a LITERAL the totals are checked against, replacing `assert GOLDEN_SET_VERSION == "v1"` | keep the version assert and document the gap / bind the totals to the version with an in-file literal / put the sixty ids in a committed manifest file the test reads / go back to a normalised content hash | THE ROW ABOVE CLAIMED A FREEZE THAT DID NOT EXIST, and this row corrects it rather than quietly patching the file. `assert GOLDEN_SET_VERSION == "v1"` compared a constant to a literal nine lines above it in the same module: it can only fail if somebody edits the thing it reads, which is what the documented process tells them to do. `FROZEN_IDS` was derived from `FINAL_TOTAL_*`, so it was not an independent record either. The escape hatch was two number edits: set `FINAL_TOTAL_IN_SCOPE` to 41, set the `batch3.jsonl` row in `EXPECTED_COUNTS` to `(11, 5)`, add the row -- and the review RAN that and watched all 13 tests pass with the version still reading "v1". The MANIFEST FILE was the stronger option and was not taken: it adds an artifact beside the data whose only reader is one test, and the in-file literal closes the same hatch as long as the record is not edited under its own name. That residual is written into the test docstring and the README instead of being left to be discovered -- editing the "v1" row is tampering visible in any diff, which is a different risk from drift during a tuning session. Proven by deliberate violation: the 41st-row edit now fails on the freeze test specifically, and the tree was watched going red and then green again | ST-35 review pass, run on MB's clone (git identity meriem-mb; gitleaks absent, which is MB's machine per the header). The partner review rule 5 asks for the OTHER pair of eyes, so if this pass was not YL's, the rule is satisfied in form and not in substance -- say so here rather than let the row imply otherwise |
| 2026-09-03 | ST-35 review | `g-out-019` (titres-restaurant) and `g-out-020` (disability hiring quota) are KEPT as out-of-scope rows and listed as arguable in the README, rather than replaced | replace both with topics the corpus does not touch / keep both and say nothing / keep both and record them as arguable / drop the out-of-scope half's near-miss policy | both refusals are CORRECT on the question actually asked, verified by reading the corpus rather than by the probe: there is no hiring quota in any wording (`quota` 0 hits, `obligation d'emploi` 0, `tenu d'employer` 0, no effectif proportion), and no titres-restaurant scheme. But each sits beside material a good sourced answer could legitimately use -- labour code articles 166 to 171 impose real duties toward disabled workers, and dahir article 19's "tous autres avantages en argent, les avantages en nature" is a catch-all. G2 demands 20 refusals of 20, so a model that answers either of these WELL costs the gate a point. Replacing them was rejected because near-misses are the entire value of the out-of-scope half -- asking a labour-law corpus about football proves nothing -- and because the same argument would eventually empty it. Recording them is what the project already does with `g-out-005`, and the caveat is now a three-row table so the ST-36 discussion is about the question rather than about retrieval | ST-35 review pass, run on MB's clone (git identity meriem-mb; gitleaks absent, which is MB's machine per the header). The partner review rule 5 asks for the OTHER pair of eyes, so if this pass was not YL's, the rule is satisfied in form and not in substance -- say so here rather than let the row imply otherwise |
