# CHANGELOG-AI (one line per change, append only)

2026-07-20 | SETUP-000 | CLAUDE.md, PHASE3-KICKOFF-PROMPT.md, .claude/hooks/config.sh, .github/workflows/gate.yml, .claude/rules/{backend,frontend,git-discipline}.md, VARIABLES.md | filled kit variables for the Sanad uv stack, rewrote CI from npm to uv, aligned rule globs to the Sanad module map, tested safety walls | one-time Setup-Prompt configuration before the Phase 3 build
2026-07-20 | SETUP-000 | .claude/hooks/config.sh | deferred TYPECHECK_CMD and TEST_CMD (set empty) until first build story installs ruff/pytest via uv sync | greenfield repo has no test runner yet, so the Stop gate blocked every session
2026-07-20 | SETUP-000 | .claude/hooks/guard.sh, .claude/settings.json | fixed two Windows safety gaps: normalize backslash file_paths so the phase2/hooks/settings/.env edit-lock fires, and add PowerShell to the guard matcher + command checks so it is no longer an unguarded bypass; config.sh excepted as the one editable hook | wall test showed Edit/Write and PowerShell bypassed the guard on Windows
2026-07-20 | SETUP-000 | .gitignore | added; ignores .env, data/, qdrant/parent stores, model caches, pycache/.venv, claude logs (keeps .env.example and uv.lock tracked) | public repo had no .gitignore, risking secret/data commits
2026-07-20 | Phase3-S0 | docs/build/INTAKE-REPORT.md | intake gate report written | Step 0 deliverable; signed pack verified complete/consistent
2026-07-20 | Phase3-S1 | .claude/logs/graph-indexed.marker | repo indexed (254 nodes) + marker set | Step 1; graph queryable, confirmed greenfield
2026-07-20 | Phase3-S2 | docs/build/BUILD-PLAN.md | build plan drafted from 52 signed stories (refs+deps+exit gate) | Step 2; approved by human
2026-07-20 | ST-02 | pyproject.toml, uv.lock, config.py, .env.example, tests/unit/test_config.py | uv project skeleton: pinned stack (Arch §8), all tunables in config (no magic literals), smoke test, pytest pythonpath fix | Sprint-0 foundation; every downstream story needs the environment
2026-07-20 | ST-02 | pyproject.toml, uv.lock, config.py, .env.example | cloud provider swapped OpenAI -> Google Gemini (langchain-google-genai); local free models (Mistral) via Ollama; re-verified green | user chose Gemini free tier + free local models over OpenAI
2026-07-24 | ST-02/CI | .github/workflows/gate.yml | fixed the duplication gate: jscpd v5 removed --gitignore; switched to --ignore globs and excluded .venv/node_modules/*.md/uv.lock | CI verify job was failing on every PR at the dup step (blocked all merges)
2026-07-24 | docs | docs/START-HERE.md | added beginner team handbook (Sprints 0-1): setup, daily git loop, Sprint-1 stories split so MB codes too | onboard both people as coders and let them run the app now
2026-07-24 | ST-02/CI | .github/workflows/gate.yml | further gate fixes: jscpd --exitCode -> --exit-code (v5 rename); pass GITHUB_TOKEN to gitleaks-action v2 (now required for PRs) | gate now fully green; PR #2 merged to main (squash 3eb19f2)
2026-07-24 | docs | docs/START-HERE.md | hardened uv setup: -ExecutionPolicy ByPass install, quit/reopen VS Code fix, `python -m pip install uv` fallback, any-Python note | teammate hit "uv not recognized" (PR #3, merged 442abb0)
2026-07-28 | ST-11 | workspaces.py, db/repo.py, tests/unit/test_workspaces.py | workspaces module: create/rename/delete/list/get + legal_flag toggle over the ST-10 schema; domain errors (WorkspaceNotFoundError, DuplicateWorkspaceNameError) replace raw sqlite3.IntegrityError; thin get_workspace/list_workspaces/update_workspace_name/update_workspace_legal_flag added to db/repo.py; 12 new tests incl. real-disk-files delete test | F-01 module-level exit gate (workspace scoping, empty listing, derived-data-only delete)
2026-07-25 | ST-04 | .github/pull_request_template.md, docs/build/BUILD-STATE.md | PR template carrying the five-point checklist from architecture §12.2 lines 480-484 verbatim, plus INTENT/VERIFY fields, squash-only + Conventional-Commit-title guidance, and a reviewer grade block; branch protection on main NOT done (human-owned settings action) | every PR should show the signed checklist; reviewer graded 8/10 and sent it back for exactly this missing journal entry
2026-07-25 | ST-10 | db/schema.sql, db/repo.py, db/__init__.py, tests/unit/test_db_repo.py | SQLite registry schema (6 tables, arch §7.3 DDL translated per §7.4 deviations) + repo.py data access (single `_connect_raw` call site, PRAGMA foreign_keys=ON, session() txn helper, minimal insert/delete per table) + 12 unit tests (cascade delete, SET NULL on sync_item.document_id, all 6 sync_item.result values + rejection, unique file-per-workspace across/within workspaces) | ST-10 exit gate; foundation for ST-11/ST-12
2026-07-25 | ST-10 | docs/build/BUILD-STATE.md | b1 integration pass: pushed feat/S1-ST-10-db to origin (7182b47, fast-forward), re-ran ruff + pytest independently (clean / 14 passed), recorded ST-10 as PR-open-awaiting-human-merge (NOT merged) and logged the reviewer's five non-blocking follow-ups for ST-11; corrected the false "repo indexed" claim (graph unavailable, absence checks run on grep); deregistered the stale agent worktree | merge is a human decision and the follow-ups must survive the branch
2026-07-25 | ST-10 | docs/build/BUILD-STATE.md | orchestrator correction: branch state said "PR OPEN" but no PR exists (gh not installed, no GH_TOKEN), and the pushed head was ad7381a not 7182b47; recorded the gh gap and the untracked .claude/agent-memory/ decision as blockers | the flight recorder must not claim a state that does not exist
2026-07-25 | ST-10 | docs/build/DECISIONS.md, docs/build/BUILD-STATE.md | decision: PR tooling = human installs gh CLI + gh auth login, agents use gh for PR open/CI status, merge stays a human press; blocker updated with the exact install + verify commands and the PATH-reload gotcha | agents cannot open PRs without gh and must never extract the GCM token to work around it
2026-07-25 | ST-10 | .gitignore (chore/gitignore-worktrees), .claude/agent-memory/*, docs/build/BUILD-STATE.md, docs/build/DECISIONS.md | installed gh 2.96.0 via winget (auth still pending, browser flow needs a human); gitignored .claude/agent-memory as machine-local scratch and refreshed its two stale notes; BUILD-STATE blocker narrowed to the single `gh auth login` command | unblock agent-driven PRs and stop a second unreviewed memory from contradicting the flight recorder
2026-07-27 | docs | README.md | added root README with a Mermaid pipeline flowchart (ingestion -> vector retrieval -> LLM prompt generation), quickstart, doc map, locality and gate notes | repo had no root README; the three-phase flow was only readable by cross-reading Architecture 5.1/5.2/7.5
2026-07-28 | ST-10 | db/repo.py, tests/unit/test_db_repo.py | fixed all 5 reviewer findings: `init_db` no longer commits and no longer uses `executescript()` (its own implicit pre-commit force-committed pending session writes regardless of the trailing commit(), verified empirically) -- now executes each DDL statement individually so `session()` alone owns the transaction; added session() commit-persists and exception-rolls-back tests (rollback test proven to fail pre-fix, pass post-fix); PRAGMA test now also asserts on a second freshly opened connection; added a negative FK test (`insert_document` with a bogus workspace_id); `_count` now whitelists table/column identifiers instead of raw f-string interpolation | reviewer verdict was 8/10 not-safe-to-merge; the real fix for finding 1 required more than the literal "drop the commit()" instruction once executescript's own commit semantics were checked
2026-07-28 | chore/no-ai-attribution | CLAUDE.md, .claude/rules/git-discipline.md, .claude/agents/b1-lead-dev.md | banned AI attribution in commits, PRs and issues: no Co-Authored-By trailer naming an assistant, no [AI] subject marker, no "generated with" footer; b1 strips any that slip through before squashing; human co-authors are still credited normally | Sanad is graded academic work and the history must carry the team's names only; CI greps for INTENT: and never for [AI], so the gate is unaffected
2026-07-28 | chore/journal-refresh | docs/build/BUILD-STATE.md, .gitattributes | refreshed the flight recorder to main=e7c777f (ST-10 moved to Done with both review verdicts recorded, ST-11 set as the task in flight and marked NOT STARTED, ownership flagged for confirmation); added .gitattributes with merge=union on CHANGELOG-AI.md and DECISIONS.md | BUILD-STATE had drifted for the third time and still claimed ST-10 was awaiting a PR it had already been merged by; all three merges that day conflicted in the append-only journals and nowhere else
2026-07-28 | ST-11 | workspaces.py, config.py, db/repo.py, tests/unit/test_workspaces.py | fixed all 7 reviewer findings: scoping test now fetches the second-inserted workspace by id (mutation-proven both ways); IntegrityError translation checks for the literal `UNIQUE constraint failed: workspace.name` text before raising DuplicateWorkspaceNameError, re-raising anything else; added `InvalidWorkspaceNameError` + `_validate_name` enforcing config-sourced 1-100 length bounds on create/rename; cascade test now counts sync_item and eval_result pre- and post-delete; `get`/`list` now use `repo.session()` instead of hand-rolled connection lifecycle; `_connect_raw` bootstraps schema.sql on every connect so the default `sqlite_db_path` works with no separate setup step | reviewer verdict was 7/10 not-safe-to-merge with 4 blockers; four blockers plus three craft/debt items fixed in one branch per the review's own scope
2026-07-28 | ST-11 | db/repo.py, workspaces.py, config.py, tests/unit/test_db_repo.py, tests/unit/test_workspaces.py | fixed the 2nd re-review's blocker: `_connect_raw` restored to pure connect+PRAGMA (no mkdir/init_db); added `repo.ensure_schema()` as the sole bootstrap entry point, called by `session()` for writes; `get_connection()` now raises `RegistryNotFoundError` instead of silently creating an empty registry when the path does not exist, so reads never fabricate one; also added `folder_path` empty/whitespace validation (`InvalidFolderPathError`, config-sourced min length), stripped-name whitespace check, and extracted `_get_workspace_or_raise` (was 3 copies); 10 new tests, 46 total | mistyped/unmounted `db_path` was reading back as an empty list, corrupting F-01 criterion 2's own exit gate
2026-07-28 | ST-11 | workspaces.py, tests/unit/test_workspaces.py | names are now normalized before persistence, not only before validation: `_validate_name` became `_normalize_name`, returns the stripped value, and create/rename store what it returns | validating a stripped copy while storing the raw string let "x"*100+" " persist at 101 chars (breaching openapi.yaml maxLength 100) and let "HR" and " HR " coexist as two rows despite workspace.name being UNIQUE; 5 tests added, each proven to fail against the pre-fix behaviour and pass after
2026-07-28 | chore/st11-journal | docs/build/BUILD-STATE.md, docs/build/DECISIONS.md | reconciled the flight recorder to main=1a4f3fb after ST-11 merged: nothing in flight, ST-11 in Done with all three review verdicts and what each caught, data-layer follow-ups renumbered with 3 and 4 marked CLOSED-by-ST-11 and two new ones added (partial-registry detection, and the missing sqlite timeout that will surface at ST-17); recorded the ST-12-to-MB ownership deviation and the name-normalization decision | the plan assigns ST-12 to YL so reassigning it needs a written reason on graded work, and the two new follow-ups are cheaper to fix before ST-12 builds on the data layer than after
2026-07-28 | CR-02 | pyproject.toml, uv.lock, CLAUDE.md, README.md, docs/build/BUILD-PLAN.md, docs/build/CR-02.md | CR-02 Part B: swapped the UI dependency (gradio removed, jinja2>=3.1 added), regenerated the lockfile (176 lines out, 2 in), updated the stack lines and the ST-51 row, and filed the change-request record | the human withdrew ADR-02's accepted cost of "less visual polish and layout control" because an examination jury judges the interface; Part A amends the signed pack and is applied by a human
2026-08-01 | ST-12 | change_detection.py, config.py, db/repo.py, tests/unit/test_change_detection.py, tests/unit/test_db_repo.py | new `change_detection.py` implementing the architecture §5.1 `H{content hash vs registry}` decision: `compute_fingerprint` (incremental SHA-256 + byte size, config-sourced chunk size), `scan_folder` (non-recursive, supported types from config, unsupported files carried out as Skipped-with-reason rather than dropped), `detect_changes` returning NEW/CHANGED/UNCHANGED/REMOVED per file; fingerprint persists as the parseable string `sha256:<digest>:<size>` in the existing `document.content_hash` column so the signed §7.3 DDL is not widened; repo gained `list_documents` (workspace-scoped) and `delete_document`; `_connect_raw` now sets a config-sourced busy `timeout`; 34 tests, 51 -> 85 | ST-12 exit gate (four transitions + size+hash collision guard) and BUILD-STATE data-layer follow-ups 5 and 7
2026-08-01 | ST-12 | change_detection.py | FolderNotFoundError interpolates the path plainly instead of with `!r`: repr() escapes every backslash, so a Windows path reached the user as 'C:\\Users\\...' | PRD §8 S2 error state requires the exact path shown; a test asserting the real path in the message caught it
2026-08-01 | ST-12 | docs/build/BUILD-STATE.md | recorded PR #14 open for the ST-12 branch, and a new machine-specific note: gh is authenticated here but absent from the agent shell's PATH, so it must be called as `C:\Program Files\GitHub CLI\gh.exe` | a bare `gh` failing looks identical to the July gh gap that stranded three branches for three days; the note stops that misdiagnosis repeating
2026-08-01 | ST-12 | tests/unit/test_db_repo.py | added two regression tests for the busy timeout closed earlier today: one spies on sqlite3.connect and asserts the config value is what gets passed, one holds a real EXCLUSIVE lock and races a second connection, asserting BOTH a floor and a ceiling on how long it waited; 85 -> 87 | follow-up 7 shipped untested. The ceiling is the load-bearing half: with only a floor the test was vacuous, because dropping the `timeout` argument restores sqlite3's 5.0s default, which is LONGER than the 0.5s the test configures, so the mutation stayed green while config was ignored
2026-08-01 | safety | docs/build/BUILD-STATE.md | recorded a CRITICAL human-only blocker: guard.sh parses tool events with `python3`, which on this machine is the Microsoft Store alias stub (exits 49, no output), and the parse is wrapped in `|| exit 0`, so the hook fails OPEN and all 15 of its deny checks are dead; the git-level pre-commit belt-and-braces was never installed either | this is why the ST-12 commit landed on main before being moved; a guard that cannot read its input must fail CLOSED, and neither layer announces its own absence
2026-08-01 | ST-12 | change_detection.py, tests/unit/test_change_detection.py | self-review after CI went green found three defects around present-but-unreadable files: scan_folder let UnreadableFileError escape (one bad file aborted the whole sync, breaking PRD F-02 #3 and contradicting compute_fingerprint's own docstring); an unreadable file fell into the REMOVED sweep because it was absent from `fingerprints`, so ST-17 would have deleted the chunks of a file still on disk; and FileChange's docstring wrongly claimed document_id is None whenever status is NEW, when a file returning after removal is NEW and keeps its row (ST-17 must UPDATE, not INSERT, or it violates UNIQUE(workspace_id, file_name)). Added ScanResult.unreadable / ChangeReport.unreadable, 6 tests, 87 -> 93 | all three sat in one blind spot: every existing test asked what happens to a file, none asked what happens to a file that cannot be read
2026-08-01 | chore | docs/build/BUILD-STATE.md | reconciled the flight recorder: ST-12 is MERGED as 1862a58 (PR #14), not "on branch, not reviewed, not merged" as this file claimed, and it shipped without the reviewer pass the Next queue was waiting on -- recorded as owed. Rewrote the safety blocker: pre-commit installed and guard.sh now fails closed (13/14 deny checks verified firing), but the `python3` -> `python` swap was BAD ADVICE and is a regression that will block every tool call on macOS/Linux, six sibling hooks are still dead on python3, and four smaller holes remain | the flight recorder claimed a state that did not exist, which is the exact failure it exists to prevent; and the orchestrator's own hook advice made the system worse for teammates while fixing it locally
2026-08-01 | ST-13 | conversion.py, config.py, change_detection.py, .env.example, tests/unit/test_conversion.py | new `conversion.py` implementing the ADR-07 ladder: pymupdf4llm for PDF (headings preserved, real page count), markitdown for DOCX behind a package check, strict-UTF-8 passthrough for TXT/MD; `convert_file` returns CONVERTED/FAILED/SKIPPED with a plain-language reason and never raises for a bad document, `convert_files` is the batch demonstration of PRD F-02 #3; two config knobs (`conversion_min_text_chars`, `text_file_encoding`) added and documented in .env.example; change_detection's `_file_type` and `_UNSUPPORTED_TYPE_REASON` promoted to public so the ladder dispatches on the same rule the scan filtered by and repeats the scan's own wording; 37 tests, 93 -> 130 | ST-13 exit gate (fixture corpus converts, corrupted file reports Failed, scanned PDF reports Skipped with a reason)
2026-08-01 | ST-13 | conversion.py | `_read_pdf` checks `doc.needs_pass` before calling to_markdown, and names `pymupdf.FileNotFoundError` as its own except clause | an encrypted PDF OPENS without error in pymupdf 1.28.0 and even answers `page_count`; only `needs_pass` reveals it, and without the check pymupdf4llm dies on an undeclared `TypeError: 'NoneType' object is not subscriptable` mid-batch, over exactly the file PRD §11 requires be reported as one Failed row. Separately `pymupdf.FileNotFoundError` SHADOWS the builtin and subclasses RuntimeError, so `except OSError` does not catch a missing PDF -- it was falling into the last-resort catch and logging a traceback for an ordinary mid-sync race
2026-08-01 | ST-13 | conversion.py, tests/unit/test_conversion.py | `_read_docx` validates the OOXML package (opens the zip, requires `word/document.xml`) before handing the file to markitdown | markitdown 0.1.5 does NOT raise on a .docx it cannot parse as Word: it falls through to another converter and returns the result AS A SUCCESS -- a corrupted .docx came back as the literal bytes 'PK\x03\x04garbage-not-a-zip' and a valid non-Word zip came back as a directory listing. Either would be reported Added, embedded, and one day cited to a user as a source: a silent poisoning of the index that no exception announces
2026-08-01 | ST-13 | conversion.py | removed a redundant `zipfile.is_zipfile` pre-check and a `DocumentSkippedError` class that nothing ever raised | mutation testing proved the is_zipfile branch could not be the cause of any verdict (opening the ZipFile already raises BadZipFile on the same inputs), and self-review after CI went green found the unraised exception class -- a second way to express an outcome that no rung used. Mutation testing cannot see a branch nothing reaches; only reading the diff finds those
2026-08-01 | ST-13 | tests/unit/test_conversion.py | every fixture is a real file built at test time -- a genuine multi-page PDF and a genuine AES-256 encrypted PDF from pymupdf, a genuine OOXML package from stdlib `zipfile`, genuinely corrupted bytes; no converter is ever mocked, and no binary blob is committed | ST-13's entire risk lives in what two third-party parsers do with hostile input, and a mocked parser answers that question by assuming it. Building the docx by hand also avoided adding python-docx as a real dependency bought with a test's convenience
2026-08-01 | chore | .claude/hooks/config.sh | restored TYPECHECK_CMD and TEST_CMD to `uv run ruff check .` and `uv run pytest -q`; they had been left empty since before the stack existed | the typecheck and stop gates were running empty commands and passing vacuously. NOTE: inert until the six sibling hooks stop calling the dead `python3`
2026-08-01 | chore | .claude/hooks/guard.sh, docs/build/BUILD-STATE.md | guard.sh now tries python3 then python and prints a reason before exit 2; verified with a 13-case harness across three simulated machines (python-only, python3-only, neither). Recorded the six still-dead sibling hooks and four smaller guard holes as deprioritized debt | the critical layer is closed; the human halted further hook work to get back to building, which is the right call since none of the remainder blocks a story
2026-08-09 | ST-14 | chunking.py, tests/unit/test_chunking.py | new `chunking.py` implementing the architecture §7.5 parent/child split: `_split_on_headings` cuts on H1-H3 (H4+ deliberately does not split, and a `#` inside a fenced code block is not a heading), `_merge_small_sections` glues consecutive sections until they reach `parent_merge_below_chars`, `_split_oversized` breaks anything past `parent_split_above_chars` at paragraph boundaries, and `_windows` cuts each parent into `chunk_child_size_chars` windows sharing `chunk_child_overlap_chars`. Returns frozen Parent/Child dataclasses carrying the exact §7.5 Qdrant payload fields (parent_id, source_file, section_label). No new config: ST-02 already pinned all four knobs. 39 tests, 130 -> 170 | ST-14 exit gate (boundary tests at 2,000 and 4,000 characters; 100-char overlap verified)
2026-08-09 | ST-14 | chunking.py | a merged parent is labelled with the RANGE of headings it covers ("Article 1 ... Article 3"), not just its first heading | PRD F-03 puts the section label in every citation, so a parent spanning Articles 1 to 3 labelled "Article 1" cites a sentence from Article 3 under the wrong heading. The separator is " ... " and not " - " because real headings carry their own dashes: the first version rendered "Article 2 - Duree - Article 4 - Rupture", where the reader cannot tell the range from the heading's punctuation. Every test heading had been a tidy "Article 1"; only running a realistic document showed it
2026-08-09 | ST-14 | chunking.py, tests/unit/test_chunking.py | added `_validate_settings`, refusing four chunk configurations up front with `InvalidChunkSettingsError` | self-review after the suite was green found that `_windows` had guarded its own infinite loop from the start while `_split_oversized` contained the IDENTICAL loop with no guard at all: `parent_split_above_chars <= 0` hangs forever appending empty strings (proven, killed at 10s). Two more silent modes were found in the same pass and neither hangs, which is worse: a non-positive child size yields zero children so a document indexes as unsearchable, and a NEGATIVE overlap leaves gaps between windows, so a passage sits in the parent but is never embedded and the only symptom is the assistant refusing a question about text it holds. The relocated guard replaced the inline one rather than joining it, so no branch is left that a test cannot reach
2026-08-09 | ST-14 | tests/unit/test_chunking.py | added a byte-exact round-trip test for `_split_oversized`, run at a small non-default limit so the piece boundaries are arithmetic rather than luck, plus a companion test for two paragraphs packed into one piece | mutation testing showed the `<=` early return to be an EQUIVALENT mutant (at exactly the threshold the fast path and the paragraph packer produce identical output), which makes the packer the thing that actually guarantees no text is lost. The first version of the round-trip test used the real 4,000 limit and a blank-line run placed mid-piece, and a mutation stripping every piece SURVIVED it; a whitespace bug that only appears at a boundary needs a test that puts it on the boundary. The companion test exists because the first still could not see a mutation that rejoined packed paragraphs with a single newline: no surviving piece held two paragraphs
2026-08-09 | ST-14 | docs/build/BUILD-STATE.md | corrected the flight recorder, which still said ST-13 was "on branch, NOT reviewed, NOT merged" while it was already on main as b75b584 (PR #18) | this is the third time this file has described a state that no longer existed, and it is the exact failure the file exists to prevent
2026-08-09 | ST-14 | docs/build/DECISIONS.md, docs/build/BUILD-STATE.md | rubric self-review recorded two properties that were true but unwritten: `chunk_document` is not idempotent (random parent ids per run, proven by chunking the same input twice) and is a hazard ST-16/ST-17 must handle; and per-file logging deliberately lives in the sync loop rather than the pure ingestion modules. Also measured chunking for the first time: 1,275,492 characters (~200 pages, the G5 corpus size) splits in 0.03s into 600 parents and 3,600 children, so chunking is nowhere near the G5 budget of 10 minutes | 'if it is not written down it did not happen'; both were design positions being carried in my head rather than in the repo
2026-08-09 | ST-14 | chunking.py, tests/unit/test_chunking.py, docs/build/DECISIONS.md | parent ids are now derived (`uuid5` over source_file + position) instead of random, making `chunk_document` idempotent; 3 tests added (same document twice gives the same ids, a changed document reuses the ids it still has, two files never collide), 40 -> 43 chunking tests. Removed chunking.py's only `db.repo` import as a side effect, so the splitter no longer depends on the data layer. Recorded as the project's first SUPERSEDED decision chain: the two earlier rows are marked replaced with their reasoning left intact | random ids orphaned every parent JSON on re-sync of a changed file and could leave a vector pointing at a deleted parent -- a search hit that resolves to nothing
2026-08-09 | chore/dev-setup-script | scripts/setup-dev.sh, docs/START-HERE.md | added a one-command developer setup that checks uv, packages, gh and its login, the pre-commit branch guard and the git board alias, then proves the machine works by running lint and the full suite; START-HERE points at it and documents the daily `git board` check | MB's machine drifted from YL's (no gh auth for days, no code graph, stale packages after the CR-02 dependency swap) and manual setup steps were being skipped; the script also deliberately refuses to install codebase-memory-mcp because the install source it found carried apparent prompt-injection text
2026-08-09 | fix/setup-script-path-detection | scripts/setup-dev.sh | setup script now tells "not installed" apart from "installed but not on PATH": added locate_tool() which falls back to the winget Links, Program Files, Programs and ~/.local/bin locations, and win_dir_of() to print a pasteable Windows folder; applied to both gh and uv | on MB's machine gh 2.96.0 was installed but off PATH, and the script reported "gh is not installed" and advised installing it, which would have produced a second copy and fixed nothing; proven by stripping the gh directory out of PATH on a working machine and confirming the message flips
2026-08-09 | fix/setup-summary-shows-the-fix | scripts/setup-dev.sh | the summary block now prints the exact command that fixes each problem, not just the problem's name: bad() takes an optional fix string, stored and replayed at the bottom; added a note distinguishing an export-PATH fix (do NOT close the window) from an install fix (do close it) | MB re-ran the script, got the correct PATH diagnosis, but the cure was 30 lines up the scrollback and the summary she actually read told her only what was wrong; the closing line also said "close and reopen your terminal", which would have thrown away the export it had just told her to run
2026-08-09 | ST-14 | chunking.py, tests/unit/test_chunking.py | ST-14 MERGED as 711f7e0 (PR #20). Parent/child chunking per architecture §7.5: sections cut on H1-H3, merged below `parent_merge_below_chars`, split above `parent_split_above_chars` at paragraph boundaries, then windowed into `chunk_child_size_chars` children sharing `chunk_child_overlap_chars`; parent ids derived (`uuid5` over source_file + position) so re-chunking is idempotent, which also removed the module's only `db.repo` import; four invalid chunk-settings combinations refused up front rather than hanging or silently dropping text; 173 passed (130 -> 173), 37 mutations, one proven equivalent | ST-14 exit gate (100-char overlap verified, boundary tests at 2,000/4,000 chars)
2026-08-16 | ST-15 | embeddings.py, tests/unit/test_embeddings.py | ST-15 MERGED as 8e5a734. multilingual-e5-base embeddings behind one private encoder seam and three public functions, so the ADR-05 `passage: `/`query: ` prefix rule cannot be bypassed; independent review graded 7/10 and found three blocking defects, all fixed: the prefix tests were self-referential (built expected values from the same config the code reads, so emptying the prefix in config left the suite green -- now literals), `load_model` was public so `.encode()` could skip the seam entirely (now private), and `embed_passages("text")` embedded one vector per character because `str` satisfies `Sequence[str]` (now raises). 191 passed and 1 skipped (up from 173), skip is the real-model test opt-in via `SANAD_RUN_MODEL_TESTS=1` | ST-15 exit gate; the reviewer's find is the exact silent-degradation failure ADR-05 exists to prevent
2026-08-17 | chore/harness-migration | .gitignore, .cbmignore, pyproject.toml, CLAUDE.md, .claude/* (deleted) | MERGED as 275886f (PR #25). Finished a half-done control-plane move: the harness installer zip had unpacked into the repo root (./agents, ./commands, ./hooks, ./rules, ./skills, ./evals) instead of a staging folder, burying Sanad's own ~770-node code graph under 5,637 nodes of vendored payload and failing ruff on 3 sample-script lint errors that are not Sanad's. Payload ignored in three places that must stay in step (.gitignore, ruff extend-exclude, new .cbmignore); the old in-repo control plane (`.claude/agents`, `.claude/hooks`, `.claude/rules`, `.claude/skills`, 41 files) deleted since the control plane now lives in `~/.claude` at the user level; CLAUDE.md corrected to stop pointing at removed paths and to name the agents that actually exist (`architect`/`scout`/`verifier`/`coach`) instead of the removed b1..b4/reviewer/o1..o4 set | ruff clean (down from 3 errors), 191 passed/1 skipped unchanged, graph rebuilt to 770 nodes/2,393 edges (down from 5,637) with zero skipped/parse-partial, CI verify green
2026-08-22 | chore/harness-fit | .claude/hooks/_config.mjs, .claude/hooks/gate.mjs, .claude/hooks/config-guard.mjs, .claude/harness.json, .claude/settings.json | Repaired a harness that fired but enforced nothing on this project. Three defects, each proven by running it rather than reading it. (1) The Stop gate was a permanent no-op: `gate.mjs` hard-exited on a missing package.json/node_modules BEFORE calling `checks()`, and `checks()` itself returned [] without a package.json, so a Python/uv repo could declare checks that could never run -- the turn ended green having executed nothing. `checks()` now accepts a literal `cmd` and the npm early-exits are conditional on an npm-shaped check. (2) `guardedPaths()` was exported by _config.mjs and imported by NO hook, so `guardedPaths: ["docs/phase2/"]` -- the signed-spec lock CLAUDE.md rule 4 calls non-negotiable -- was enforced nowhere; a Write aimed at Sanad_PRD_v1.0.md passed all four PreToolUse hooks. (3) `config-guard.mjs` read only `tool_input.command` and was registered on the Bash matcher alone, so every Write/Edit bypassed it entirely. Also: the staleness scan never saw the flat root modules (sourceDirs was [db, tests]), so editing chunking.py left the gate believing nothing had changed | the gate and the spec lock were both dead, and a check that has never executed is untested, not passing
2026-08-22 | chore/harness-fit | .gitignore, .claude/** (untracked) | Took the Claude Code control plane back out of the repository: 98 files removed from the index with `git rm -r --cached .claude`, `.claude/` added to .gitignore. This restores the outcome of PRs #25/#28/#29, which PR #31 had reversed by installing the harness at project level. Files stay on disk and keep working; only git stops carrying them. Consequence recorded in the ignore rule itself: a local repair to .claude/hooks/ now lives on one machine and is invisible to the other teammate, so durable hook fixes belong in ~/.claude/hooks/ | the harness is tooling, not Sanad; it buried the code graph and put unreviewable churn in story diffs
2026-08-22 | chore/harness-fit | .gitignore, .mcp.json (untracked), docs/journal/BUILD-STATE.md | Closed the verification gaps left by the first two commits and recorded the six that no agent can close. Proven since: the whole CI gate run locally in gate.yml order for the first time (uv sync --frozen clean, a step never previously executed here, plus ruff and pytest), and the skip-dir change MEASURED rather than asserted -- the old four-entry list walked 39,486 files in 2,529ms against 47 files in 2ms, and 22,101 of those carried source extensions inside .venv, so the staleness stamp was tracking DEPENDENCY mtimes rather than Sanad's code: a correctness bug, not merely a slow scan. Also untracked .mcp.json, which had been committed carrying an absolute per-user Windows path that would fail silently on the other teammate's machine | a gate step that has never executed is untested, and a machine-specific path in a shared file is a trap
2026-08-22 | ST-16 | vector_store.py, parent_store.py, embeddings.py, tests/unit/test_vector_store.py, tests/unit/test_parent_store.py, tests/unit/test_embeddings.py | Architecture §7.5's two derived stores. `parent_store.py` writes `data/parents/<workspace_id>/<parent_id>.json` atomically (temp file plus os.replace, because parents are read at ANSWER time while a sync may be rewriting them) and refuses any id that could escape the directory; `vector_store.py` gives each workspace its own Qdrant collection `ws_<workspace_id>_children` carrying a 768-dim dense vector, a sparse BM25 vector and the four §7.5 payload fields. Isolation is STRUCTURAL in both -- a collection that does not hold a point cannot return it, unlike a filter someone has to remember to add. `embeddings.py` gained the sparse half behind the same seam as the dense one, because fastembed's BM25 is asymmetric (`embed` for documents, `query_embed` for queries) and using the wrong side raises nothing: exactly the silent degradation ADR-05 exists to prevent. Deletion of a document's vectors and its parent files is ONE call, ordered vectors-then-parents so a crash strands orphan files rather than live vectors citing files that are gone. Child point ids derived by uuid5 because qdrant-client 1.18 rejects a non-UUID point id outright | ST-16 exit gate (isolation both directions, parents resolve by id); 11 mutations, one SURVIVED and exposed a vacuous test of my own -- the batching fixture produced one parent holding one child, where a per-parent and a document-wide numbering are indistinguishable
2026-08-23 | ST-16 | docs/journal/BUILD-STATE.md | closed the ST-16 entry: it said "pushed and PR opened" while 52ee47b was already on main. Now records the merge commit, the PR number, the author-merge deviation, and the post-merge re-verification ON main (ruff clean, 272 passed / 2 skipped) rather than only the branch numbers | this file has three separate CORRECTION notes about describing a state that no longer existed; writing the closing line in the same session as the merge is the only thing that has ever prevented it
2026-08-23 | ST-17 | sync.py, tests/unit/test_sync.py, db/repo.py | new `sync.py` implements architecture 5.1 end to end and is the first caller of any ingestion module: scan -> classify -> convert -> chunk -> embed -> write both derived stores -> registry -> one report row per file. Six `sync_item.result` values, PRD F-02's four criteria on real fixtures (real SQLite, real embedded Qdrant, a real AES-256 encrypted PDF; only the two encoders are faked), a second Sync blocked by the unfinished run row plus a process lock across the check-and-insert, and per-file transactions so one bad file cannot roll back the batch. Derived stores are written PARENTS THEN VECTORS, the mirror of vector_store's delete order, pinned by a test that crashes the second write and asserts no vector lacks a parent. db/repo.py gained update_document, get_sync_run, get_running_sync_run, finish_sync_run and list_sync_items. 44 tests, 272 -> 318; 12 mutations injected one at a time, all 12 killed | nothing called ST-12..ST-16 before this; F-02 is the product's second-highest feature and the five stages were individually green and collectively untested
2026-08-23 | ST-17 | change_detection.py, tests/unit/test_change_detection.py | split `_STATUS_WITHOUT_DERIVED_DATA` into two constants answering two different questions: re-ingest (`removed`, `failed`, `skipped`) and already-reported-removed (`removed` only). Added 3 tests pinning both directions, including one asserting an `active` row with identical bytes is still UNCHANGED so the fix cannot degenerate into re-ingesting everything every sync | a `failed` row with unchanged bytes classified UNCHANGED forever, so a corrupted file was reported Failed on its first sync and then dropped out of every later report while still being unanswerable, and a file the user repaired was never picked up -- PRD F-02 criterion 3 held for exactly one run. The existing suite never caught it because ST-12 was the module that reads these statuses and ST-17 is the first that writes them
2026-08-23 | ST-17 | sync.py, tests/unit/test_sync.py | self-review after green: `sync_workspace` now calls `vector_store.ensure_collection` once per run, after the folder scan succeeds | `upsert_children` returns before creating the collection when there are no children, so a run in which every file was Skipped or Failed left no collection at all and `vector_store.search` then told the user the workspace had NEVER BEEN SYNCED seconds after a sync. Reproduced by deleting the line and watching two tests fail with that exact message
2026-08-23 | ST-17 | ~/.claude/settings.json (machine-local, not in repo), docs/journal/BUILD-STATE.md | fixed all 7 codebase-memory hook registrations: the command was wrapped in SINGLE quotes around `cmd.exe /c`, which cmd.exe does not treat as quoting, so cmd started interactively, printed its banner, echoed the hook JSON and exited 0 -- the hooks never ran. Replaced with the plain quoted path, tested from bash and cmd.exe before writing, backup verified byte-identical, edit made on parsed JSON proven to round-trip byte-exactly, asserted to change nothing outside `hooks`, then all 3 distinct commands executed as stored (exit 0, real hookSpecificOutput) and the harness proven to FAIL on the old form | the two dead hooks were `cbm-session-reminder` and `cbm-code-discovery-gate`, i.e. the exact mechanism that tells an agent to query the code graph before reading files -- which is why this session opened ST-17 with Read/Grep on a fully indexed repo. BUILD-STATE had recorded this registration as the CORRECT one and claimed a permissions deny-list made it human-only; both claims were false and are corrected
2026-08-23 | ST-17 | docs/journal/BUILD-STATE.md, docs/journal/DECISIONS.md | closed the ST-17 entry against the merge that actually happened: MERGED as 0210408 (PR #35, squash), CI verify green on all four gate steps, gate re-run ON main after the merge (318 passed / 2 skipped) rather than trusting the branch numbers. Queue re-ordered: ST-07 is now the sole blocker on ST-18, and the reviewer-pass debt is recorded against TWO stories, ST-12 and ST-17 | this file has three prior CORRECTION notes about describing a state that no longer existed, every one of them a branch recorded as unmerged while it was already on main; closing the entry in the same session as the merge is the only thing that has ever prevented it
2026-08-23 | fix/S1-citation-label-markup | chunking.py, tests/unit/test_chunking.py | added `_clean_label`: strips bold/italic/code/HTML markup from a heading before it becomes a section_label, collapses ragged whitespace, and returns None for a markup-only heading. 10 new cases, 320 -> 330 tests, 7 mutations injected one at a time and all 7 killed | found by RUNNING the ST-17 sync engine on real downloaded PDFs rather than fixtures: a real labour-law PDF produced `**G-** **<u>Securite sociale et charges sociales</u>**` as the section label, which is the text a user would have been shown as the source of an answer (PRD F-03). Two mutation SURVIVORS were read rather than dismissed -- one was a genuinely redundant guard now pinned at `_clean_label`'s own level, one was undistinguishable whitespace collapsing now pinned by a ragged-spacing case
2026-08-23 | ST-17 smoke run | data/corpus/ (git-ignored), docs/journal/ | ran the merged sync engine end to end on 3 real French PDFs (119-page Moroccan labour code from the ILO, a CNSS social-security guide, a labour-law summary) plus 10 French Python .txt manuals: 13 files, ~672k chars, both conversion rungs, real multilingual-e5-base and real fastembed BM25. Result: every file Added, 0 cross-workspace leaks, 0 unresolved parents, second sync all Unchanged, double-sync guard blocked correctly against the real registry. Query latency 0.11-0.14s | ST-17 merged without ever running outside pytest, and on this project the real-document run has found a defect every time (ST-14's unreadable range label, ST-16's too-small isolation fixture). It did so again -- see the two DECISIONS rows filed the same day
2026-08-23 | fix/S1-citation-marker-labels | chunking.py, config.py, tests/unit/test_chunking.py | parents and children are now labelled by the citable markers inside their own text (`_citation_label`, config `parent_citation_marker_pattern`, case-sensitive, empty string disables), falling back to the enclosing heading. 6 new tests written to FAIL FIRST against the old behaviour, 330 -> 336, 7 mutations injected one at a time and all 7 killed after a survivor exposed untested whitespace normalisation | closes the OPEN ESCALATION raised the same day, and closes it by withdrawing it: the escalation said the fix needed a change request against signed 7.5, and re-reading 7.5 showed it constrains where parents are SPLIT while saying nothing about how `section_label` is computed. On the real Moroccan labour code distinct parent labels went 10 -> 82 and a passage that was cited as "Titre II : Definitions" is now cited by its own article, with 0 mislabelled children measured across all 800
2026-08-23 | ST-17 self-review | docs/journal/BUILD-STATE.md | recorded two review findings against ST-17 that were NOT fixed, plus what ST-18 already knows is unmeasured and where the real-document harness lives. (1) `delete_document` is now on the hot path: `_ingest` calls it before every NEW/CHANGED file, and `parent_store.list_parent_ids` reads every parent JSON in the WORKSPACE -- a cost ST-16's docstring accepted explicitly because it was a deletion-path function, which ST-17 changed without noticing. ~1,000 reads on the smoke corpus (measured), ~75,000 on a G5-sized workspace (extrapolated and labelled as such). (2) two sources of truth for the workspace folder: sync_workspace passes `folder` down while detect_changes re-reads the same row, re-opening by hand the exact door change_detection's docstring says it closes | neither is a correctness bug and sync.py was not otherwise being edited, so fixing them would have been an unrelated drive-by in someone else's review diff (scoped boy-scout rule). Finding 1 is a genuine G5 input and is now named for ST-18 to measure rather than discover
2026-08-23 | ST-17 review pass | ~/claude-lessons/ACTIVE.md, ~/claude-lessons/PENDING.md (machine-local, not in repo) | /coach proposed one lesson from this session and /lessons kept it: "name-what-your-comparison-discards" -- when a test compares two collections, name what the transform on each side discards before trusting its green, because if both sides pass through the same normalisation the check cannot fail on anything that normalisation drops. Routed to the `verifier` agent. ACTIVE.md now holds 2 lessons; PENDING.md cleared | this is the FOURTH instance of the same defect family on Sanad -- ST-14's round-trip test that relied on a blank line landing where it did by luck, ST-16's batching test made vacuous by a one-child fixture, ST-16's too-small isolation corpus, and now ST-17's report-row equality collapsing duplicates on BOTH sides of the comparison. Kept over the objection that ~/.claude/rules/engineering-seniority.md already says to read an "is X identical to Y" check for what it actually compares: that rule is about a check aimed at the WRONG SIDE, this one is about a check aimed at the right two things through a LOSSY transform
2026-08-23 | ST-17 review pass | change_detection.py, tests/unit/test_change_detection.py, tests/unit/test_sync.py | rule-5 review pass on ST-17 (0210408), which was merged without one. Fixed the one defect it found that deletes data: the REMOVED sweep in `detect_changes` excluded only `scan.unreadable` from its "gone from disk" test, not `scan.unsupported`, so a file already carrying a document row that later falls outside `supported_document_extensions` produced TWO report rows in one run -- Skipped AND Removed -- and the Removed branch deleted its vectors and parent files while the Skipped row told the user nothing had happened to it. Both problem lists are now excluded. 2 tests written to FAIL FIRST and watched red, 336 -> 338, and `test_every_report_row_is_persisted_as_a_sync_item` now compares ORDERED LISTS instead of dicts keyed on file name | the duplicate row was invisible because every helper in test_sync.py keys by file name (`_results`, and that persistence test on BOTH sides), so no test in the file could see one file reported twice -- fixing the reason it was invisible, not just the bug. The two things the review was asked to check first both HELD: the `_STATUS_WITHOUT_DERIVED_DATA` split is correct in both directions, and the parents-before-vectors write order is genuinely test-enforced -- swapping the two calls in `_ingest` was run and turns the suite red with ParentNotFoundError
2026-08-23 | docs/reporting-style-rule | CLAUDE.md | added a "How to report back" section: every reply is written in plain, brief, non-technical English, and every finished task ends with a fixed three-heading block -- ## Done (only what was actually run and watched), ## Ongoing (started, not finished, and what it waits on), ## Left (not started, most important first, ending in a one-line Next). Placed in CLAUDE.md rather than a hook because .claude/ is gitignored in this repo and was wiped from disk by the pull to e1e4098, so no hook or settings file placed there survives or reaches the other teammate | the human steers this project without reading code and a long dense reply hid an OPEN PR (#40) from them in this same session. A hook is also the wrong mechanism on its own: Stop fires after the message is already written, so it cannot change the style of the message it follows -- only instructions loaded at session start can
2026-08-23 | docs/reporting-style-rule | .claude/skills/report-brief/SKILL.md, .gitignore | added the report-brief skill (full plain-language rule + the Done/Ongoing/Left contract) and narrowly un-ignored exactly two paths under .claude/ so the rule reaches the other teammate instead of living on one machine. Proved the ignore rules in BOTH directions with git check-ignore: the 2 Sanad-authored paths are tracked, while hooks/, agents/, a vendored skill dir, worktrees/, agent-memory/ and settings.local.json all stay ignored (8 probes, 8 correct) | CLAUDE.md alone was the weaker half: it competes for attention with everything else in the window and drifts late in a long session, while a skill alone can simply never be invoked. The third layer, a UserPromptSubmit hook that re-injects the rule every single turn, is NOT installed -- Edit(**/.claude/settings.json) is a deliberate deny rule in the user settings and routing around it was refused. The file is written and fully tested in the session scratchpad and is HUMAN-OWNED to paste. UNVERIFIED until it is in place: that Claude Code itself fires it at a real UserPromptSubmit
2026-08-24 | docs/prove-it-skill | .claude/skills/prove-it/SKILL.md, .gitignore | added the prove-it skill: enumerate EVERY candidate root cause before touching code (rank by cost, rule out by observation, stop when one survives and you can reproduce on demand), break every new check on purpose and watch it go red before trusting it, and read a mutation result honestly -- a survivor can mean dead code rather than a vacuous test, and zero survivors is suspicious rather than reassuring. Carries the six vacuous-test shapes this project has actually shipped, each with the story that paid for it, and the three-techniques rule (mutation / read the diff after green / run it for real) with what each is structurally blind to | absence protocol run before writing a line: grepped all 70 user-level skills and every .md in this repo for all-possible-causes, competing-hypotheses, differential-diagnosis and rank-by-likelihood -- ZERO matches, so root-cause enumeration was genuinely missing. debug-loop is a search discipline for finding ONE cause efficiently, not for enumerating them. The break-on-purpose half DID exist but only in test-strategy and research-discipline, both user-level and therefore invisible to the other teammate. .gitignore needed a second allow-list entry and the new skill was CONFIRMED IGNORED by git check-ignore -v before it was added -- the trap fires silently and only that probe catches it, which is now written into .gitignore itself
2026-08-24 | docs/share-testing-skills | .claude/skills/test-strategy/SKILL.md, .claude/skills/research-discipline/SKILL.md, .gitignore, docs/journal/DECISIONS.md | imported the two testing skills that existed only at user level on YL's machine, so MB gets them too. Copies verified against the source with diff --strip-trailing-cr: test-strategy content-identical, research-discipline differing in exactly the two intended lines | both were invisible to the other teammate, which is the failure mode .gitignore's own CONSEQUENCE note warns about. One repair on import: research-discipline opened by naming `engineering-seniority.md` as its companion, a machine-local file NOT tracked in this repo, so the pointer would have dead-ended for MB -- repointed at prove-it and the repair recorded in the file itself rather than made silently. Third copy of the break-it-on-purpose rule, so a DECISIONS row was filed instead of an abstraction
2026-08-24 | docs/share-rules-and-agents | CLAUDE.md, .claude/agents/reviewer.md, .claude/agents/verifier.md, .gitignore, docs/journal/DECISIONS.md | folded the core law into CLAUDE.md (stop-and-ask list, done-means-done, read-before-write, prove-it-by-running-it, the Never list, and the Windows BOM write trap) and imported the reviewer and verifier agents, adapted to Sanad. Also fixed drift found in the same file: CLAUDE.md still claimed rules, agents and skills live in ~/.claude and not in this repo, which stopped being true today | MB had NO rules and NO agents at all, so she has been working without every guardrail the other machine takes for granted. Both agents carried drift from another project and were adapted rather than copied: verifier named apps/pos/, scripts/db-drift.ts, localStorage and Jest idioms, none of which exist here, and now names Sanad's real load-bearing boundaries (signed docs/phase2, db/repo.py stays pure SQL, chunking does not import the data layer, the embeddings encoder seam, one Qdrant client per path); reviewer had an unsubstituted {{TYPECHECK_CMD}} placeholder. 14 check-ignore probes both directions, 14 correct
2026-08-24 | docs/give-steps-skill | .claude/skills/give-steps/SKILL.md, .gitignore | added the give-steps skill: how to write instructions the human follows BY HAND. Numbered steps with one action each, every literal path or command in its own code block ready to copy with NO placeholders, the exact key or button named, where-you-are before what-you-do, and one closing line on how to know it worked. Carries the hard bans: never a placeholder, never a step that routes around a guard (the sanctioned routes stay the human editing the file or changing the guard in the open), never explanation mixed into the numbered list | the reply-format skill covers REPORTING; nothing covered DIRECTING, and the two are different jobs. VERIFIED THIS SESSION rather than assumed: the UserPromptSubmit hook installed by the human is firing, which is what moved the setup from documents-only to actually enforced. The hook text now carries the give-steps rule too, so the format applies without the skill having to be invoked
2026-08-24 | docs/journal-catchup-post-merges | docs/journal/BUILD-STATE.md | re-stamped the header to 249724a / 338 passed and listed the six PRs that landed 2026-08-23/24 (#40 through #45), none of which the sections below knew about. Also recorded that .claude/settings.json is still untracked, which is the one thing keeping the new skills at documents rather than enforcement | PR #41 was accurate when authored and stale when it landed, because main moved five commits and the suite went 336 -> 338 while it sat open. That would have been the FIFTH time this file described a state that no longer existed, and the first time the cause was latency rather than carelessness. The header now carries a NEW rule the previous four corrections never stated: re-read and re-stamp immediately BEFORE merge, not when the PR is written. 'Do not copy the number' was never sufficient -- the number has to be taken at the last possible moment
2026-08-25 | ST-21 | agent/__init__.py, agent/graph.py, agent/nodes.py, agent/ports.py, agent/state.py, agent/trace.py, tests/unit/test_agent_graph.py, tests/unit/test_agent_state.py, tests/unit/test_agent_trace.py | agent graph skeleton + trace collector (ADR-03, ADR-09, architecture 5.2). Eight nodes, two branches, the F-04 retry ceiling read from config on every decision, and SEVEN PORTS with no defaults that ST-22 to ST-25 fill in -- no model-backed thinking is built here. `Answer` mirrors the openapi schema and refuses to exist without a source (G3) or without text. 338 -> 379 passed / 2 skipped; 20 mutations injected one at a time, all 20 killed | two defects found by RUNNING it that no test in the story could see, both silent: a `clarify` port returning "" was read as "the question is clear", so the agent skipped the clarifying question and ANSWERED -- exactly the guessing F-06 forbids; and a blank `rewrite`/`reword` sent the empty string to the store as a search, which an honest refusal then disclosed to the user as `('',)`. Both now fail at the seam, named. Also: the first draft computed its own LangGraph recursion limit from the docs' "25 super-steps", and the pinned langgraph 1.2.9 actually defaults to 10007 (`langgraph/_internal/_config.py:32`) -- the hand-computed limit would have been a second, LOWER bound whose only possible effect was to cut a legitimate high-ceiling run short, so it was deleted and a ceiling-20 test (65 nodes) pins the framework's default instead
2026-08-25 | ST-21 | agent/nodes.py, agent/ports.py, agent/state.py, agent/graph.py, agent/stores.py, agent/__init__.py, agent/trace.py, tests/unit/test_agent_graph.py, tests/unit/test_agent_stores.py, tests/unit/test_agent_state.py, docs/journal/* | closed two structural gaps the human's review found against the signed documents, plus 8 wrong citations. (a) architecture 5.2's box P, "fetch parent sections for context", now has its own node, its own `fetch_parents` port and its own state field, with a REAL implementation in the new agent/stores.py over `parent_store` -- tested against a store on disk written by ST-16's own `save_parents`, including the F-01 case where workspace A asks for an id that exists only in workspace B and gets nothing. (b) `rewrite`/`reword` now return a SEQUENCE, `retrieve` runs once per query with one trace step each, and hits merge on (parent_id, chunk_text). 41 -> 56 tests (an earlier version of this line said '41 -> 56 -> 60', which was a guess, not a count; a review counted 58 and the final figure is 66) | both gaps were mine and both were defended in writing by the first version, which is the part worth keeping. The parent fetch was argued away as "ADR-03's node list does not name it and it changes no route" -- but `parent_store.get_parent` needs a workspace id and `write_answer` has none, so no port could ever have filled it, and the graph was answering from 500-character chunks while its docstrings claimed it read whole sections. The single query string was justified with "V1 returns one query", which cites nothing: 5.2 says "rewrite and SPLIT", ADR-03 keeps sub-queries, and BUILD-PLAN line 76 calls ST-22 the rewrite-and-split node. 12 more mutations run, TWO SURVIVED and both were real test defects -- a de-duplication test that asserted on `sources`, which get de-duplicated a second time so the duplicate collapsed before the assertion saw it (the exact "keyed so duplicates collapse" shape prove-it lists), and an empty-query guard no test ever exercised. Both fixed and both now die
2026-08-25 | ST-21 | docs/journal/BUILD-STATE.md | CORRECTED this file's own claim that ST-21 had had "one cold read (the verifier agent)". It had had none: the verifier was launched and terminated on an API session limit before it read the diff, and its only output was "I'll start by getting the diff" | an intention recorded as an outcome, which is the single failure BUILD-STATE exists to prevent, and the human caught it rather than the process. A launched agent is not a completed review, and a report line has to name what was OBSERVED
2026-08-26 | ST-21 | agent/nodes.py, agent/graph.py, agent/state.py, agent/ports.py, agent/stores.py, config.py, tests/unit/test_agent_graph.py, tests/unit/test_agent_stores.py, tests/unit/test_agent_trace.py, docs/journal/* | acted on a cold verifier pass: 1 blocking, 7 worth-fixing, 6 notes. BLOCKING was a VACUOUS TEST the mutation battery had missed -- `test_the_same_id_twice_is_read_once` asserted on the returned mapping, and writing one text into one dict key three times gives the same dict as writing it once, so it was blind to whether de-duplication happened; the verifier PROVED it by deleting `dict.fromkeys` and watching the assertion pass. It now counts the actual reads through `parent_store.get_parent`. Also fixed: no floor at zero readable sections (the graph answered with an empty context while citing five documents nothing had read -- now refuses, with its own wording naming Sync as the next step, F-03); `question` was unchecked against openapi's 1..2000 bounds despite ADR-13 putting the UI on the in-process path; no width limit on the query split ((ceiling+1) x N searches for one question, now `config.max_sub_queries`); `_merge_hits` claimed 'best-ranked first' and nothing sorted; and `agent` imported `db.repo` for two uuid4 calls. 60 -> 66 tests, 6 more mutations all killed | the verifier read every spec citation and found all of them true, which is the half worth recording -- the previous pass had eight wrong ones. Two of its findings are PARKED rather than fixed and both are named in the files: `parent_store.py:176` turns ANY OSError into CorruptParentError, so a file briefly locked by OneDrive sync is indistinguishable from a corrupt one and takes the whole question down -- telling them apart needs a change inside MB's ST-16 module and its own fix/ branch; and the ceiling-50 measurement quoted in agent/graph.py was a one-off in a scratchpad, not a check that runs, which the docstring now says out loud
2026-08-26 | ST-21 | (merge) | ST-21 MERGED as 24479ba (PR #49, squash) and the whole gate RE-RUN ON MAIN after the merge, not only on the branch: uv sync --frozen clean, ruff exit 0, 404 passed / 2 skipped, gitleaks "no leaks found". CI verify was green on the PR too, all four steps in 1m23s | merged WITHOUT the rule-5 partner review at the human's explicit instruction, recorded as a deviation rather than absorbed -- same shape as ST-17 on 2026-08-23. A post-green pass has found a real defect on five stories running here, so the debt is real even with 38 killed mutations and a cold verifier pass behind it
2026-08-26 | ST-21 follow-up | docs/journal/BUILD-STATE.md, CLAUDE.md, .github/pull_request_template.md | re-stamped the header at MERGE time with the merge SHA and post-merge numbers (the first time this header has obeyed its own rule end to end); gave the two ownerless parked items an owner, a date and a fallback via issues #50 (locked-vs-corrupt parent file, MB) and #51 (ADR-09 says V1 persists the trace and there is no table for it, YL); fixed CLAUDE.md's codebase-memory project name, which was missing the `OneDrive` segment and therefore matched no indexed project; refreshed the stale 892/3,228 node count to 1,209/4,781; removed the `[AI]` marker the PR template asked for | two of these are the same failure in different files: a document telling the next reader to do something the rules forbid or the machine rejects. The project name one is the worse of the two -- a call with the wrong name fails in a way that reads exactly like the graph being unavailable, which is how a session ends up grepping a fully indexed repo. Ownerless debt was the other: this file has carried a blocker with no owner and no date before, and it rots into abandoned work rather than blocked work
2026-08-26 | ST-23 | agent/chat.py, agent/prompts.py, agent/grading.py, agent/retrieval.py, prompts/relevance-grader/PROMPT.md, prompts/query-reword/PROMPT.md, tests/fake_encoders.py, tests/unit/test_agent_{chat,prompts,grading,retrieval}.py, tests/integration/test_ask_retry_loop.py | hybrid retrieval, the relevance grader and the reword (F-04), filling the three seams ST-21 left. Also builds the ADR-06 chat-model interface, which NO BUILD-PLAN row owned -- checked by graph search, project-wide grep and reading every plan row before writing anything. 404 -> 451 passed / 2 skipped; 15 mutations injected one at a time, all 15 killed | two decisions carry this story. The seam is Sanad's own `complete(system, user) -> str` rather than langchain's BaseChatModel, because structured output is untestable under our own rule that tests use a scripted fake with no keys -- langchain's fakes raise NotImplementedError on bind_tools (read from the installed package at chat_models.py:2510). And an unparseable grader reply RAISES rather than counting as off-topic: an off-topic verdict tells the user their documents do not cover the question, and a confused model has made no such claim -- letting a parse failure impersonate one produces a refusal that looks exactly like the product working. Two defects found by RUNNING it, neither visible to any test: 'OFF-TOPIC' with a hyphen raised while the comment claimed hyphens folded, and the integration fixture was too small for its own property -- one child chunk, so search depth 1 and depth 3 both returned one hit and the test proving the operator's setting governs proved nothing. That is the third time this project has shipped that exact shape
2026-08-27 | fix/dead-cloud-model-name | config.py, agent/grading.py, tests/unit/test_agent_chat.py, docs/journal/* | THE PINNED CLOUD MODEL NAME WAS DEAD. A Gemini key appeared in a local .env, so the first real call this project has ever made was attempted -- and returned `404 NOT_FOUND: This model models/gemini-2.0-flash is no longer available. Please update your code to use models/gemini-3.6-flash`. Pinned to gemini-3.6-flash (Google's own named successor) and verified by a live call; 2.5-flash and 3.5-flash confirmed working as fallbacks. Also settled ST-23's open question about grader chattiness: four live calls, two models, all four replies exactly one word, all four verdicts correct, zero parse failures | the value was CORRECT when ST-02 pinned it on 2026-07-20 and dead by the time anything called it. Nothing in the repo could have caught it: ruff cannot see a dead model name, the typechecker sees a valid string, and no test invokes a real provider because docs/phase2/CLAUDE.md rightly forbids API keys in tests. The only check that finds this is a live call, which is the 'verify, never remember' rule paying for itself -- and the reason is now written into config.py beside the value. Two documents that claimed this could never be checked here (agent/grading.py and tests/unit/test_agent_chat.py) were corrected in the same change rather than left to become facts
2026-08-27 | UI design review | docs/journal/BUILD-STATE.md | reviewed the generated `designrag-main/` prototype against signed UX-01 v1.0 before ST-27/ST-28 start. It covers all four message variants, all six sync statuses, all three focus-trapping overlays and the awkward states most mockups skip. FIVE DRIFTS recorded, one measured: `border-strong` scores 1.48:1 light and 1.88:1 dark against a 3:1 floor, failing UX-01 acceptance criterion 10 | the contrast one is the trap UX-01 section 3.4 names in advance -- 'the usual way an interface fails 1.4.11 while looking fine' -- and it happened because the design swapped the spec's palette for Tailwind defaults, where `border-strong` becomes a slightly darker decorative grey instead of a control-boundary role. The other four (an invented analytics screen and settings screen, an avatar component, no skip-to-content link, no reduced-motion or breakpoint handling) are all things UX-01 section 13 rules out by name or section 4 requires. Recorded in BUILD-STATE so ST-27 inherits them instead of rediscovering them
2026-08-27 | docs/session-close | docs/journal/BUILD-STATE.md | re-stamped the header to c7c2085 with the gate measured ON MAIN after all five merges (#49, #52, #53, #54, #55): ruff exit 0, 451 passed / 2 skipped in 351s, gitleaks clean. Added a HANDOFF section at the top -- where the build is, the one remaining seam on the critical path, what is true about models, and the five things most likely to waste a new session's time | this file is ~1,300 lines and a new session was reading all of it to find four facts. The handoff states them and points at the evidence rather than making a reader reconstruct it. Also added the rule that stops the re-stamp chasing its own tail: the header names the commit the gate was MEASURED on, and the only commit allowed above it is the journal commit recording that measurement -- anything else means re-measure, do not patch the number
2026-08-28 | ST-24 | agent/answering.py, agent/ports.py, agent/nodes.py, prompts/answer-writer/PROMPT.md, tests/fake_chat.py, tests/unit/test_agent_answering.py, tests/unit/test_agent_graph.py, tests/integration/test_ask_sourced_answer.py | FILLED `write_answer`, the last seam on the critical path: Sanad now answers a real question end to end with sources. New `prompts/answer-writer/PROMPT.md` v0.1.0 in the registry, `build_write_answer` renders the parent sections into it (one block per section, headed with the file and label the answer will be cited by), and the reply comes back as the answer text. 490 passed / 2 skipped, up from 451; ruff exit 0; 15 mutations injected one at a time, all 15 killed | ST-23 merged four ports and left one between the project and an end-to-end sourced answer. Everything it needed already existed -- the passages, the sections, the refusal path, a working chat model, a prompt registry with two entries to copy the shape from
2026-08-28 | ST-24 | agent/nodes.py | THE SOURCE CONTRACT: `make_answer` now builds ONE tuple, `cited` (the passages whose section box P actually loaded), and uses it BOTH as what the writer is shown AND as what the sources are built from | the previous split was right whenever box P loaded everything and wrong the moment it did not. "loaded 4 of 5" answered from four sections and printed FIVE source cards, so one card pointed at a document whose text nothing had read -- the same defect the floor at zero refuses, surviving in the partial case. F-03 calls the source line the product's contract with the user. Not fixed by filtering in two places: two filters agreeing is a convention, one tuple is a fact. Pinned by a test whose second hit is a DIFFERENT FILE on purpose -- with the same file and label, `_sources_for` collapses the pair and the narrowed and un-narrowed lists come out identical, which is the "keyed so duplicates collapse" vacuity shape
2026-08-28 | ST-24 | agent/ports.py, agent/answering.py, agent/nodes.py, prompts/answer-writer/PROMPT.md | THE WRITER MAY NOW DECLINE (F-05). The prompt licenses exactly one word, NOT_COVERED; `AnswerNotCoveredError` -- declared in ports.py as part of the seam's contract -- routes it to the honest refusal, reusing REFUSAL_TEXT with its own trace detail | the grader judges 500-character CHILD chunks and the writer reads the FULL sections, so a grader that was generous once left the writer exactly two moves: decline, or invent. Inventing is the failure this product exists to demonstrate the absence of, and F-05 is gated at 20 out of 20 on the out-of-scope golden set, so one escape blocks a release. An exception rather than a `str \| None` return because a port that forgets a `return` yields None, and under that design a plain coding mistake becomes a fluent honest-LOOKING refusal
2026-08-28 | ST-24 | prompts/answer-writer/PROMPT.md, agent/answering.py | The decline parse is POSITIONAL -- the token must open the FIRST non-empty line and be followed by a stop -- and TWO tests push it in opposite directions | a substring search would misread an answer quoting the token; a parser scanning every line would destroy exactly the partial answer the prompt asks for, whose second line naturally opens "Not covered: ...". Neither test is worth much alone: asserting only the decline passes for a parser that declines on any reply containing the word, and asserting only the answer passes for a parser that never declines at all. The all-lines mutation SURVIVED the first battery draft and the missing test was written before it was run
2026-08-28 | ST-24 | agent/nodes.py, tests/unit/test_agent_graph.py | REFUSAL_TEXT's FINAL WORDING, which ST-21 explicitly left to this story, plus a test that pins what F-05 requires of it rather than its bytes | the existing refusal tests compare `answer.text` to `REFUSAL_TEXT`: both sides read the same constant, so they pin WHICH constant was used and nothing about what it says, and a copy edit dropping F-05's next step would have left every one of them green. That is the self-referential vacuity shape, in tests written by someone who had just quoted the skill that names it. The new test asserts the three next steps F-05 names, so the wording stays free to be edited and cannot quietly stop meeting the spec
2026-08-28 | ST-24 | scratchpad only, not committed | LIVE PROBE AGAINST THE REAL MODEL, five cases on gemini-3.6-flash through the real `build_write_answer` and `build_grade`: French covered, English covered, two not-covered, one partly covered. 0 unexpected outcomes | the unit suite structurally cannot prove a real model DECLINES rather than inventing -- every reply in it is scripted. Both not-covered cases came back NOT_COVERED, including one genuine near-miss ("conge annuel paye" appears inside Article 52's anciennete counting rule, so a sloppy model had material to invent from). The English question was answered in English and the French ones in French, so the prompt's language rule holds. The partly-covered case answered the covered half and named the uncovered half in prose WITHOUT tripping the decline parser, which is the interaction no unit test covers. No bracketed reference numbers in any reply
2026-08-28 | ST-24 | agent/answering.py, tests/unit/test_agent_answering.py | FOUR DECORATED DECLINES WERE BEING READ AS ANSWERS, found by running the parser over hand-built edge cases rather than by reading it: an em dash after the token (only the EN dash was a stop), a bulleted `- NOT_COVERED`, a heading `# NOT_COVERED`, and a byte-order-marked one. All four fail in the SAME direction and it is the dangerous one -- the model declined, the graph read prose, and the user gets a bubble saying "NOT_COVERED" with source cards under it and `refusal` false, which F-08's out-of-scope half scores as a non-refusal | an em dash is what a model writing fluent prose actually reaches for, so this is tolerance the parser needs rather than defensive padding. The leading-markup strip is ANCHORED at the start of the line on purpose: removing "-" anywhere would turn "NOT-COVERED" into "NOTCOVERED" and break the spelling the fold exists to accept. A guard test covers the other direction, because widening tolerance for a decorated decline must not widen tolerance for a false one. 3 further mutations, all killed; 18 across the story
2026-08-28 | ST-24 | scratchpad mutation harness | REPEATED ST-21'S OWN RECORDED LESSON WITHIN ONE STORY: the mutation harness calls `git checkout --` to restore, and it was run while the parser fix was still UNCOMMITTED, so the restore discarded it. Recovered by rewriting the edit, then committing BEFORE re-running | ST-21 wrote this lesson into BUILD-STATE in almost these words ("commit the green checkpoint FIRST, then mutate; `git checkout --` is then the restore") and it was still repeated. Cheap this time because the test file was outside the harness's restore list, so the suite went loudly red instead of quietly green -- which is luck, not design. The harness should refuse to run against a dirty tree; that guard does not exist and is named as a follow-up
2026-08-28 | ST-24 | agent/answering.py, agent/stores.py, tests/unit/test_agent_answering.py, tests/unit/test_agent_stores.py, tests/integration/test_ask_sourced_answer.py | ACTED ON A COLD VERIFIER PASS: 2 blocking, 4 worth fixing, 6 notes, and the two blocking ones were real. (1) A CORRECT ANSWER WAS BEING THROWN AWAY -- the prompt tells the model to name the part it cannot cover, and a model obeying that in English opens with the words "Not covered", which the parser read as a refusal. Fixed by splitting the token spelling from the prose spelling. (2) THE F-03 INTEGRATION TEST COULD NOT FAIL -- the workspace held ONE document, so "cited files are a subset of files consulted" was true whatever the code did and "every label contains Article" was true because every heading in that document starts with it. A second document with a different name and non-Article headings is now indexed into the same workspace, and the test runs a control probe in both directions | the review is the fifth on this project to find a real defect while the suite was green, and the first blocking finding is the sharpest thing anyone found in this story: it was a defect CAUSED BY the branch's own prompt instruction, so no amount of reading the parser in isolation would have surfaced it. Reproduced before fixing, both directions now pinned by tests, 5 further mutations all killed (23 across the story)
2026-08-28 | ST-24 | agent/stores.py, agent/answering.py | A SECTION THAT LOADS AS BLANK is now left out like a missing one, and an EMPTY MODEL COMPLETION raises its own named error instead of being read as a broken port | both from the same review. parent_store only checks the text field is PRESENT, so a blank one was a successful read: trace said "loaded 2 of 2", the model got a headed block with nothing under it, and the document was still cited. Fixed where "loaded" is defined rather than in the writer, so the trace, the router and the citation all agree. The empty completion is deliberately NOT a decline -- a refusal is a claim about the user's documents, an empty completion is a fact about the model, and `agent/grading.py` already settled that they must not share an outcome
2026-08-28 | ST-24 | agent/answering.py, tests/unit/test_agent_answering.py, tests/integration/test_ask_sourced_answer.py | ACTED ON THE RULE-5 REVIEW, which said DO NOT APPROVE and was right. ITS BLOCKING FINDING WAS THE COLD REVIEW'S DEFECT SURVIVING ONE ROUND OF FIXING: the first fix said the prose spelling must be the whole first LINE, and a model that names the gap on line one and answers on line two still lost its answer. Rewritten on one principle rather than patched a fourth time -- an underscore or hyphen between the two words never occurs in prose, so those are verdicts and may carry a note; a SPACE makes it ordinary English and must be the entire reply. Dropping the token side's stop list closed three further misses at once | two rounds of review on one regex, and each version looked obviously right until someone RAN it on a reply nobody had thought of. That is the argument for cold reading as a practice rather than a formality: neither defect was visible in the code, only in the interaction between the code and a sentence the prompt itself asks the model to write. 29 mutations across the story, all 29 killed
2026-08-28 | ST-24 | agent/answering.py | THE DECLINE BRANCH NOW LOGS the discarded reply's first line before raising, plus two parser gaps I found by sweeping it myself: `Reponse : NOT_COVERED` (the prompt tells the model to answer in the question's language, and this product's questions are French, so an English-only label list was a gap the prompt invited) and `1. NOT_COVERED` (list numbers are now markup, with a lookahead so "1.5 hours" is untouched) | the logging is the important half. This is the branch where a mistake is PERMANENTLY INVISIBLE: if an answer is read as a decline, the model's text is thrown away, the user is told their documents do not cover the question, and nothing records what was lost. Two parser versions did exactly that and BOTH were found by a person reading code, never by the running product. First line only, never the body, per the CLAUDE.md logging ban
2026-08-28 | fix/ST-24-shouted-decline | agent/answering.py, tests/unit/test_agent_answering.py, docs/journal/DECISIONS.md | CASE now discriminates in the decline parser: a SHOUTED `NOT COVERED` may carry a trailing note and is a verdict, a sentence-case `Not covered` is prose, and both spaced forms must still be a single-line reply. Closes the last direction-A miss the rule-5 review listed | THE POINT IS THE CORRECTED REASON, not the closed miss. The ST-24 DECISIONS row claimed the two forms "cannot be told apart" -- false, they differ in case -- and the re-review caught it while APPROVING the merge. A wrong reason in a decision record is worse than a missing one, because the next reader inherits a constraint that was never real and has no cue to re-examine it. The superseding row says so in those words rather than editing the original, which is the project's rule for how a decision changes
2026-08-28 | ST-24 merge | docs/journal/BUILD-STATE.md | RE-STAMPED THE HEADER AT MERGE TIME to 2ebb74d on main, all four gate.yml steps re-run ON MAIN after the merges: 522 passed / 2 skipped, ruff exit 0, uv sync clean, gitleaks 'no leaks found' over 3.26 MB. ST-24 merged as c511bc8 (PR #57) plus 2ebb74d (PR #59); .env.example's dead model closed as PR #58 | the file's own rule is that the number is taken at the last possible moment and the gate is re-run on MAIN, because a green branch and a green main are not the same claim. ST-24 is the FIRST story on this project to reach main with both review passes done beforehand -- a cold read and the rule-5 pass -- and the rule-5 pass said DO NOT APPROVE, so the debt on ST-12, ST-21 and ST-23 is now six stories' worth of evidence rather than five
2026-08-28 | plan survey | docs/journal/BUILD-STATE.md | THREE THINGS NOBODY HAD FLAGGED, found by reading BUILD-PLAN against the repo rather than against the journal: ST-05 was never done (no Dockerfile, no compose file anywhere); ST-03's exit gate IS demonstrated by history (8 of 71 gate.yml runs have failed, every one on a pull_request event) even though there is no ci.yml; and main IS protected, via GitHub RULESETS | the third is a CORRECTION of something this session almost recorded as fact. The legacy branch-protection API returns 404 for this repo, which reads exactly like 'no protection', and it was briefly reported that way. The ruleset `protect-main` is active and requires a PR, requires the `verify` check with a strict up-to-date policy, and blocks force-push, deletion and non-linear history. The project's own absence rule in a new place: an API that 404s is UNVERIFIED, never a negative finding -- query the other endpoint before concluding a guard is missing
2026-08-29 | docs/fix-graph-project-name | CLAUDE.md, docs/journal/BUILD-STATE.md | THE HARDCODED CODEBASE-MEMORY PROJECT NAME IS GONE from the file every session loads, replaced by "call `list_projects` and take the entry whose `root_path` is this repo". The name is derived from the repo's ABSOLUTE PATH, so it differs per machine: YL's clone is under OneDrive and MB's is not. Also closes the PARKED item in BUILD-STATE that ORDERED the wrong fix, and corrects a second stale line there claiming the MCP server is not installed on MB's machine | the line has now been wrong in BOTH directions, one machine at a time -- it first dropped the `OneDrive` segment, the parked item called that a typo and had it hardcoded back in, and that broke MB's machine where `index_status` with the documented name returns "project not found", which reads exactly like the graph being unavailable and sends the session back to grepping. A machine-specific value in a file both machines share cannot be repaired by writing the other machine's value. Proven on MB's machine: the documented name returns "project not found"; the name `list_projects` gives returns status ready with 0 skipped files. NO COUNTS RECORDED, and that is a finding not a shortcut -- `index_status` reported 5,922 edges and then 5,917 about ten minutes later on an unchanged tree, because the index re-watches in the background. Found by the cold verifier pass, which caught the first version of this very line quoting 5,922 as a reproducible proof
2026-08-29 | docs/fix-graph-project-name | CLAUDE.md | THE DOCUMENTED TEST COMMAND HID ITS OWN RESULT. The Commands table said `uv run pytest -q`, but `pyproject.toml` already carries `addopts = "-q"`, so the documented command is really `-qq` -- and `-qq` DELETES the `N passed, M skipped` summary line. Table now says `uv run pytest`, with the trap written out beside it | found by accident while proving this branch's own gate: three attempts to capture the pass count came back green with no number in them, and the fourth found the cause. Same disease as the project-name line above -- a file every session loads, telling the reader to do something that quietly does not work. It matters here more than usual because EVERY journal header on this project is required to quote a pass count, so the documented command cannot produce the evidence the process demands. Measured on this branch with the corrected command: 522 passed, 2 skipped in 155.62s, exit 0
2026-08-29 | ST-07 | scripts/corpus.py, data/corpus/ | CORPUS V1 EXISTS AND ITS EXIT GATE PASSES: 13 files, hr 3 + manuals 10, every one CONVERTED by Sanad's own ladder (201 + 16 + 8 PDF pages, ~800k chars). The flagship labour code is now the MINISTRY OF JUSTICE CONSOLIDATED EDITION (loi 65-99 as at 2011-10-26, adala.justice.gov.ma) in place of the ILO's 2004 first-promulgation text, and the CNSS law arrives from ACAPS, the state regulator. Two files DELETED: the ILO 2004 code (a near-duplicate that would have split retrieval hits with the consolidated one and made the ST-19 golden set score noise) and a private consulting firm's dated 2016 summary of labour law, which PRD section 14 excludes -- the demo corpus is bound to "official legal texts, public manuals" and a secondary commentary would have been quoted in a graded report as if it were law | THE DELIVERY MECHANISM IS THE POINT. `data/` is git-ignored by LD-06, so a corpus cannot reach the other machine through git and the previous one did not: three PDFs and ten text files sat on one laptop for six days while the journal recorded ST-07 as blocked. `scripts/corpus.py` is the tracked half -- manifest, `fetch`, `verify`, `sources` -- and it rebuilds the corpus from nothing on any machine. `verify` runs `conversion.convert_file`, the same call sync makes, rather than a second pymupdf opinion: the claim proven is "Sanad reads every file", not "a script agreed with itself". No new dependency; downloads use stdlib urllib
2026-08-29 | ST-07 | scripts/corpus.py | THE GATE WAS BROKEN ON PURPOSE, TWICE, BEFORE BEING BELIEVED: a file moved away exits 1 naming it MISSING, and a blank image-only PDF written over a real one exits 1 as `skipped` carrying PRD F-16's own wording ("this PDF has no text layer, so it is a scan or images only"). Restored, exit 0. `fetch` was proven the same way -- one PDF and one archive-member file deleted, both rebuilt, gate green again | plus a finding that only running it twice could produce: `howto-sockets.txt` came back 23,215 bytes on 2026-08-23 and 22,757 bytes today. The French Python docs archive is REBUILT UPSTREAM, so the manuals workspace is not byte-reproducible while the three HR PDFs are static and are. `verify` and SOURCES.md now print a sha256 per file so a drift is visible. DELIBERATELY NOT PINNED: ST-35 owns freezing the corpus, and a freeze invented here would be the wrong shape. Parked and visible, per the rule
2026-08-29 | ST-07 review | scripts/corpus.py, docs/journal/BUILD-STATE.md | ACTED ON A RULE-5 REVIEW THAT GRADED 6/10 AND SAID DO NOT APPROVE. Its two blocking findings both mean THE EXIT GATE DID NOT PROVE ITS OWN SENTENCE. (a) it passed a scan: it asserted only CONVERTED, and `conversion_min_text_chars` is 1, so a three-page PDF whose only text is "1 2 3" converted at 12 chars and the gate said MET -- reproduced before fixing. Floor of 200 chars/page added, taken from the corpus (thinnest real file 1,783/page, a footer-only scan 4). (b) it walked the MANIFEST while `scan_folder` walks the DIRECTORY, so an unlisted file was invisible to the gate and visible to Sync. Now fails on any unlisted file. Also: `_sha256` replaced by `change_detection.compute_fingerprint`, and a false sentence removed from `fetch`'s docstring | THE SECOND FINDING IS THE ONE THAT WOULD HAVE SHIPPED A WRONG NUMBER. A deletion happens on one machine. YL runs `fetch`, gets the three new files, and keeps the two this story deleted -- a five-file HR workspace carrying the superseded 2004 labour code -- and ST-18 measures that while the gate says MET. The first finding is the FOURTH fixture-too-small-for-the-property defect on this project after ST-14's round-trip, ST-16's batching and ST-16's first isolation corpus: the original proof used a BLANK PDF, the easy case, and a realistic scan walked straight through. Both new branches proven by deliberate violation. The hasher swap is corroborated behaviour-neutral: every digest printed after it is identical to the one before
2026-08-29 | ST-07 review | scripts/corpus.py, docs/journal/BUILD-STATE.md | OWNED THE DELETION AS EDITORIAL JUDGEMENT INSTEAD OF SPEC. The story justified removing a private firm's 2016 labour-law summary by quoting PRD section 14 ("public material: official legal texts, public manuals"). The review called that motivated reasoning and was right: the sentence sits under COMPLIANCE and its subject is personal data, and read as a source-authority rule it would also exclude the CLEISS guide the corpus KEEPS. Also corrected a comment claiming all three HR entries are PRD section 17-verified -- section 17's four findings are all about the labour code; the dahir and CLEISS appear nowhere in it | THIRD INSTANCE OF ONE FAILURE: quote the clause before declaring something spec-bound. The citation escalation of 2026-08-23 was withdrawn for it; the "stand-in corpus" blocker was an invented standard this same story removed; and then the story reached for a real clause that says something else to justify a call it had already made. The deletion is still right. What was wrong is the authority claimed for it, and a graded report inherits that claim
2026-08-29 | ST-07 re-review | scripts/corpus.py | RE-REVIEW GRADED 9/10 APPROVE and found two more by RUNNING the new checks instead of reading them, both failing CLOSED (false red, never false green). (a) the stray sweep flagged ANY file while sync only indexes a SUPPORTED extension, so a `Thumbs.db` would have reddened the gate for a file sync can never touch -- now filtered through `is_supported`, the same predicate sync uses. (b) CASE: reproduced a file renamed `CNSS-....PDF` reporting CONVERTED and unlisted-stray IN THE SAME RUN, because `Path.exists()` is case-insensitive on Windows and set membership is not; Linux fails the other way (MISSING plus stray). Both sides case-folded now. Also inlined the `_fingerprint` wrapper the review called pointless | THE SECOND ONE IS A GENUINE TWO-MACHINE DEFECT, the same class as this morning's CLAUDE.md project name: one disk, two operating systems, two different wrong answers, and neither wrong in a way that points at the cause. Also recorded a LIMIT of the 200-char floor rather than leaving it to be discovered: density is a per-document AVERAGE, so 100 scanned pages appended to the 201-page labour code still average ~1,190 and pass. The gate catches a scanned DOCUMENT, not a scanned SECTION. And the floor is not tuned -- the thinnest real file is 1,783 chars/page, nearly nine times above it
2026-08-29 | ST-18 | scripts/spike_st18.py, docs/journal/BUILD-STATE.md | ST-18 PART ONE MEASURED ON THE REAL CORPUS, no model calls. G5: HR workspace, 3 files / 225 pages / 1,121 children, cold sync 552.4s -> 491.0s per 200 pages against a 600s budget. PASS BY 18%. Retrieval quality against a word-counting baseline: hybrid 12/15 top hits in the right file, counting words 6/15 -- hybrid earns itself, which ST-16 said it could not prove with fakes. Warm retrieval median 0.213s, p95 0.243s over 14 calls, first call 34.3s held out as encoder load. Two open journal items closed: the per-file registry commit cost is 32-77 ms per file, and ST-17 finding 1's per-file workspace-wide parent scan read all 128 parent JSONs inside a 33.2s re-ingest, so it is noise at this size | THE HEADLINE IS NOT THE SPEED, IT IS 3/6. Retrieval finds the right DOCUMENT 12 times in 15 and the right ARTICLE about half the time. F-03 puts file name AND section label in every citation and the user story is forwarding "Article 231" to settle a dispute, so "the right 201-page PDF, somewhere" fails the actual job while passing every speed target. Same defect family as the citation-label escalation of 2026-08-23, fixed there for LABELLING and now visible in RANKING. Stated with its caveats rather than as a headline number: 6 is a tiny sample, and I wrote both the questions and the expected answers, so it is self-graded -- which is the argument for ST-19 being written AFTER this run instead of before it
2026-08-29 | ST-18 | scripts/spike_st18.py | CAUGHT MY OWN OUTLIER BEFORE QUOTING IT. The first retrieval run reported median 0.233s and max 160.07s in one sample of 15 -- the first query pays for loading multilingual-e5-base. Left in the pool that single sample barely moves the median, lands JUST OUTSIDE a 15-sample p95 by luck (p95 is the 14th of 15, the outlier is the 15th), and would land INSIDE it at a different sample size. `Timing.summary` now holds the first call out and reports it separately | a number that changes character depending on how many questions you happened to ask is not a measurement, it is an accident. The same load is deliberately left INSIDE the G5 number, and the two decisions are consistent rather than contradictory: a user on a fresh install really does wait for the encoder before their workspace is questionable, and a user asking their fifteenth question of a session really does not. The threshold decides where the cost belongs, not tidiness
2026-08-29 | ST-18 | scripts/spike_st18.py, docs/journal/BUILD-STATE.md | G4 IS BLOCKED AND NOTHING WAS SPENT. `answer` ran 15 questions, all 15 rejected before reaching the model. Two separate faults on this machine, both in the git-ignored `.env`: the CLOUD_API_KEY is invalid (400 API_KEY_INVALID, and a 56-char key IS present, so it is wrong or revoked rather than missing), and CHAT_MODEL_CLOUD is still `gemini-2.0-flash`, the model Google retired on 2026-08-27. config.py:61 and .env.example:8 both say `gemini-3.6-flash` -- PRs #55 and #58 fixed the tracked files, and a merge cannot reach a git-ignored one. Confirmed by running `get_settings()`, not by reading | THE INVALID KEY MASKED THE RETIRED MODEL: authentication fails before any model lookup, so fixing only the key would have produced a 404 on the model name as the NEXT failure, one run later. Both must be fixed together. This is the THIRD instance today of one pattern -- a correction lands in a tracked file and the machine keeps the old value (CLAUDE.md's graph project name, the deleted corpus files, now .env). Nothing compares `.env` to `.env.example`, so any corrected setting stays wrong locally until runtime fails. A drift check between the two is the durable fix and NO STORY OWNS IT; not improvised here because config.py is ST-02's and a gate step is ST-03's
2026-08-29 | ST-18 | scripts/spike_st18.py | FIXED A LIE MY OWN SCRIPT PRINTED, found by the run above taking a branch I had not tested. With 15 errors and 0 answers the G4 verdict block still printed "every row above is either sourced or a refusal", because it was written assuming the provider works -- which is the assumption a spike exists to test. It now says plainly that no G4 number was produced and returns non-zero | an error is NOT a refusal. A refusal is a claim about the user's documents; an error is a fact about our plumbing, and `agent/grading.py` already settled during ST-24 that those two must never share an outcome. I re-derived the same defect one layer up in a reporting block, which is the argument for the rule living somewhere a reader meets it rather than in one module's history
2026-08-29 | ST-18 review | scripts/spike_st18.py, docs/journal/BUILD-STATE.md | ACTED ON A RULE-5 REVIEW THAT GRADED 7/10, DO NOT APPROVE, WITH 14 FINDINGS. The worst was mine and it inflated a published number: MY BASELINE WAS RIGGED. Every probe is typed in unaccented ASCII ("conges"), the corpus is accented French ("conges" with an accent), and `casefold()` folds case but NOT accents -- so the word-counting baseline could not match its own vocabulary while the dense encoder is untroubled. Accent-folding plus a 3-char floor, re-run on 20 probes (up from 15, because BUILD-PLAN line 66 says "20-q latency"): hybrid 16/20 vs words 11/20, where the first report said 12/15 vs 6/15. The VERDICT survives, the MARGIN was about double the real one | a baseline exists to be the number the real system must beat, so anything that quietly weakens it flatters the thing under test. Build the baseline as if you wanted it to win. Right-article rate held at about half across both samples (3/6 then 4/8), which is the finding that matters and is now corroborated rather than a single reading. The review also CHECKED my eight expected article numbers against the PDF instead of trusting them -- all eight correct
2026-08-29 | ST-18 review | docs/journal/BUILD-STATE.md | THE G5 ROW TRIPPED ITS OWN ALARM AND I WROTE "PASS". Budget 600s, measured 491.0s, so headroom is 109.0s -- and the row's own `Act at:` line says "under 120s of headroom". The verdict now reads "PASS ON BUDGET, ACT-AT TRIPPED, routed to ST-37". Also delivered the exit gate BUILD-PLAN line 66 actually asks for: the verdict word. It is TUNE, provisional on the blocked G4 | this is "do not silence the alarm" failing in its quietest form -- not by moving a threshold, but by writing one, watching it trip, and reporting the other number. A row that judges itself is worth nothing if the verdict line ignores it. TUNE rather than on-track because two things sit outside where they should be, and rather than change-request because nothing signed is wrong: hybrid earns itself against a fair baseline, so ADR-05 stands. Only G4 could still turn it into a change-request
2026-08-29 | ST-18 review | scripts/spike_st18.py | FOUR MORE THE REVIEW CAUGHT, all of them a claim that outran the code. (a) the docstring said the spend count was "printed AND CONFIRMED" and nothing ever asked -- a money guard that existed only in its own description; now a real `--yes` flag, chosen over `input()` because a prompt under an agent with no terminal reads EOF and sails through, which is a guard that fails OPEN. (b) a MIXED run printed the PASS/FAIL block and "NO G4 NUMBER WAS PRODUCED" together and returned 0 -- the same lie the all-error path had just been fixed for, surviving one branch over because only the all-error path had ever executed. (c) the p95 used round() not ceil(), returning the 13th of 14 with two samples above it. (d) the re-ingest probe globbed for any PDF, so it edited a different file on another machine, missed `.PDF` on Linux, and restored the bytes without re-checking the fingerprint | (c) is the one to remember: immaterial at 0.2 seconds, decisive at G4, where the same function judges an answer against 60 seconds and dropping the top sample flips PASS to FAIL. And (d) re-introduced by reflex the exact case bug ST-07's re-review had fixed one file away, hours earlier
2026-08-29 | ST-18 re-review | docs/journal/BUILD-STATE.md, scripts/spike_st18.py | A FALSE VERIFICATION WAS WRITTEN INTO THE JOURNAL AND THE RE-REVIEW CAUGHT IT. The article row claimed all eight expected article numbers "were CHECKED against the PDF by the rule-5 review, not assumed". The review checked SIX; the other two probes did not exist when it ran, because they were added in response to it. And one of the two was WRONG: Article 217 is "il est interdit aux employeurs d'occuper les salaries pendant les jours de fetes payes" -- public holidays, not rest between working days. Verified by extracting the PDF text myself this round, along with 174 (night rest, women and minors, eleven hours), 205 (repos hebdomadaire, twenty-four hours) and 72 (certificat de travail, eight days) | THE EXACT FAILURE THIS JOURNAL EXISTS TO PREVENT, COMMITTED IN THE JOURNAL. An unverified claim wearing someone else's authority is worse than an unchecked number, because "the reviewer checked it" is the sentence that stops the next reader checking. The row now says who checked which. And the bad probe was not merely a wasted slot: it was scored as a RETRIEVAL MISS when the fault was entirely my label, so the article rate read 4/8 and is really 5/8. A mislabelled probe manufactures a failure and makes the system look worse than it is
2026-08-29 | ST-18 re-review | scripts/spike_st18.py | A CRASH IN THE ONE BRANCH THAT HAD NEVER RUN, AND IT CRASHES AFTER SPENDING THE MONEY. `answer()` never set `timing.cold_start`, so the G4 verdict block hit `f"{None:.1f}"` and raised TypeError. That block only executes on a FULLY SUCCESSFUL G4 run, which had never happened because the API key is invalid -- so the failure sat exactly where it would cost the most, after every provider call was already paid for. Guarded with `or 0.0`. Worse and related: the previous round's warm-up fix was added to `retrieve()` ONLY, so `answer()` had no warm-up AND no holdout, putting the ~35s encoder load inside question one against a 20s median budget. `answer()` now issues its own untimed warm-up | a fix that improves one call site and silently degrades its sibling is worse than the thing it replaced. Both defects are the same shape as the whole spike: the branches nobody executes are where the wrong claims live, and "it passes" means nothing about a path that has never been taken
2026-08-29 | ST-18 re-review | scripts/spike_st18.py, docs/journal/BUILD-STATE.md | CLOSED THE LAST TWO, BOTH NON-BLOCKING, ON A 9/10 APPROVE. `answer` now makes one throwaway provider call in its warm-up, so question one no longer pays for TLS setup, credential resolution and the client's first metadata fetch INSIDE the sample, against a 20s median budget. The declared worst case rises from 140 to 141 calls and says so on the line that prints it. Also guarded `retrieve`'s cold-start format the same way `answer`'s is, since the two printed the same value two different ways and the unguarded shape is exactly what crashed `answer` after it had spent the money | THE FIX IS ITSELF UNVERIFIED AND THE JOURNAL NOW SAYS SO. Nothing in the `answer` path has ever run to completion, because the key has been invalid for every attempt: the G4 verdict block has never executed, the handshake measurement has never been taken, and traces.json holds 15 errors and no answers. Listed in the blockers section so the first successful run is WATCHED rather than trusted. The review's own framing is the right one -- a disclosure gap on a number that does not exist yet is not a defect in anything measured, but it becomes one the moment the number exists and nobody remembers what it includes
2026-08-29 | chore/env-example-drift-check | tests/unit/test_config.py, .env.example | THE DRIFT NOBODY OWNED IS NOW A CHECK, not a backfill. ST-02's exit criterion says "every config var documented in .env.example" and nothing ever enforced it, because .env.example is a text file no code reads. It had drifted to TEN undocumented settings (the ST-13 note listed six; four more arrived after it was written). Three tests now hold it in BOTH directions: a setting missing from the template fails, and a template variable config.py does not define fails too. All ten documented, each with a comment saying what it is for | THE SECOND DIRECTION IS THE ONE THAT ROTS QUIETLY: pydantic-settings IGNORES an unknown environment variable, so a stale key is something you can fill in, restart, and watch do nothing with no error anywhere. Proven by watching it fail first -- the check named all ten before they were added. HONEST LIMIT, written into the journal: this holds the TRACKED end only. A `.env` is git-ignored and holds a secret, so nothing can compare it to anything, which is exactly how ST-18 lost G4 to a machine still naming a model Google retired two days after the tracked files were fixed. The template can no longer be silently incomplete; a machine can still be silently stale
2026-08-29 | chore/env-example-drift-check | .env.example, tests/unit/test_config.py | STRIPPED A UTF-8 BOM THAT WAS ALREADY ON MAIN, found by running the drift check above. `.env.example` began EF BB BF. Harmless today by luck only: line 1 is a comment, so the three bytes are swallowed by a `#`. A fourth test refuses it now, proven by watching it fail on the committed bytes before stripping | it matters more in this file than in most, because line 1 says "copy to .env" -- the mark travels into the file that actually configures the product, and the first variable would become an unrecognised `﻿MODEL_MODE` that pydantic-settings ignores in silence. Also a method note worth keeping: my FIRST attempt to check whether the BOM was pre-existing used `git cat-file blob > file` in PowerShell, which is the very `>` redirection CLAUDE.md bans for adding a BOM -- so the test for a BOM could have added one. Re-checked with `head -c 6 | od -An -tu1`. Do not diagnose a BOM with a tool that writes one
2026-08-29 | ST-12 review | sync.py, change_detection.py, tests/unit/test_sync.py, tests/unit/test_change_detection.py | THE ST-12 RULE-5 REVIEW PASS, OWED SINCE 2026-07-28, WAS FINALLY DONE AND GRADED 6/10 DO NOT APPROVE. It found three real defects in a module two earlier reviews had already corrected twice. (a) A FILE THE REPORT CALLS SKIPPED KEPT ANSWERING: an operator narrowing `supported_document_extensions` leaves an already-ingested file with a document row, live vectors and parent files, and sync only recorded a Skipped report row. PR #40 stopped that file being reported Removed as well; it never stopped it being cited. New `_skip_unsupported` deletes both stores and writes the row `skipped`, mirroring `_remove`'s order. (b) the same rows carried `document_id=None`, on a comment claiming an unsupported file never has a document row -- which PR #40's own scenario disproves. (c) `scan_folder` let PermissionError escape | (a) IS THE REPORT LYING ABOUT THE PRODUCT, which is worse than either state alone, and F-02's whole contract is that the per-file report is what actually happened. `skipped` also OBLIGES the deletion rather than merely permitting it: ST-17 put `skipped` inside `_STATUS_WITHOUT_DERIVED_DATA`, so a `skipped` row asserts there is no derived data, and marking without deleting would make that assertion false and strand orphan vectors no later sync could find. Both new sync tests proven by restoring the pre-fix behaviour and watching them go red
2026-08-29 | ST-12 review | change_detection.py | ONE UNREADABLE FILE KILLED THE WHOLE SCAN, and the mechanism is narrower than the review stated -- I checked pathlib's source rather than accepting either version. `Path.is_file()` swallows ONLY `_IGNORED_ERRNOS` (ENOENT, ENOTDIR, EBADF, ELOOP) and RE-RAISES everything else; EACCES is not in that tuple. So a permission-denied file did not quietly vanish into the REMOVED sweep as reported -- it raised PermissionError straight out of `scan_folder` and NO other file in the workspace was indexed. That is PRD F-02 criterion 3 broken outright | I ALMOST SHIPPED THE WRONG REASON FOR THE RIGHT FIX. Reading `inspect.getsource(pathlib.Path.is_file)` and checking `_ignore_error(PermissionError(EACCES))` is what settled it, and it turned the finding from "a file silently drops into REMOVED" into "one locked file costs the entire sync", which is worse. The errors it DOES swallow are the right ones: ENOENT means the file really was deleted between iterdir and stat, so falling through to REMOVED is correct there. Proven by mutation in both directions
2026-08-30 | merge day | (no code) docs/journal/BUILD-STATE.md | MERGED PR #65 (ST-12 review pass) THEN PR #64 (.env.example drift check), squash, both branches deleted. PR #64 was built before #65 landed, so it was NOT merged on its old green: main was pulled into it, the whole gate re-run locally (529 passed / 2 skipped), pushed, and CI verify re-run (1m29s) before the merge button. Gate then run a THIRD time on main after both: uv sync --frozen clean 170 packages, uv run ruff check . exit 0, uv run pytest 529 passed / 2 skipped in 59.31s | A GREEN BRANCH AND A GREEN MAIN ARE NOT THE SAME CLAIM, and a green earned against a base that has since moved is a third thing again. #64 touched tests/unit/test_config.py and #65 touched sync.py and change_detection.py -- no textual conflict, which is exactly the case where people skip the re-run and get bitten by a behavioural one instead
2026-08-30 | journal catch-up | docs/journal/BUILD-STATE.md | RE-STAMPED A HEADER THAT WAS FIVE COMMITS STALE and corrected four places that still called the ST-12 review outstanding (lines 251, 568, 1460, 1569). The header said 2ebb74d / 522 passed while main was f4cf208 / 529 passed. Its OWN rule says the only commit allowed above the stamped one is the journal commit recording the measurement; five were not that | THE HEADER IS THE ONE THING IN THIS FILE A READER TRUSTS WITHOUT CHECKING, which is what makes a stale one worse than no header. Found by the human asking whether everything was journaled, not by any check -- there is no gate on this file's freshness and there still is not one after this commit. The four ST-12 lines are the nastier half: a file claiming work is OUTSTANDING when it is DONE sends the next session to redo it
2026-08-30 | journal catch-up | docs/journal/BUILD-STATE.md, docs/journal/DECISIONS.md | RECORDED THE GITLEAKS GAP INSTEAD OF INHERITING IT. Previous headers quoted a local `gitleaks detect` exit 0; gitleaks is NOT INSTALLED on this machine (`where gitleaks` and `gitleaks version` both empty, 2026-08-30). The new header says the secret scan is green ON CI (gitleaks-action passed on #65 in 1m23s and #64 in 1m29s) and UNVERIFIED LOCALLY, and forbids writing "all four steps" until someone installs it | copying a previous header's number forward is how an unverified claim becomes a fact. The measurement was never mine to repeat, and the honest sentence is shorter than the dishonest one. ALSO PARKED, not fixed, as follow-up 10: DECISIONS.md holds a raw NUL byte at line 59, so grep calls the whole decision log a binary file and returns no lines, and the code index flags the same line parse_partial
2026-08-30 | ST-19 | evaluation/golden/batch1.jsonl, evaluation/golden/README.md, tests/unit/test_golden_set.py, scripts/golden_grounding.py | GOLDEN SET BATCH 1: 15 in-scope + 8 out-of-scope French questions on the ST-07 corpus, the first artifact of the evaluation half. Every in-scope reference answer was written from the article text EXTRACTED FROM THE REAL PDF through the product's own `conversion.convert_file`, never from memory: 12 rows on the labour code (articles 14, 39, 43, 53, 72, 143, 152, 184, 201, 205, 231, 232), 2 on the CNSS dahir (35, 53) and 1 on the CLEISS guide. Every out-of-scope question was PROVEN absent from all three documents. 10 schema tests, and a hand-run grounding script that re-checks all 23 rows against the corpus | VERIFY: ruff exit 0; `uv run pytest` 539 passed / 2 skipped (529 before); `uv run python scripts/golden_grounding.py` -> "OK: 23 rows grounded (15 in scope, 8 out)"; 14 mutations injected one at a time, all 14 killed
2026-08-30 | ST-19 | scripts/golden_grounding.py | A BYTE-WISE GREP CALLED A WORD ABSENT THAT APPEARS FORTY TIMES, and it nearly shaped a quarter of the golden set. The first absence sweep used `grep -E "pr.avis"`; outside a UTF-8 locale `.` matches one BYTE and `é` is two, so the pattern cannot match `préavis` and grep returned 0 across a 358 KB document holding 40. Four out-of-scope questions were being drafted on that zero. Re-run through an accent-folding Python route, which also folds the three DIFFERENT apostrophes the three PDFs use (U+02BC in the dahir, U+2019 in the CLEISS guide, plain in the code) | THE FIX IS NOT "USE BETTER GREP FLAGS", it is that the checker now refuses to report any absence until a CONTROL PROBE it knows is present comes back found -- proven by pointing the control at a string in no document and watching all three files report "the matcher is blind; every absent result below is meaningless". A route that cannot see returns exactly what a true absence returns. This is the project's own absence protocol failing in the one place it was being used to make 8 permanent claims, and it was caught only because the word looked wrong, not by any check
2026-08-30 | ST-19 | tests/unit/test_golden_set.py | THE FRENCH-LANGUAGE CHECK FAILED ON ITS FIRST RUN AND THE TEST WAS RIGHT. It asserted each question contains one of a hand-written list of accented letters, `éèêàùôûçîï`, and `g-in-008` ("À partir de quel âge...") carries only À and â -- neither in the list. Replaced with "does NFKD decomposition change the string", which needs no list | a list of characters is a list somebody has to remember to extend, and it would have failed silently in the other direction at ST-29 by passing a question that happened to contain one accent from the list while being English. Worth recording because the first-run failure was the cheap outcome: the same test written one row differently would have gone green and been believed
2026-08-30 | ST-29 | evaluation/golden/batch2.jsonl, evaluation/golden/README.md, tests/unit/test_golden_set.py | GOLDEN SET BATCH 2: +15 in-scope, +7 out-of-scope, RUNNING TOTAL 30 + 15, which is exactly ST-29's exit gate. Same method as ST-19: every reference answer written from article text extracted through the product's own `conversion.convert_file`, every out-of-scope term proven absent. DELIBERATELY REBALANCED toward the two CNSS documents -- 8 code / 4 dahir / 3 CLEISS against batch 1's 12/2/1, so the 30 in-scope rows sit at 20/6/4. Five rows are PAIRED with a batch-1 row (employer's vs employee's gross misconduct, severance scale vs who qualifies, pension amount vs pension conditions, the two different fourteen-week rules, retirement at 55 vs 60) because the pair is harder than either question alone | VERIFY: ruff exit 0; `uv run pytest` 541 passed / 2 skipped; `uv run python scripts/golden_grounding.py` -> "OK: 45 rows grounded (30 in scope, 15 out)"; 6 new mutations injected one at a time, all 6 killed. TWO ARTICLE-NUMBER COLLISIONS ARE NOW IN THE SET ON PURPOSE (code art. 53 vs dahir art. 53; code art. 33 vs dahir art. 33): a citation with the right number and the wrong document is still wrong, and F-03 makes the source line the contract with the user
2026-08-30 | ST-29 | tests/unit/test_golden_set.py | THE PER-BATCH COUNT TEST BECAME A TABLE INSTEAD OF A SECOND COPY, plus a test that refuses any golden file the table does not list. The plan builds this set in three batches, so copy-paste ends at three near-identical tests differing by two numbers, and the third copy is where the core law says abstract or write down why not. ST-35 now adds ONE ROW. Also added a cap on the running total: 40 in-scope and 20 out-of-scope, because G2 grades "20 refusals out of 20" and a 21st out-of-scope question makes the gate's own denominator a lie | THE COMPANION TEST IS THE HALF THAT MATTERS, and it was written by asking what the table cannot see: without it, ST-35 could add batch 3 and forget its row, and every count test would go on passing while grading a set nobody counted. Proven by dropping an unlisted `batch3.jsonl` into the folder and watching only that test go red. Per-batch document coverage was kept per-batch rather than over the whole set for the same reason -- a total would let one batch drift entirely onto the labour code
2026-08-30 | ST-29 | tests/unit/test_golden_set.py | THE FRENCH-LANGUAGE CHECK FAILED FOR THE SECOND TIME, ON ITS SECOND DESIGN, and again the test was right and the design was wrong. `g-out-009` is "Comment saisir le conseil de prud'hommes contre mon employeur ?" -- flawless French containing NOT ONE accented character, so the NFKD test that replaced the hand-written letter list rejected it. Third version: an accented character OR at least two distinct French function words, failing only when BOTH miss | THE CHECK WAS MEASURING ACCENTS AND CALLING THE ANSWER "FRENCH". Both earlier versions were proxies that happened to hold on the rows written so far, and each survived exactly until a row arrived that was correct in a way the proxy had not imagined. The marker list deliberately EXCLUDES English look-alikes ("a", "on", "par", "sans"): a marker an English sentence can contain makes the check weaker, not more generous. Re-proven by putting an English question in batch 2 and watching it go red
2026-08-30 | fix/ST-18-article-marker-substring | scripts/spike_st18.py, tests/unit/test_spike_st18_marker.py, docs/journal/BUILD-STATE.md | THE RIGHT-ARTICLE FIGURE IS WITHDRAWN AND THE MATCHER THAT PRODUCED IT IS FIXED. `probe.expect_marker in joined` was a plain substring test against bare numbers, so "14" scored a hit on "Article 143", "39" on "article 139", and BOTH "72" and "184" on "dahir n 1-72-184" -- which is the NAME OF ANOTHER DOCUMENT in the same workspace, so a probe expecting a labour-code article could be satisfied by retrieving a different file entirely. Four of eight markers collide. Also fixed: the answer path zipped a sorted SET of file names against an unsorted LIST of section labels, printing one document beside another document's article and dropping the remainder via strict=False, into traces.json -- the file ST-19's golden set is meant to be built from | found by a cold review of the two spike scripts, which NO test had ever exercised. That is the lesson rather than the bug: measurement code that is wrong does not crash and does not fail a gate, it reports a BETTER number than the truth and the number gets quoted. It survived a rule-5 review, a re-review and a green suite. `scripts/` now has its first tests, pinning both directions with a mutation back to the original substring test. THE NUMBER ITSELF CANNOT BE RECOMPUTED on YL's machine: data/ is absent and corpus.py fetch fails with CERTIFICATE_VERIFY_FAILED (TLS interception), which is not a thing to disable for a number
2026-08-30 | review of MB's six PRs | docs/journal/BUILD-STATE.md | REVIEWED #61-#66 (2,425 insertions) against the RUNNING code, not by reading: whole gate re-run on her main (529 passed, gitleaks clean, which closes the one gap she declared); 3 faults injected into her fixes and all 3 caught by the tests she named, so her 'proven in both directions' claim is reproduced independently; spec citations 4/4 true; published arithmetic 3/3 exact; her pathlib _IGNORED_ERRNOS claim verified against the installed source. Assessed 89% | the weighting is a judgement and is written down so it can be re-weighed; the measurements under it are not. The 11 points come off almost entirely for 1,331 lines of scripts/ that no test touches
2026-08-30 | review of MB's six PRs | docs/journal/BUILD-STATE.md | THREE BLOCKING FINDINGS LEFT OPEN with an owner and a fallback date rather than fixed: a paid `answer --yes` run writes traces.json AFTER its early return so any single error discards every provider call; re-running `index` rmtree's the directory that file lives in with no warning; and `corpus.py verify` prints EXIT GATE MET without ever checking the text is French, while ST-07's signed gate says 'French text selectable' and PRD section 17 names an ENGLISH edition of the same labour code | not fixed here because all three are ST-07/ST-18's own design decisions and the corpus is absent on this machine, so a fix could not be tested against anything. Parked and visible beats fixed-wrong
2026-08-30 | root cause of the ST-18 defect | pyproject.toml (read, not changed) | WHY IT GOT THROUGH IS A CONFIG LINE, NOT A HABIT: `testpaths = ["tests"]` puts scripts/ outside every check. MB has every skill and both agents -- all eight files under .claude/ are tracked, settings.json and its two hooks included -- and she used them, running two rule-5 reviews and mutation-proving her product code | the tools were not missing, they were all pointed at the product while the bug was in the RULER. Every one of them depends on a test existing: mutation testing cannot kill what nothing asserts, and the gate cannot fail on code it never runs. Measurement code that is wrong does not crash -- it quietly reports a better number than the truth
2026-08-30 | ST-27 | app.py, ui/ (ports, runs, conversation, screen, templates, static) | SANAD HAS A SCREEN. S1 chat, server-rendered Jinja off the FastAPI host (CR-02), no JS toolchain (ADR-10). Every S1 state from PRD section 8 renders: no-workspace, no-documents, empty, loading with real stage hints, error panel, all four message variants (user/answer/refusal/clarification), source cards, and the passage viewer with the cited span marked. 598 passed / 2 skipped, ruff exit 0 | THE UI CALLS ask() IN-PROCESS (ADR-13), so ST-51 is not a dependency and the screen holds the real Answer object rather than a JSON copy of it. The engine has been able to answer since ST-24; until this commit nothing could look at it
2026-08-30 | ST-27 | ui/runs.py | THE STAGE HINT IS READ AT THE PORT SEAM, NOT FROM A TIMER. Entering `retrieve` IS 'Searching the workspace', entering `grade` IS 'Checking the answer', entering `write_answer` IS 'Writing'. dataclasses.replace wraps the ports; agent/ is untouched | UX spec principle 3 bans faked progress in one sentence and the React reference breaks it with two setTimeout calls at 650ms and 1300ms (ChatScreen.tsx:111-112). PROVEN DISCRIMINATING: the same question is held at three different ports and must report three different labels; mutating two of the three stages to a fixed value killed exactly 2 of the 3 parametrised cases and left the third green, which is what a never-advancing timer would score
2026-08-30 | ST-27 | ui/runs.py, ui/conversation.py | THE PASSAGE VIEWER READS THE ARGUMENTS write_answer WAS CALLED WITH, not a second filter that reproduces make_answer's rule. The first draft recorded at fetch_parents and re-derived which hits were cited -- which is 'two filters agreeing', the exact shape agent/nodes.py:418 says it built one tuple to prevent. Changed before it shipped | a source card and the passage it opens are now two views of ONE list. Mutating the viewer to show the matched 500-char chunk instead of the section fails on the end-of-article marker, so 'search the small thing, read the big thing' (architecture 7.5) is checked rather than claimed
2026-08-30 | ST-27 | ui/static/sanad.css, tests/unit/test_ui_contrast.py | THE SIGNED PALETTE SHIPS, AND A TEST MEASURES IT FROM THE STYLESHEET. UX spec 3.4's hex values, not Tailwind's. Every colour role's contrast is computed from the bytes the browser loads -- not from a fixture copy, which is how a drift check goes green on a broken page | the build journal's own UI review recorded border-strong at 1.48:1 light and 1.88:1 dark in the React reference, against a 3:1 floor. PROVEN BY BREAKING IT: setting border-strong back to #cbd5e1 reproduces 1.48:1 and turns the test red. The test also re-derives the journal's four published ratios, so wrong arithmetic cannot pass every assertion in the same direction
2026-08-30 | ST-27 | app.py | FORMS ARE PARSED WITH urllib.parse.parse_qsl, NOT python-multipart. FastAPI's Form(...) requires that package at import time and Starlette asserts on it inside request.form() whatever the content type | it exists for multipart/form-data, the file-upload encoding, and UX spec 13 rules out file upload outright ('workspaces point at folders on disk'). A dependency added for a format the product refuses to accept. parse_qsl is the standard library's own decoder for urlencoded bodies -- not a hand-rolled parser
2026-08-30 | ST-27 | pyproject.toml, uv.lock | DECLARED httpx IN THE DEV GROUP, with the human's approval, so fastapi.testclient can drive the real routes in CI. Already installed transitively and already in uv.lock; re-lock resolved the same 195 packages and installed nothing new | an undeclared import works until the day the package that dragged it in stops depending on it. NOTED AND NOT ACTED ON: starlette 1.3.1 emits a deprecation warning asking for httpx2 instead. httpx works today; swapping it is a real dependency change and belongs to whoever picks it up deliberately
2026-08-30 | ST-27 | ui/ports.py | THE THREE UNBUILT SEAMS ARE STUBBED OUT LOUD, each naming its owner and its cost. summarize (ST-25) throws the history away, so F-07 memory does not work. clarify (ST-22) returns None, so THE CLARIFICATION VARIANT CANNOT APPEAR IN THE RUNNING APP -- the screen renders it and a test drives it through the real graph, but nothing live produces one. rewrite (ST-22) does not split, so a two-part question is one search | agent/ports.py's rule is that a plausible stub is the most dangerous object in this project. Writing them in the open, with the bill attached, is what stops 'the UI is done' from quietly meaning 'three features are silently absent'
2026-08-30 | ST-27 live run | (no code) docs/journal/BUILD-STATE.md | DEMONSTRATED LIVE, not by fixture: real workspace over data/corpus/hr, Sanad's own Sync indexed 2 documents / 217 pages / 118 parent sections in 1255s, server on 127.0.0.1:8000, real gemini-3.6-flash. Empty, loading (three stage hints sampled at t=1/3/4), error (server restarted with MODEL_MODE=banana), user, answer with 5 sources and the disclaimer, refusal with 7 searches disclosed, passage viewer showing Article 14's whole 3,811-char section with the 512-char retrieved chunk marked, and cancel leaving 'Incomplete -- not an answer'. Clarification and the two no-workspace states are TEST ONLY and say so | a test proves the state renders; only a live call proves the model is reachable, the model name is not retired, the templates resolve on disk and the French text is not mojibake. Checked that last one explicitly: the page decodes as UTF-8 with ZERO replacement characters and the accents are real codepoints
2026-08-30 | ST-27 | app.py | app.py CREATES THE SQLITE REGISTRY IN ITS LIFESPAN. Found by running it: with no data/sanad.db, GET / raised RegistryNotFoundError and served a stack trace instead of criterion 1's 'No workspace yet' -- the FIRST screen a new operator ever sees | no amount of reading would have found it, because every test had made its own database first. The test that now pins it deliberately does NOT, and mutating ensure_schema out reproduces the original error
2026-08-30 | ST-27 | ui/static/sanad.js | THE POLL UPDATES THE STAGE LABEL'S TEXT AND NEVER REPLACES THE NODE. An earlier draft swapped the whole conversation area each tick, which destroys and recreates the role=status aria-live element -- so UX spec 6.4's 'stage changes during loading are announced' silently stops being true, because a live region only announces changes to a node the screen reader was already watching | caught re-reading my own diff, not by a test, and it is the kind of defect that never shows up in a screenshot. Nothing else on the page can change mid-run anyway, so the swap bought nothing
2026-08-30 | ST-27 | (no code) docs/journal/BUILD-STATE.md | PARKED, NOT FIXED: a cold server is UNRESPONSIVE during its first search while the real encoders load -- minutes, during which even GET /chat/messages never reaches the log. Observed; the GIL is the likely cause rather than a proven one. Second question in the same process answers in seconds | the fix is a startup warm-up in app.py, which trades a slow boot for a fast first question, and that is the demo owner's call rather than this story's. For the defense the mitigation is one line in ST-30's script: ask a question before the jury walks in
2026-08-31 | ST-27 review fixes | ui/conversation.py, app.py | TWO LIVE RACES CLOSED. Starlette runs plain def routes on a threadpool and the page polls every 700ms, so `settle` (read-check-clear) had two threads append ONE answer TWICE -- into the transcript AND into `turns`, which fed a duplicated exchange back as F-07 memory -- and `_start` read `busy` then assigned `conversation.run` on a later line, so a double-clicked Send started TWO paid model runs and discarded the first. Now `Conversation.begin` and `Conversation.settle`, each one locked step. Pinned by tests that release 16 threads off a threading.Barrier | THE DOCSTRING WAS THE TELL: it said 'no second reader to race with'. One user is not one thread, and this screen generates its own concurrency. Found by both review passes independently; invisible to a green suite, to ruff and to every live run I did
2026-08-31 | ST-27 review fixes | ui/runs.py | CANCEL DID NOTHING DURING 'WRITING'. The checkpoint only fired when the NEXT port was entered and write_answer is the last one, so the flag set during the one stage a user actually waits through was never read. Now checkpointed after the write | the suite was green over it because the cancel test gated on `grade` and never on `write_answer` -- a test that exercises the easy instance of the property it names. Accepted cost, stated: a completed paid-for answer is discarded, which is what UX spec 6.3's 'stops after the current stage' means. Showing it anyway would make the button a lie
2026-08-31 | ST-27 review fixes | app.py, ui/templates/_sources.html, ui/templates/_conversation.html | A PASSAGE LINK RESOLVED AGAINST WHATEVER WORKSPACE WAS ACTIVE WHEN IT WAS FOLLOWED. Switch workspace, press Back, open a source card, and you read another workspace's section -- silently, looking entirely correct. That is F-01 isolation. The URL now carries the workspace id; a test follows the same message/card coordinates against a second workspace and requires the 'no longer on screen' page | third addressing scheme for this link and the third was earned: by card alone an older answer opened the newest answer's section; by message+card it crossed workspaces. Both failures look right on screen, which is what makes the extra path segment worth it
2026-08-31 | ST-27 review fixes | ui/conversation.py | THE ERROR PANEL COULD HAVE PRINTED THE API KEY. UX spec 5 requires 'the exact failing value' and Google AI Studio puts ?key=... in the request URL its SDK exceptions carry, so a failed call would render the key on screen and into any screenshot or projector. Redacted by EXACT CONFIGURED VALUE, not by guessing what a key looks like | core law: never log a secret. Exact-value matching cannot have false negatives against the one secret this process holds, and a regex for 'key-shaped' would both miss real keys and censor innocent text. The rest of the message survives, so the panel still names the failing value
2026-08-31 | ST-27 review fixes | ui/static/sanad.js, ui/templates/_conversation.html | THE POLITE LIVE REGION NEVER ANNOUNCED ANYTHING. The only path that added a message was a full page load, and a live region announces what is INSERTED into it, never what was already there at load. aria-live was correct markup doing nothing, and `assert role="status" in page` could not tell the difference. The poll now appends the finished answer into the existing list and re-wires the new cards' dialogs | UX spec 6.4 is 'new messages are announced through a polite live region'. The attribute is not the feature. The <noscript> path still reloads and still cannot announce; stated rather than papered over
2026-08-31 | ST-27 review fixes | ui/static/sanad.css, tests/unit/test_ui_contrast.py, app.py, ui/templates/base.html | THREE SPEC GAPS CLOSED: eight direction-locked CSS properties that would not mirror under dir=rtl (criterion 11), now logical properties with a sweep test that found them; UX spec 4's missing 'the conversation context has moved' notice on a workspace switch; and `.cited { padding: 0 2px }` off UX spec 3.2's spacing scale, now swept mechanically. Added the RTL preview UX spec 6.5 asks for (?dir=rtl), which is what makes criterion 11 checkable at all | the RTL faults were invisible in LTR, which is the whole problem: V1 ships LTR, so nothing an operator does today would ever reveal them. Both new stylesheet sweeps were proven by putting an offender back and watching them fail
2026-08-31 | ST-27 review fixes | app.py, tests/integration/test_s1_chat_screen.py | `vector_store.open_store()` IN THE LIFESPAN HAD NEVER EXECUTED UNDER TEST -- every test passes a ports_factory and takes the early return. It ran live twice by hand, which is evidence, not a check. Now covered. Also capped the posted form body at 64 KiB instead of buffering whatever arrives | this project's own law: a line that has never run is untested, not passing. The one line ADR-04's single-client promise rests on was the line nothing exercised
2026-08-31 | ST-27 escalation | (no code) docs/journal/BUILD-STATE.md | RAISED, NOT DECIDED: design principle 1 says a source card is 'never smaller than body text' (15px) and section 3.3 puts the Mono role -- which is exactly what file names and section labels are -- at 13px. A card's file name is both. An earlier sanad.css settled it in a code comment at 13px | that is the reinterpretation rule 1 forbids. Shipped at BODY size pending a ruling because between two signed readings the accessible one is safer, and principle 1 names this component by name. One line to reverse. ALSO DECLARED UNVERIFIED: criterion 4, focus returning to the invoking card, rests on <dialog>.showModal() and nothing here drives a real browser. Owner ST-38
2026-08-30 | merge day, ST-19 | (no code) docs/journal/BUILD-STATE.md | MERGED PR #67 (ST-19 golden set batch 1) as `7c28d6b`, squash, branch deleted. NOT MERGED ON ITS ORIGINAL GREEN: YL's #68 and #69 landed while it sat open, so main was merged INTO the branch (never rebased, per the 2026-07-28 decision), the whole gate was re-run by hand on the updated branch -- 554 passed / 2 skipped, up from 539 -- and CI `verify` re-ran and passed in 1m21s with all four steps before the merge button. The job's STEPS were listed rather than the green tick trusted: Lint, Tests and Secret scan all `success`, which is the only place step 4 executes on this setup | MERGED WITHOUT THE RULE-5 PARTNER REVIEW, at the human's explicit instruction ("push all changes to github and merge them"). Same shape as the ST-17 deviation of 2026-08-23 and ST-21's of 2026-08-26, and it creates the same debt. The concern was raised before acting and the instruction was not withdrawn, so it proceeded; ST-19 and ST-29 now join ST-21 and ST-23 on the unpaid-review list. What was NOT reviewed is the part that matters: the eight out-of-scope questions, which are the rows G2 is graded on
2026-08-30 | ST-29 | docs/journal/BUILD-STATE.md | RECORDED THAT ST-19'S GOLDEN SET WAS NOT BUILT FROM traces.json, because #68's own journal line says it was. #68 fixed a spike matcher that paired one document's name with another document's article and called traces.json "the file ST-19's golden set is meant to be built from". It was not: every reference answer in batches 1 and 2 came from article text pulled out of the PDF through the product's own `conversion.convert_file`, and `scripts/golden_grounding.py` re-reads the documents rather than trusting any intermediate file | left uncorrected, that line would have put a question mark over 45 rows that the defect never touched -- and the honest half is that the safer route was as much luck as design, so it is now written down as the route rather than discovered again. ALSO INHERITED YL'S ROOT CAUSE: `testpaths = ["tests"]` puts scripts/ outside every check, and `golden_grounding.py` is one more file in that blind spot. It has been RUN and its output quoted with a date, but nothing asserts it; its control probe is all that stands in for a test. Owner MB
2026-08-31 | ST-27 | tests/unit/test_ui_conversation.py | MY OWN RACE TESTS WERE VACUOUS AND I FOUND IT BY RUNNING THE MUTATION, not by reading them. The first version released 16 threads off a threading.Barrier and asserted the outcome -- and it PASSED with both locks taken out, because CPython does not switch threads inside a check that does no I/O and allocates nothing. It was hoping for an unlucky interleaving. Rewritten so the `done` read sleeps 20ms: without the lock every thread sleeps at once and acts on the same stale answer (now `assert 3 == 1`, twice); with it they queue. Green with the locks, red without | I had already written 'pinned by tests that release 16 threads off a barrier' into the commit message, BUILD-STATE and a message to the reviewer. All three were wrong. A barrier INSIDE the critical section is not the fix either -- with the lock held the correct code would deadlock -- which is why the sleep sits in the property both critical sections read first
2026-08-31 | merge day | (no code) docs/journal/BUILD-STATE.md | MERGED PR #71 (MB's re-stamp) THEN PR #72 (ST-27), squash, both branches deleted. #71 was reviewed before merging rather than waved through: her header figure of 556 passed on main was CORROBORATED BY ARITHMETIC rather than trusted -- the merged tree runs 621 and `pytest --collect-only` counts exactly 65 ST-27 tests, and 621-65=556 to the test. Her four-PR list checked against git log, 4/4 correct | #72 could not run CI AT ALL until it was merged with main: main had moved (ST-19, ST-29) and the PR was CONFLICTING, which is why statusCheckRollup was empty rather than red. A conflicting PR gets no merge ref, so the pull_request workflow never fires -- an empty check list is NOT a green one, and it reads exactly like a gate nobody has configured
2026-08-31 | merge day | docs/journal/BUILD-STATE.md | RE-STAMPED THE HEADER AT c70c654 with all four gate steps measured on MAIN after both merges: uv sync --frozen clean 170 packages, ruff_exit=0, PYTEST_EXIT=0 with 621 passed / 2 skipped in 375.52s, and gitleaks detect --no-git over the whole tracked tree (112 files, 2.65 MB) 'no leaks found' exit 0 | FIRST HEADER ON THIS PROJECT THAT CAN SAY ALL FOUR RAN LOCALLY -- gitleaks is installed on YL's machine and absent on MB's. The scope is stated precisely rather than compressed: the local scan covers every tracked file AS IT STANDS, not the git history, because a full-history scan timed out twice at nine minutes. History is covered by CI's gitleaks-action on #72. Three previous headers were vaguer than the command they quoted
2026-08-31 | merge day | docs/journal/BUILD-STATE.md | RESOLVED THE SAME BUILD-STATE CONFLICT TWICE, once against 9da0c56 and again against #71, and both times by keeping MB's newer queue as the base and folding ST-27 into it rather than overwriting. Two of her three entries were closed by her own merges; ST-32 survives and OUTRANKS both UI stories, which is now written into the queue in those words | the conflict was the append-only journal working as designed -- CHANGELOG-AI.md and DECISIONS.md auto-merged both times because nothing edits an existing line there. BUILD-STATE conflicts because it is the one journal file that REWRITES sections, and its Next queue is the section both people touch
2026-09-01 | ST-35 | evaluation/golden/batch3.jsonl, tests/unit/test_golden_set.py, evaluation/golden/README.md | GOLDEN SET COMPLETE AND FROZEN at 40 in-scope + 20 out-of-scope. Batch 3 adds 10 in-scope (6 labour code, 2 CNSS dahir, 2 CLEISS guide, so the 40 sit at 26/8/6) and 5 out-of-scope, plus the EXPECTED_COUNTS row ST-35 owns. Every reference answer was written from article text pulled through the product's own `conversion.convert_file`, never from memory. VERIFIED: `uv run pytest tests/unit/test_golden_set.py` 13 passed exit 0; `uv run python scripts/golden_grounding.py` "OK: 60 rows grounded (40 in scope, 20 out)" exit 0, control probe found in all three files first | the set is what F-08 and G2 are graded against, and ST-36 cannot start until it stops moving. THREE NEW TRAPS, all of a kind only a SOURCED-answer product can fail: one sick day owes the employer 48 hours (code art. 271) and the CNSS 30 days (dahir art. 33), so the same event has two deadlines in two documents; the two article 33s are now BOTH rows rather than a note on one; and "54 jours de cotisation" appears three times with three different windows (6 / 10 / 6 months) for three different benefits, so the number alone does not identify the entitlement
2026-09-01 | ST-35 | evaluation/golden/README.md | AN OUT-OF-SCOPE QUESTION WAS WRITTEN, CHECKED, AND THROWN AWAY, and the checker is what caught it. "Ai-je droit a une indemnite pour perte d'emploi ?" looks like the ideal refusal -- unemployment insurance is not what a 1972 dahir or a 2011 labour code is about. The probe came back PRESENT THREE TIMES (code=2, cleiss=1): the labour code grants `l'indemnite de perte d'emploi` twice and the CLEISS guide carries a paragraph on the scheme WITH ITS CONTRIBUTION RATES (0,38% employer, 0,19% employee) | scored as out-of-scope it would have made G2 read 19 refusals out of 20 -- a release gate failing for a reason nobody reading the report could have found. Second time on this story line that a confident claim of absence was false, and the second time the script rather than a human is what noticed. THE CANDIDATES WERE PROBED BEFORE ANY ROW WAS DRAFTED, not after, which is why this cost ten minutes instead of a review round
2026-09-01 | ST-35 | evaluation/golden/README.md | A SECOND QUESTION WAS DROPPED THAT THE CHECKER PASSED, and that is the more interesting half. "Un pere a-t-il droit a un conge de paternite ?" -- the term appears NOWHERE in the three documents, so the grounding script says out-of-scope and is correct about the term. Dropped anyway: the corpus answers the question under ANOTHER NAME, since g-in-015 is the father's three-day `conge de naissance`. A refusal would have been the wrong answer | term-absence is NECESSARY AND NOT SUFFICIENT. The checker can only see the string it was given; whether the corpus can answer the question under any name is a judgement only a human reading the rows can make. Recorded because the checker's green is exactly the kind of green this project keeps learning not to over-read -- see the ST-19 byte-grep row above, which is the same lesson from the opposite direction
2026-09-01 | ST-35 | tests/unit/test_golden_set.py | THE RUNNING-TOTAL TEST WAS STRENGTHENED FROM `<=` TO `==`, and a FROZEN_IDS check was added that pins the sixty ids as a CONTIGUOUS run, gated on a new GOLDEN_SET_VERSION constant. 19 mutations injected one at a time on a committed-green checkpoint, ALL 19 KILLED, including the two that matter most: g-in-040 renumbered to g-in-041 (the freeze), and an out-of-scope probe repointed at `perte d'emploi` (the real defect above, re-run as a mutation) | while the set was being BUILT in batches a cap was the only honest assertion -- 23 rows and 45 rows both had to pass. Now that it is complete an UNDER-count is a defect too and a cap cannot see one. STATED IN THE TEST'S OWN DOCSTRING RATHER THAN LEFT TO BE DISCOVERED: FROZEN_IDS does NOT pin the WORDING of a question or a reference answer. Pinning the bytes would have, and was deliberately refused -- the file is checked out on two machines and git rewrites its line endings on checkout (the commit for this very batch printed "LF will be replaced by CRLF"), so a hash would fail for a reason having nothing to do with the golden set
2026-09-03 | ST-35 review | tests/unit/test_golden_set.py | THE FREEZE DID NOT HOLD AND NOW IT DOES. `assert GOLDEN_SET_VERSION == "v1"` compared a constant to a literal in the same file, so it could not fail; FROZEN_IDS was derived from FINAL_TOTAL_*, so it was not an independent record either. Adding a 41st question needed two number edits and the file went green with the version untouched -- the review RAN that mutation and watched 13 of 13 pass. Replaced with `FROZEN_TOTALS = {"v1": (40, 20)}` plus a test binding the live totals to the frozen record. VERIFIED by deliberate violation: with the 41st row and both edits applied the suite now fails on `test_the_totals_still_match_the_version_they_were_frozen_under` (exit 1), and green again once restored (14 passed, exit 0) | this is the rule-5 review pass finding the defect a green gate could not. The story's own exit gate says "later edits need a new version", and that was being reported as met by a check that could never go red. Residual limit written into the docstring and the README rather than left to be discovered: editing the "v1" row itself is still possible, but it is tampering visible in a diff, not the tuning-session drift the table exists to stop
2026-09-03 | ST-35 review | evaluation/golden/batch3.jsonl, evaluation/golden/README.md | THREE FACTUAL ERRORS IN THE NOTES, each repeated into three artifacts. "54 jours de cotisation appears three times with three different windows (6/10/6)" is wrong twice over: it appears FOUR times in the CLEISS guide alone and the windows are 6, 6, 10, 6, so TWO distinct windows, not three. g-in-036's "huit durees" in labour code article 274 is SIX. And g-out-016 claimed the pension at 60 and the retraite anticipee both require "cessation de toute activite salariee" -- the corpus says that only for the pension at 60 (one occurrence); the anticipee paragraph asks for the employer's agreement, 3 240 days and 54 days over 6 months, with no cessation clause | all three re-counted on 2026-09-03 by re-reading the PDFs through the product's own `conversion.convert_file` with a control probe found first, per this project's absence protocol. A golden set is a list of claims about documents; a claim in a notes field is still a claim, and these had already been copied into README, BUILD-STATE and CHANGELOG before anyone re-counted
2026-09-03 | ST-35 review | evaluation/golden/batch3.jsonl, evaluation/golden/README.md | g-out-020's REFUSAL IS CORRECT AND ITS REASONING WAS NOT. The row rested its absence claim on the single word `quota` being missing, which is exactly the error that got "conge de paternite" dropped two entries above. Re-established by reading the chapter: labour code articles 166 to 171 impose real duties toward disabled workers (maintien dans l'emploi, travaux interdits, examen medical, accessibilite, amendes) and there is still NO hiring quota under any wording -- `quota` 0 hits, `obligation d'emploi` 0, `tenu d'employer` 0, no effectif proportion. Reference answer rewritten to describe what the corpus actually holds; note rewritten to say the chapter was read | the reference answer for an out-of-scope row is what a correct refusal must convey, so one that misdescribes the corpus teaches the grader the wrong thing. Kept rather than replaced, and listed as arguable instead -- see the DECISIONS row
2026-09-03 | ST-35 review | evaluation/golden/README.md | THE ARGUABLE-ROW CAVEAT IS NOW A THREE-ROW TABLE, not a sentence about g-out-005. g-out-019 (titres-restaurant, against dahir article 19's "tous autres avantages en argent" catch-all) and g-out-020 (disability quota, against articles 166-171) join it. The README previously said g-out-005 was "the set's one arguable row" and that "a second one would have been a choice, not an accident" -- batch 3 shipped two more, and that sentence is corrected rather than deleted | G2 grades 20 refusals out of 20, so a model that answers one of these WELL costs the release gate a point. Naming them now means the ST-36 conversation is about the question rather than about retrieval, which is the whole purpose the caveat section was created for
2026-09-03 | ST-35 review | evaluation/golden/batch3.jsonl, evaluation/golden/README.md | TWO DOCUMENTATION CONTRADICTIONS CLOSED. The batch-2 section claimed "two article-number collisions are now in the set" and counted the two article 33s, but at batch 2 only the code's article 33 was a row (g-in-017); the dahir's arrives with g-in-037 in batch 3, which the batch-3 section then announced as new. Both cannot be true. AND batch 3 created an undocumented THIRD collision nobody had noticed: g-in-032 is labour code article 37, g-in-025 is dahir article 37 | found by the two review passes independently. A collision that is deliberate in one paragraph and news in another is a document nobody can trust on the point it exists to make, and an undocumented one is a trap the set sets for itself rather than for the model
2026-09-03 | ST-35 review | evaluation/golden/README.md | The paternity write-up said the words `conge de paternite` "appear nowhere in the corpus". True of the phrase, and it understates the case: the WORD `paternite` appears once, in labour code article 269 -- which is the birth-leave article that answers the question. Corrected in place | the entry's whole point is that term-absence is necessary and not sufficient. Stating the absence more loosely than the corpus supports weakens the very lesson it is teaching, and the sharper version is a better warning: the phrase is absent, the topic is one word away
2026-09-03 | ST-35 | docs/journal/BUILD-STATE.md | RE-STAMPED THE HEADER AT MERGE TIME, at `617c972` on main after #74 squash-merged, and re-ran the gate ON MAIN rather than trusting the branch's green: `uv sync --frozen` clean (170 packages), `uv run ruff check .` exit 0, `uv run pytest` 623 passed / 2 skipped in 169.49s, `uv run python scripts/golden_grounding.py` "OK: 60 rows grounded (40 in scope, 20 out)" exit 0, every exit code read from `$?`. Step 4 gitleaks NOT run locally -- absent on this clone by three routes -- but CI `verify` ran all four steps green on #74's final push (run 33796546493, secret scan included), so the tree is covered by the two halves rather than by one claim. Next queue rewritten: ST-32 is now unambiguously the top of the critical path | the header's own rule is to take the number at the last possible moment, because a green branch and a green main are different claims. Also recorded: the review that preceded this merge ran on MB's own clone for MB's own story, so rule 5 is met in form and not necessarily in substance; the human was told before merging and chose to proceed, which makes it a decision rather than an oversight
